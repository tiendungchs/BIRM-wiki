# Want-list — sources to acquire

Produced by the `wiki-acquire` skill, part A. Consumed by the human's Obsidian Web Clipper.
Nothing is clipped that is not on this list; every row names the registry row it settles.

**This file holds the ACTIVE want-list only.** A row leaves it the moment it is filed
(it is then tracked in `_work/manifest.tsv` and `_work/ingest-queue.md`) or the moment it
is judged unreachable (recorded once under the wave's *Not acquired* line in
`_work/ingest-queue.md`, then dropped). No archive, no history — the gitlog is the changelog.

**Access:** institutional (UBO Brest) — a paywall is not a filter. The filter is **HTML vs PDF**:
a PDF conversion is lossy on exactly the equations and figures the wiki needs.

**Route column:** `clip` = human, Obsidian Web Clipper · `self` = Claude fetches and writes `raw/` ·
`pdf` = last resort, `./tools/pdf2md.sh`, flagged `LOSSY`.

**Status column:** `open` → `clipped` → `filed` (then the row is deleted from this file).

After clipping, drop the files in `raw/` and run:

```bash
./tools/clip-check.sh              # validate every untracked file in raw/
./tools/clip-check.sh --manifest raw/<file>.md   # then file the manifest row
```

---

## Active

### Wave 20 — the starved bands: measurement validity, and the six `L0` gaps whose artefact the wiki has never held

Anchor: `P29` at [[wiki/priority-tasks.md]] and `L27` at the same page. The spec has consumed `L1` 93% and `L2` 97%, and the two bands the ladder works *first* stand at `L0` 39% and `L0-INSTR` 13%. `L0-INSTR` is now the largest band in the wiki — **70 rows across both registries** — and `L17` states why no ingest has moved it: the tension criteria close on *experiments*, not on readings. So this wave does not chase criteria. It buys the two things a reading actually can buy: **the instrument-validity papers themselves** (block A), and **the missing artefact behind an `L0` gap the wiki argues about second-hand** (block B). Block C discharges the five acquisitions that have been `Blocked on: human curation or a web-search pass` for nine passes.

Every URL below was probed. Verdicts are recorded in the `Route` column; three probes returned a bot-block (`403`) rather than a stub and are marked as such.

#### Block A — `L0-INSTR`: does the instrument measure what it names

| # | Target | Clip URL | Venue | Route | Closes | Settles | Status |
|---|---|---|---|---|---|---|---|
| 1 | Ericsson, Gouk & Hospedales 2021, *How Well Do Self-Supervised Models Transfer?* | `https://ar5iv.labs.arxiv.org/html/2011.13377` | CVPR 2021 (ar5iv, full MathML) | `clip` | `T310` | **13 self-supervised models × 40 downstream tasks, ranked under linear probe *and* under fine-tuning.** `T310` asks whether a frozen linear probe ranks representations at all; the wiki's flagship number (I-JEPA 66.9 vs 40.7) is a single-depth readout. Probe note, recorded honestly: the paper does *not* itself report the probe-vs-finetune rank correlation — it reports both rankings, which is the raw material for it, and `T310`'s `Closes when` asks for exactly one re-run at non-zero readout depth | `open` |
| 2 | Garrido, Balestriero, Najman & LeCun 2023, *RankMe: assessing the downstream performance of pretrained self-supervised representations by their rank* | `https://arxiv.org/html/2210.02885` | ICML 2023 (native arXiv HTML) | `clip` | `G108`, `T310` | **`G108` is `OPEN` and this is a direct candidate answer**: `RankMe(Z) = exp(−Σ p_k log p_k)`, `p_k = σ_k(Z)/‖σ(Z)‖₁ + ε` — a label-free criterion that ranks checkpoints within 0.5 points of label-guided linear evaluation, and *beats* it out of distribution. `G108`'s `Closes when` wants a quantity computable from model and unlabelled data that ranks runs by a structural property; effective rank is that quantity, and the wiki can then ask whether rank is *structural* or merely anti-collapse ([[wiki/concepts/representational-collapse.md]], `P10`) | `open` |
| 3 | Zeraati, Engel & Levina 2022, *A flexible Bayesian framework for unbiased estimation of timescales* | `https://www.nature.com/articles/s43588-022-00214-3` | Nat. Comput. Sci. 2:193–204 (cookie-gated → institutional) | `clip` | `T323` | **`T323` cannot be run because the estimator it needs does not exist in the wiki.** The row's `Closes when` demands a *per-neuron* autocorrelation `τ` fit, identifiable at realistic trial counts, against the population fit ([[wiki/concepts/timescale-hierarchy.md]] holds Murray et al. 2014's population fit and nothing else). This paper is the instrument: it shows the standard direct-fit procedure is biased by finite sample size, and replaces it with an OU-mixture generative model plus adaptive ABC returning a *posterior* over `τ`. Probe note: the paper applies it to *pooled* activity, so it supplies the estimator and not the measurement — which is the correct division of labour for an `L0-INSTR` row. Open fallback if the institutional clip fails: `https://www.biorxiv.org/content/10.1101/2020.08.11.245944v2.full` | `open` |
| 4 | Laumann et al. 2017, *On the stability of BOLD fMRI correlations* | `https://pmc.ncbi.nlm.nih.gov/articles/PMC6248456/` | Cereb. Cortex 27(10):4719–4732 (PMC, open) | `clip` | `T240` | **`T240` asks whether a resting-state functional network is an object or an artefact of the integration window, and says no fcANN result reports the stability.** This is the paper that ran the null: stationary simulated data reproduce the apparent "dynamic" state structure, and >98% of day-to-day correlation variability in a single individual is sampling error. It converts `T240` from an open question into a stated null the wiki's brain-network entity pages must be re-read against | `open` |
| 5 | Chari & Pachter 2023, *The specious art of single-cell genomics* | `https://journals.plos.org/ploscompbiol/article?id=10.1371/journal.pcbi.1011288` | PLOS Comput. Biol. 19(8):e1011288 (open HTML) | `clip` | `T159` | **`T159` — is a geometry that appears only after dimensionality reduction a property of the representation or of the projection?** — is asserted on five pages and has no source that *measures* the distortion. This one does, outside neuroscience and therefore without the field's own stake in the answer: neighbour Jaccard distance >0.7 after 2-D embedding, cell-type neighbour-rank correlation ≤0.4, max/min distance ratios inflated 4–200×, and the Johnson–Lindenstrauss bound that 10,000 points need ~1,842 dimensions for 20% distance fidelity. `T159`'s `Closes when` says the discarded remainder must be characterised; this characterises it | `open` |

#### Block B — `L0` gaps the wiki argues about and holds no artefact for

| # | Target | Clip URL | Venue | Route | Closes | Settles | Status |
|---|---|---|---|---|---|---|---|
| 6 | Cai, Shin & Song 2017, *Making neural programming architectures generalize via recursion* | user downloads PDF → `./tools/pdf2md.sh` (`arXiv:1704.06611`) | ICLR 2017 (ar5iv conversion fails: *"Fatal error … document may be truncated"*; no HTML anywhere) | `pdf` | `G70` | **`G70` is the wiki's cleanest unanswered `L0` gap — every induced program in it is a straight-line composition, so no program can extrapolate a count — and the artefact that answers it has never been filed.** Recursion inside the Neural Programmer-Interpreter, with base cases and reduction rules *verified* rather than tested, giving provable generalization on addition and quicksort. It is the one case in the literature where a trip count is not fixed at synthesis time. Text-and-pseudocode heavy, so the `LOSSY` flag costs little here | `open` |
| 7 | Rosas, Mediano, Gastpar & Jensen 2019, *Quantifying high-order interdependencies via multivariate extensions of the mutual information* | `https://ar5iv.labs.arxiv.org/html/1902.11239` | Phys. Rev. E 100:032305 (ar5iv, full MathML) | `clip` | `G105` | **`G105` says every graph-discovery mechanism in the wiki estimates pairs, and its `Closes when` asks for an `n`-ary relation inferred directly from observations rather than by thresholding pairwise scores.** The O-information `Ω(Xⁿ) = (n−2)H(Xⁿ) + Σ_j [H(X_j) − H(X⁻ʲ)]` is exactly that estimator: symmetric, needing no predictor/target split, and — unlike partial information decomposition — scaling gracefully with `n`. It gives `G105` its first computable discriminator between synergy and redundancy | `open` |
| 8 | Wolff, Jochim, Akyürek & Stokes 2017, *Dynamic hidden states underlying working-memory-guided behavior* | `https://www.nature.com/articles/nn.4546` | Nat. Neurosci. 20(6):864–871 (cookie-gated → institutional) | `clip` | `G112`, `T2` | **`G112` states that the synaptic and activity solutions to structured sequence memory are provably the same function and asks for a protocol — inputs and scored outputs only — that separates them.** This is the nearest thing anyone has built: *perturb and decode*, "pinging" the network during maintenance and recovering item-specific information from the impulse response when delay activity is absent. Whether that counts as behavioural or as decoding is itself the ruling `G112` needs, and it cannot be made without the paper. Also re-prices `T2` (is the fast level a separate store or recurrent activity?), `LIVE` with 10 citers | `open` |
| 9 | Mongillo, Barak & Tsodyks 2008, *Synaptic theory of working memory* | `https://www.science.org/doi/10.1126/science.1150769` | Science 319(5869):1543–1546 (probe returned `403` bot-block, not a paywall verdict; full HTML through the institution) | `clip` | `G112`, `T2` | **Row 8's opposing position, and the wiki cites it second-hand on the plasticity pages without ever holding it.** Calcium-mediated presynaptic facilitation as the memory buffer, loaded and refreshed by low-rate spiking. `G112`'s duality argument is only as good as its two halves; filing 8 and 9 in the same wave is what lets the ingest state the duality with both mechanisms in hand rather than one | `open` |
| 10 | Golubeva, Neyshabur & Gur-Ari 2021, *Are wider nets better given the same number of parameters?* | `https://ar5iv.labs.arxiv.org/html/2010.14495` | ICLR 2021 (ar5iv, full MathML) | `clip` | `G101` | **`G101`'s `Closes when` is "an experiment scales edge density at fixed unit count and reports what the higher synapse-per-unit ratio buys, separating it from the effect of adding units" — and this is that experiment, run in the machine direction.** Width is grown with the weight count held fixed by static random sparsity; width, not weight count, carries most of the gain, optimally at 1–10% connectivity. It is the first result in the wiki that can price a units-vs-connections trade, and it bears on `P5` (units-to-competence) and `T234` | `open` |
| 11 | Elston 2003, *Cortex, cognition and the cell: new insights into the pyramidal neuron and prefrontal function* | `https://academic.oup.com/cercor/article/13/11/1124/274054` | Cereb. Cortex 13(11):1124–1138 (abstract-only to the probe → institutional HTML) | `clip` | `G101` | **Row 10's biological twin, and the measurement `G101` was opened on.** Prefrontal pyramidal cells carry up to **23×** the dendritic spines of V1 cells — the per-unit connection count varying by an order of magnitude *within one brain*, at roughly constant cell type. `G101` was written from a 2.75×-volume-on-1.25×-neurons figure quoted second-hand; this is the primary measurement of the quantity, and it is what makes the gap a number rather than an intuition | `open` |
| 12 | Wang et al. 2023, *Learning to grow pretrained models for efficient transformer training* (LiGO) | `https://arxiv.org/html/2303.00980` | ICLR 2023 (native arXiv HTML) | `clip` | `G100`, `P24` | **`G100` says no architecture in the wiki has a growth schedule, and closes when a grown model beats a full-size-from-step-0 twin at matched final size.** LiGO is the cleanest instance: a *learned* linear map from the small model's parameters to the large one's initialisation, factorised into width- and depth-growth operators, saving 44.7% (BERT-Base), 47.2% (RoBERTa) and 55.4% (DeiT-B) of the FLOPs to reach the same performance. It gives [[wiki/concepts/developmental-heterochrony.md]]'s per-module phase-offset vector a machine-side counterpart that can actually be run | `open` |

#### Block C — the five acquisitions blocked for nine passes

| # | Target | Clip URL | Venue | Route | Closes | Settles | Status |
|---|---|---|---|---|---|---|---|
| 13 | Bengio, Louradour, Collobert & Weston 2009, *Curriculum learning* | `https://dl.acm.org/doi/10.1145/1553374.1553380` | ICML 2009, 41–48 (probe returned `403` bot-block; ACM renders full HTML to an institutional session) | `clip` → `pdf` if the session fails | `L20`, `P26`, `G100` | **`L20` has been carried unmoved for nine passes: `curricul*` is on 39 wiki pages, reaches 3+ mentions on eight, and is owned by none — always an assumed lever, never a claim with evidence.** This is the artefact the term traces to. `S7`'s converse says the fix for breadth-without-depth is an acquisition, not a synthesis; this is that acquisition | `open` |
| 14 | Orhan, Gupta & Lake 2020, *Self-supervised learning through the eyes of a child* | `https://ar5iv.labs.arxiv.org/html/2007.16189` | NeurIPS 2020 (ar5iv, full MathML) | `clip` | `L20`, `P26`, `G32` | **`P26` asks for the machine lever and the biological schedule to land together rather than the lever alone — this is the second half.** ~500 h of head-mounted video from three children aged 6–32 months (221/141/137 h), self-supervised, yielding high-level categorisation and invariance from a developmentally realistic input *stream*. It is also the wiki's first source that treats the experience stream as a designed object, which is `G32` (`Nothing designs the experience stream`, `OPEN`, 13 citers) | `open` |
| 15 | Blundell et al. 2016, *Model-free episodic control* | `https://ar5iv.labs.arxiv.org/html/1606.04460` | arXiv:1606.04460 (ar5iv, full MathML) | `clip` | `P20` | **The last artefact on `P20`'s second-hand list.** DQN, progressive networks, EWC, neural episodic control and MCTS are all now paged from primaries; MFEC is cited through [[wiki/entities/neural-episodic-control.md]]'s baseline section and nowhere else. `Q_EC(s,a) ← R_t` on first sight, `max{Q_EC(s,a), R_t}` thereafter — a non-parametric maximiser with no averaging, which is a *different* memory contract from everything on [[wiki/concepts/complementary-learning-systems.md]], and the reason the row was worth keeping open. Closing it closes `P20` entirely | `open` |
| 16 | Sagawa, Koh, Hashimoto & Liang 2020, *Distributionally robust neural networks for group shifts* | `https://ar5iv.labs.arxiv.org/html/1911.08731` | ICLR 2020 (ar5iv, full MathML) | `clip` | `I1`, `P19`, `G6` | **`I1` is the first instrument in the inventory and has had no primary source for its image half for nine passes.** `P19` named Shift-MNIST / biased CelebA / unfair dSprites; the probe found no paper that *constructs* them — they are named in Geirhos et al. 2020, which the wiki already holds. Waterbirds is the same instrument with a real primary: the confound is planted by construction (95% of waterbirds on water, 5% on land) and the output is the worst-group drop — ERM 60.0%, regularised ERM **21.3%**, regularised group-DRO 84.6%. That 60 → 21.3 inversion under *more* regularisation is a finding the wiki has nothing like, and it bears on `G6` (spurious edges are the default, not the exception) | `open` |
| 17 | Baxter 2000, *A model of inductive bias learning* | user downloads PDF → `./tools/pdf2md.sh` (`arXiv:1106.0245`, or the JAIR PDF) | JAIR 12:149–198 (JAIR is abstract + PDF only; ar5iv conversion fails) | `pdf` | `L20`, `P17`, `G12` | **`L20` found its twin by the same scan and this pass is the first chance to act on it: `inductive bias` sits on 37 wiki pages, reaches 3+ mentions on six, and is owned by none — the identical broad-and-shallow signature as `curricul*`.** `P17`'s acquisition has to own the term before it can ask `T283`'s question about routers. This is the formal treatment: hypothesis-space families, bias learned from an environment of related tasks, and sample-complexity bounds showing how many tasks and how many examples per task the bias costs. Heavy on theorems, so `LOSSY` is a real cost — flag it and check the bounds by hand at ingest | `open` |

**Counts.** 17 targets: **14 `clip`** (one of which, row 13, falls back to `pdf` if the institutional session does not render ACM), **2 `pdf`** (rows 6 and 17 — no HTML exists anywhere for either), **0 `self`** (nothing in this wave is an equation-light open page; every row is a research article).

**What each block closes.** Block A puts five primary sources under the wiki's largest and least-worked band — `T310`, `G108`, `T323`, `T240`, `T159`, five of the 70 `L0-INSTR` rows, and the first five that a *reading* can move rather than an experiment. Block B hands six `L0` gaps the artefact they have been argued about without: `G70` (no loop induction), `G105` (no `n`-ary relation), `G112` and `T2` (the two carriers, both halves in one wave), `G101` (edge density, machine side and biological side paired), `G100` (growth schedule). Block C empties the `Blocked on: human curation or a web-search pass` column: `P20` closes outright, `P19`/`I1` gets its image-half primary, and `L20` plus its twin get the two owners the term has been borrowed without.

**Order of ingest**, once filed — `S`urvey → `F`ramework → `M`echanism/`B`enchmark → `R`esult → e`X`position, oldest first within tier: Baxter 2000 (F) · Mongillo 2008 (F) · Bengio 2009 (F) · Elston 2003 (M) · Cai 2017 (M) · Rosas 2019 (M) · Blundell 2016 (M) · Sagawa 2020 (B) · Wolff 2017 (R) · Laumann 2017 (R) · Ericsson 2021 (R) · Golubeva 2021 (R) · Orhan 2020 (R) · Zeraati 2022 (R) · Chari 2023 (R) · LiGO 2023 (R) · RankMe 2023 (R).

After clipping, drop the files in `raw/` and run `./tools/clip-check.sh`, then `--manifest` each file.
