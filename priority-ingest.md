# Current ingests

`_work/ingest-queue.md` hold the full ingest queue. This file contain only the current wave. Once a wave is fully ingested, replace the wave below with a new one.

## Wave 24 — the parse itself: visual understanding as a mechanism, not a substrate

Anchor: the want-list's wave 24, from a QUERY pass asking whether visual understanding is covered. The answer was an asymmetry between the registries and the pages — seven rows name perception as the bottleneck (`T215`, `T191`, `T151`, `G75`, `G73`, `G27`, `T154`) while `wiki/concepts/` holds no page for any visual mechanism (0 files for *visual routine*, *active vision*, *perceptual grouping*, *object file*, *FINST*, *mental imagery*, *what-where*). Block A (`ullman`, `ballard`, `pylyshyn`, `kravitz`, `nieder`, `pearson`) buys the mechanism vocabulary; block B (`rahmanzadehgervi`, `hu`, `acuaviva`, `wang`) prices it on ARC-format tasks. 11 of 12 want-list targets landed — validated by `./tools/clip-check.sh` (0 FAIL, 6 WARN), manifest rows 453–463; clips arrived under their article titles and were renamed to `author-year-slug` before validation. Three arrived as PDF (`ullman`, `ballard`, `pylyshyn`) and were converted with `./tools/pdf2md.sh`, all flagged `LOSSY`: `ullman-1984` is an OCR scan (visible character errors — check every equation and figure caption by hand), and `pylyshyn-2001` came back as mojibake under `pymupdf4llm` and was re-converted with plain `pymupdf` text extraction, so it carries page markers and no heading structure. Two pairs are deliberately opposed: `ullman`/`mollard` (declared vs learned grouping criterion) and `hu-2025`/`acuaviva-2025` (is the visual prior trainable from the task alone, or must it be pretrained), the second to be ingested as a tension candidate. `hu-2025` is the paper the want-list mis-attributed to "Li et al." — first author is Hu. Expected pages: `wiki/concepts/visual-routines.md`, `wiki/concepts/visual-indices.md`, `wiki/concepts/active-vision.md`, `wiki/concepts/mental-imagery.md`, `wiki/concepts/numerosity.md`, `wiki/entities/dorsal-visual-stream.md`, `wiki/entities/varc.md`, `wiki/entities/blindtest.md`. `G75` and `T215` are the two rows most likely to change status.

**Not acquired.** Roelfsema 2006, *Cortical algorithms for perceptual grouping* (Annu. Rev. Neurosci. 29:203–227) — `annualreviews.org` is outside the UBO Brest subscription, confirmed by the user at the paywall; added to the skill's excluded-venues table. `G27`, `T278` and `T151` keep their second-hand citation of base- vs incremental-grouping. The first attempt at Mollard, Bohte & Roelfsema used the bioRxiv `v2.full` URL and returned 395 words of references and cookie boilerplate; the user re-clipped it from PLOS Comput. Biol. 22(4):e1014193, where the paper has since appeared, and that clip is what is filed — the bioRxiv stub was discarded.

One INGEST each:

- [x] `kravitz-2011-new-neural-framework-for-visuospatial-processing.md` — visual-cortex · S · 2011 — user-clipped; Nat. Rev. Neurosci. 12:217–230 (Kravitz, Saleem, Baker & Mishkin); three distinct dorsal pathways — parieto-prefrontal, parieto-premotor, parieto-medial-temporal (via posterior cingulate and retrosplenial) — each with its own targets, which is why "where/how" is the wrong label; the wiki holds the `what` pathway ([[wiki/entities/ventral-visual-stream.md]]) and has never opened the spatial-relation slot a grid task runs almost entirely on, and the third pathway lands directly on [[wiki/entities/retrosplenial-cortex.md]] · `G30`, `T47`, `G43`
- [x] `nieder-2016-neuronal-code-for-number.md` — prefrontal-control · S · 2016 — user-clipped; Nat. Rev. Neurosci. 17(6):366–382 (Nieder); number neurons encode cardinality invariantly across modality and across simultaneous vs sequential presentation, with tuning width setting the grain by a stated principle (Weber scaling) rather than by the author's hand — one vocabulary primitive with a measured code and a stated grain, which is what `G4` says nobody supplies; small-`n` subitizing vs large-`n` estimation is `G27`'s discretisation question at a scale where the answer is known · `G4`, `G27`
- [x] `pearson-2019-human-imagination-visual-mental-imagery.md` — visual-cortex · S · 2019 — user-clipped; Nat. Rev. Neurosci. 20:624–634 (Pearson); imagery runs the sensory hierarchy backwards, is graded from aphantasia to hyperphantasia, and *competes* with afferent perception in a way binocular-rivalry priming quantifies; `G90` is an `arrangement` row that will not be closed by a component — what it needs first is a worked case of the arbitration with a dial on it, and `G15` reads the same material as the question of what turns the mode on · `G90`, `G15`
- [x] `ullman-1984-visual-routines.md` — visual-cortex · F · 1984 — `LOSSY` (pdf2md, pymupdf4llm over an OCR scan — visible character errors, check every equation and figure caption by hand); Cognition 18(1–3):97–159 (Ullman); the only account in the literature where the parse is a *program* rather than a setting — elemental operations (shift of processing focus, indexing, boundary tracing, marking, bounded activation/colouring) assembled **per task** into a routine over a base representation; `G75` asks for a model that chooses its decomposition from an open space, and this specifies what that space is made of, with [[wiki/entities/arc-vsa-solver.md]]'s six-hypothesis menu as a degenerate case · `G75`, `G73`, `T191`
- [x] `ballard-1997-deictic-codes-for-embodiment-of-cognition.md` — embodied-robotics · F · 1997 — `LOSSY` (pdf2md, pymupdf4llm from the open author copy; includes the BBS peer commentary and the authors' response — the target article is the first ~40 pages); Behav. Brain Sci. 20(4):723–767 (Ballard, Hayhoe, Pook & Rao); deictic strategies ("do it where I am looking") replace a full scene representation with a fixation that binds a variable to a location, collapsing the computation at the cost of a serial schedule — perception as a sequence of actions priced in actions, which is [[wiki/entities/arc-agi-3.md]]'s own scoring rule; `G25` and `G32` are both about this loop and neither holds a source in which *looking* is one of the actions · `G25`, `G32`, `G73`
- [x] `pylyshyn-2001-visual-indexes-preconceptual-objects.md` — visual-cortex · F · 2001 — `LOSSY` (pdf2md; `pymupdf4llm` returned mojibake on this file's embedded fonts, re-converted with plain `pymupdf` text — page markers, no heading structure); Cognition 80(1–2):127–158 (Pylyshyn); FINSTs individuate and track 4–6 items across changes in their properties and locations *before* any conceptual description, so a relation can be computed over individuals without those individuals being described — a pointer to a thing that does not categorise the thing, which the wiki has no object for; `G39` is this problem from the memory side and ARC-AGI-3's frame sequences are it from the benchmark side · `G39`, `G75`, `T191`
- [x] `mollard-2026-multiscale-incremental-grouping.md` — visual-cortex · F · 2026 — user-clipped (PLOS, after a bioRxiv `v2.full` clip returned only references); PLOS Comput. Biol. 22(4):e1014193 (Mollard, Bohte & Roelfsema); a recurrent architecture for scale-invariant tracing of curves and objects — a feedforward pathway dynamically selects the scale, a recurrent pathway propagates enhanced activity over horizontal and feedback connections through a VIP/SOM disinhibitory loop, trained by a biologically plausible reinforcement-learning scheme and generalising from short curves to long curves and 2D objects, choosing the scale from inter-curve distance and object width as humans and monkeys do; a parser whose grouping criterion is **learned**, which is the third option `T151` has never been given — neither slot architectures (`K` fixed at design time) nor post-hoc recovery ([[wiki/entities/spelkenet.md]]) leaves a persistent tag a downstream reader can bind to · `G75`, `T151`, `G34`
- [ ] `rahmanzadehgervi-2024-vision-language-models-are-blind.md` — reasoning-eval · M · 2024 — user-clipped (13 lines of HTML residue — strip at ingest); arXiv 2407.06581v6, ACCV 2024 (Rahmanzadehgervi, Bolton, Taesiri & Nguyen); BlindTest is 7 tasks no reasoning step survives — circle overlap, line-intersection counts, nested-square counts, grid row/column counts, single-coloured path tracing — on which four frontier VLMs average 58.07% (best 77.84%) against a human 100%; path tracing and row/column counting are precisely the operations an ARC grid demands, measured in isolation from any rule, so this is a candidate `I`-row instrument: a perception floor to report beside any ARC score · `T215`, `G17`, `G36`
- [ ] `hu-2025-arc-is-a-vision-problem.md` — arc-benchmarks · R · 2025 — user-clipped; arXiv 2511.14761 (Hu, Cy, Qiu, Ding, Wang, Zhu, Andreas & He — the want-list mis-attributed this to "Li et al."); ARC reformulated as image-to-image translation on a canvas that carries the visual priors — a vanilla ViT trained from scratch on ARC data only, generalising to unseen tasks by test-time training, **60.4% on ARC-1**; the cleanest existing evidence for `T215` position B, since the reasoning stack is *removed* and the score goes up, and `T154`'s counterexample since there is no foundation-scale pretraining at all; pairs against [[wiki/concepts/test-time-training.md]] · `T215`, `G73`, `T154`
- [ ] `acuaviva-2025-rethinking-visual-intelligence-video-pretraining.md` — world-models · R · 2025 — user-clipped; arXiv 2510.24448v2 (Acuaviva, Davtyan, Hassan, Stapf, Rahimi, Alahi & Favaro); video diffusion models adapted with LoRA against LLMs on ARC-AGI (16.75% two-attempt, CogVideoX1.5-5B), ConceptARC across 16 concept categories, plus Sudoku, mazes and cellular automata — the claim being that spatiotemporal pretraining is where the structural inductive bias comes from; `T154` position A with a new corpus type, and `G95` reads it as an invariance group discovered from dynamics rather than declared by an augmentation list; disagrees with `hu-2025` on overlapping task sets, so ingest as a tension candidate · `T154`, `G95`, `T151`
- [ ] `wang-2026-perception-bottleneck-abstract-reasoning-benchmarks.md` — reasoning-eval · R · 2026 — user-clipped; arXiv 2512.21329v2 (Wang, Huang, Zhang, Wang & Ma); an error-attribution protocol across Mini-ARC, ACRE and Bongard-LOGO concluding that perception, not reasoning, is the dominant factor in the human–model gap — `T215`'s `Closes when` ("supply the parse alongside the image and re-score", called cheap and unrun) almost verbatim; if the protocol matches, `T215` moves to `RESOLVED B` and **every ARC score in the wiki has to be re-read** as a joint perception-and-reasoning measurement; `T291` is the same claim generalised · `T215`, `T291`, `G17`
