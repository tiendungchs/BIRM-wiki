# Index — Entities

Models, frameworks, benchmarks, and biological systems. One page per artefact. No researcher pages. Updated whenever an entity page is created.

## Models and frameworks

| Page | What it is | Load-bearing for |
|---|---|---|
| [[wiki/entities/aixi.md]] | Sequential decision theory (expectimax) with the unknown environment prior replaced by the universal semimeasure `ξ`; uncomputable, plus the `AIXItl` reduction that is computable at `t̃·2^l̃` per cycle | The wiki's formal ceiling — the only system covering all six hardness sources; the separability vocabulary; self-certified value as a policy-selection criterion |
| [[wiki/entities/bayesian-program-learning.md]] | Concepts as simple stochastic programs over a shared library of pen primitives, sub-parts, parts and relations; inference recovers the program from one example | The wiki's only architectural (not emergent) `g`/`x` factorization; human-level one-shot classification and a passed visual Turing test; the causal end of the generative spectrum |
| [[wiki/entities/autotom.md]] | Automated agent modeling for theory of mind: an LLM proposes a Bayes net over mental variables (state/observation/belief/action/goal/interactive state), estimates every local conditional, and the *structure* is grown one variable or one earlier timestep at a time until the query posterior falls below one bit of entropy | The wiki's first running structure-discovery loop with an enumerated model space (`30^T`), a stated selection criterion (`U = −H(P(q\|X)) − α\|M\|`) and a measured per-query price (~10K tokens, ~27 API calls); 82.43 average over five ToM benchmarks against GPT-4o 63.39 and fixed-model Bayesian inverse planners at 42.10/48.94 — and the first nested mental inference in the wiki run and costed past depth one (order 4) |
| [[wiki/entities/hbtom.md]] | Hierarchically Bayesian Theory of Mind: inverse planning over an MDP with a Dirichlet prior on per-agent goal preferences and an inverse-Gamma prior on per-agent rationality; plausibility read out as a product of three posterior-surprise metrics | The wiki's first primary source for Bayesian inverse planning and its cleanest architectural two-level hierarchy (hyperprior → per-agent params → per-trial goal); 96.0–99.7% pairwise accuracy on the Baby Intuitions Benchmark against baselines at chance, with the entire state space hand-supplied |
| [[wiki/entities/ilp-arc-synthesizer.md]] | Rocha et al. 2024: an ARC solver whose hypothesis is a sequence of Horn clauses induced by inductive logic programming over 7 authored object/relation predicates, executed as ordered writes onto an empty grid | The wiki's first ingested ARC attack and first ILP source; negative examples manufactured from the hypothesis's own overgeneration; multiple overlapping parses kept until the explaining relation selects one; the source of gap G70 (no induced program has a loop) |
| [[wiki/entities/corethink-compositional-reasoner.md]] | Das et al. 2025: a four-stage ARC-AGI-2 pipeline with nothing trained — connected-component scene abstraction, a frozen LLM detecting which of 22 authored parameterized "unit patterns" explains each demonstration, a detection-count consensus filter, and a second frozen LLM solving the test grid from the surviving patterns as a text hint | The wiki's priced comparison of *where to spend a vote*: counting hypothesis labels across demonstrations is +5.5 points at additive cost, cell-wise voting over 3–10 full solves is +2.5 at `N×` cost (15.0 → 24.4 combined, 30.8 ensembled); plus its first run-time router between structure types (symmetry score > 0.70 bypasses the LLM, G12) and the coarse end of the DSL-grain axis |
| [[wiki/entities/lisa.md]] | Learning and Inference with Schemas and Analogies (Hummel & Holyoak 1997/2003, via Holyoak 2012): a symbolic-connectionist analogy model — distributed semantic units under localist object/role/sub-proposition/proposition units, role-filler bindings carried by **neural synchrony**, correspondence learned as Hebbian mapping connections between co-active units in a driver and a recipient analog | The wiki's only model whose working-memory capacity is *derived* rather than stipulated (bindings = mutually out-of-phase time slots ⇒ 2–3 live propositions, so complex mappings serialise); the architecture matching the prefrontal dissociation between relational integration and interference control; and the counter-case to every dot-product similarity scheme in the wiki — a phase code has no vector to compare |
| [[wiki/entities/mlc.md]] | Meta-Learning for Compositionality (Lake & Baroni 2023): a 1.4M-parameter standard seq2seq transformer meta-trained over 100,000 episodes, each a randomly generated **interpretation grammar** that is never observed and must be inferred from 14 in-context study examples; frozen at test, no task-specific parameters | The wiki's demonstration that systematicity lives in the *task distribution* rather than in the architecture — 100% exact match where the identical architecture trained on a static corpus scores 0% in 10/10 runs, ≤0.22% error on SCAN's lexical splits and 0.87% on COGS's 18, against **100% error** on the length and structural splits the episode sampler never varied; plus the wiki's first concrete `p(T)` generator (a meta-grammar), an inductive bias installed by sampling it at a measured rate (80/20, matching human 80.7%), and the finding that a gold-grammar oracle is a *worse* model of human responses than a biased neural one. Supplementary Information adds: 99.3–99.8% on rules withheld under a semantic-equivalence quotient, a typical run at 92.9% (SD 8.2) rather than 100%, a sampled bias applied at the right marginal rate but without human context-dependence (98.0% vs 68.2% mutual exclusivity), and GPT-4 at 58.0% collapsing to 14.0% when the support examples are shuffled |
| [[wiki/entities/macfac.md]] | MAC/FAC (Gentner & Forbus 1991; Forbus, Gentner & Law 1995): a two-stage similarity-based retrieval model — stage 1 (MAC) scores every item in memory by the dot product of **content vectors** (counts of each functor occurring in a structured description), stage 2 (FAC) runs SME in literal-similarity mode on the survivors, each stage selecting the best match plus everything within 10% | The wiki's only cheap first stage that estimates the expensive stage's **workload** (number of match hypotheses SME would generate) rather than its output — hence derived rather than trained, and one-sided; and the source of the human retrieval error profile (LS .56, SF .53, AN .12, FOR .09) that makes surface-driven access a design target rather than a defect. The 1995 journal version adds the validation apparatus — a sensitivity map over the design space showing that a cheap filter's statistic must contain **both** attributes and relations (neither alone has a viable setting), a measured ~2×10³ stage-cost ratio and a two-orders-of-magnitude win over ARCS, the tiered-identicality rule (relations identical or re-represented; functions and entities free), trickle-down structural evaluation, and the finding that stage 1 is impenetrable to task instructions while stage 2 is tunable |
| [[wiki/entities/arc-vsa-solver.md]] | Joffe & Eliasmith 2025: object-centric ARC program synthesis inside a vector-symbolic algebra — objects as colour ⊕ Spatial-Semantic-Pointer centre ⊕ SSP shape, a searched object parse, an 11-operation DSL, and rule conditions learned as unit-norm prototype vectors in the same space as the data | The wiki's first computed criterion for choosing a *discretisation* of an observation (six parses ranked by mean max-softmax of output-to-input similarity, G27); a non-length selection criterion (minimum hitting set over operation/parameter reuse across demonstrations, G26); and its sharpest price on demonstration-consistency — 48.8% of ARC-1-Train tasks reproduced in full against 10.8% solved (3.0 on Eval, 0.0 on ARC-2-Eval; 94.5 Sort-of-ARC, 83.1 1D-ARC) |
| [[wiki/entities/irene.md]] | Intuitive Reasoning Network: relational GraphSAGE encoder over a hand-predicated scene graph + 6-layer transformer over 8 familiarisation trials, trained only on next-position regression; surprise = `max_t` mean-squared prediction error | The matched-input control for T21 — same symbolic Baby Intuitions JSON as HBToM, no agent variable anywhere, and still at chance on Preference; plus the wiki's clearest case of a benchmark average rewarding the model that kept the shortcut, and a non-monotone curriculum ablation (G32) |
| [[wiki/entities/coin-model.md]] | COntextual INference: a switching state-space model with a sticky hierarchical Dirichlet process prior over an unbounded context set; one posterior drives memory creation, probability-weighted expression, and responsibility-scaled updating of *every* memory | The wiki's only model of retrieval-and-allocation over a growing memory library, and its only experimental demonstration that behavioural change ≠ learning: evoked recovery confirmed as a novel prediction, ΔBIC 302.6/394.1 nats over the dual-rate model, three "learning rate" phenomena reproduced parameter-free with the learning rates unchanged |
| [[wiki/entities/h-jepa.md]] | A fully differentiable modular agent around one *configurable* world-model engine; predictions in representation space (JEPA), stacked across time scales (H-JEPA), planning by cost minimisation over action sequences | The wiki's most complete design for a reasoning agent and its least-evidenced entry: jumpy planning via subgoals-as-conditions, abstraction as long-range predictability, Mode-2 → Mode-1 amortisation, the case against generative world models |
| [[wiki/entities/v-jepa-2.md]] | A 1B-parameter video encoder trained by masked *feature* prediction on >1M hours of internet video, frozen, plus a 300M block-causal action-conditioned predictor post-trained on 62 h of unlabeled Droid teleoperation; planning by cross-entropy-method search over `‖P(â;s,z) − z_g‖₁` (Assran et al. 2025) | The wiki's first at-scale instantiation of [[wiki/entities/h-jepa.md]] and its audit (no stack, no latent `z`, no learned cost); zero-shot pick-and-place on Franka arms in two unseen labs (80%/65% vs Octo's 15%/10%); the compute argument for latent-space prediction — 16 s/action at 800 samples against Cosmos's 4 min at 80, same planner; SSv2 77.3 and EK100 39.7 R@5 from a frozen backbone; and a language-free encoder beating image–text encoders at feeding an LLM (T149) |
| [[wiki/entities/neo-neural-theorizer.md]] | NEO: induces an executable compositional program from a single raw observation pair `(x,y)` — VQ codebook symbols with no stipulated meaning, a goal-conditioned "theory programmer" `q_φ(z_k \| s_k, y)` selecting them, one shared deterministic executor `f_θ` supplying their semantics, MDL choosing the program length per instance, and a state-grounding loss keeping every intermediate state decodable | The wiki's first ingested primary source in program induction, and its first system to co-discover a primitive vocabulary *and* its semantics from observation alone (G4); the measured amortisation gap (primitiveness 1.0, length-OOD 0.02, search recovers 0.70 at ~180× cost) |
| [[wiki/entities/byol.md]] | Bootstrap Your Own Latent: negative-free joint-embedding self-supervision — an online encoder + projector + **predictor** regresses onto the stop-gradiented projection of an EMA copy of itself on a second augmented view of the same image (Grill et al. 2020) | The source of the EMA-target-plus-stop-gradient the whole JEPA lineage runs on, and of the caveat it inherited: the per-step loss is exactly minimised by collapse, so there *is* no objective being descended (T164, G34); 74.3% ImageNet linear eval; both stabilisers substitutable by a near-optimal predictor alone (52.5% / 66.5%); negatives shown to author their own shortcut (crop-only: −13.1 vs SimCLR's −27.6); and a representation bootstrapped from a *randomly initialised* target (1.4% → 18.8%) |
| [[wiki/entities/dinov2.md]] | A 1.1B ViT-g/14 trained on an automatically curated 142M-image corpus by two cross-entropy losses against an EMA teacher — one over class-token prototype scores, one over masked patch tokens — with the teacher's output distribution centered (or Sinkhorn–Knopp equipartitioned) and a KoLeo nearest-neighbour entropy term; smaller models distilled from it (Oquab et al. 2023) | The primary source for the encoder the wiki cites everywhere it does not train its own (DINO-WM's backbone, [[wiki/entities/spelkenet.md]]'s baseline, [[wiki/entities/v-jepa-2.md]]'s comparison column, T154); a fifth anti-collapse locus — normalising the *teacher's output distribution*, no predictor and no negatives; the term-vs-read-out decomposition (KoLeo +8.3 mAP retrieval / 0 segmentation; masked-patch term +2.9 mIoU / 0 retrieval; both ~0.5 on the ImageNet linear probe everything else is ranked by); frozen features within 2.0 points of full fine-tuning and linearly decodable to depth, where a 5× larger language-supervised encoder loses; curated vs uncurated at matched 142M and matched compute worth 14 points; and object *parts* emerging as PCA components that match across pose, style and category |
| [[wiki/entities/dinov3.md]] | A 7B ViT/16 successor to DINOv2 — same DINO + iBOT + KoLeo objective on a 1.69B-image corpus, but with constant (unscheduled) hyperparameters, axial RoPE with box jittering, and a second training phase whose loss is **Gram anchoring**: `‖X_S X_Sᵀ − X_G X_Gᵀ‖_F²` between the student's patch-similarity matrix and that of an *earlier iterate of itself* (Siméoni et al. 2025) | The wiki's first case of **partial, read-out-specific collapse under a well-defined objective** — global metrics rise monotonically over 1M iterations while dense patch read-outs peak at 200k and end below their own start, with patch norms stable and no loss term diverging; a sixth anti-collapse locus that constrains *second-order structure over tokens within one input* and takes its target from the model's own past, repairing 800k iterations of damage in 10k; the first competitive **frozen-backbone** detection (COCO 66.1 mAP, 100M trainable), segmentation (ADE20k 63.0) and depth systems, beating fine-tuned specialists and SAM-distilled encoders; the first SSL parity with weakly-supervised models on classification while the instance-retrieval gap widens (Met 55.4 vs 0.5); ViT-H+ at 1/8 the size matching the 7B teacher, and distillation crossing into ConvNeXt; and the same recipe unchanged setting Earth-observation SOTA on 12/15 tasks from RGB alone |
| [[wiki/entities/i-jepa.md]] | The first image JEPA: from one large context block predict the EMA target-encoder's representations of four smaller blocks of the same image — narrow ViT predictor, L2 in latent space, no augmentations, no decoder, no negatives, no anti-collapse term (Assran et al. 2023) | The JEPA lineage's missing primary source, between [[wiki/entities/h-jepa.md]] and [[wiki/entities/v-jepa-2.md]]; the wiki's only controlled price on predicting in representation space vs pixels (66.9 vs 40.7 at ImageNet-1%, T162); the **mask-sampler result** — one loss, one architecture, one dataset, four pair samplers, 15.5 → 54.2, which is a larger swing than the entire anti-collapse literature (T167, G32); the read-out trade of dropping augmentations (−9.7 iNat18, +19.0 Clevr depth vs DINO); ~10× less compute than MAE at equal or better accuracy; and a label-free RCDM probe showing the predictor keeps part identity, pose and positional uncertainty while discarding background |
| [[wiki/entities/hit-jepa.md]] | Three JEPAs stacked over a Conv1D/MaxPool resolution pyramid of one sequence — each level predicting masked spans at its own granularity under SmoothL1 + per-level VICReg, coupled only by ConvTranspose-upsampled **attention matrices** passed top-down with a learnable scale; H3 hexagonal cells + node2vec as the input basis (Li et al. 2025) | The wiki's first *trained* hierarchical JEPA, and the audit of [[wiki/entities/h-jepa.md]]'s stack: it trains jointly on one GPU, but the level axis is sequence resolution rather than prediction horizon and there is no action, latent or cost; the headline is that the hierarchy is an **out-of-distribution device** — ~4% in-distribution, but halves single-level error transferring taxis→ships across a 7-level change of spatial scale; a top-down attention-transport mechanism for the configurator's spotlight; and the factor-465 collapse caused by changing only the inter-level wiring with the objective fixed (G34, T168) |
| [[wiki/entities/barlow-twins.md]] | Two **identical** networks (no predictor, no stop-gradient, no EMA, no negatives) whose `D×D` cross-correlation matrix over a batch of two-view embeddings is pushed to the identity: `Σ_i (1−C_ii)² + λ Σ_{i≠j} C_ij²`, i.e. Barlow's 1961 factorial-code hypothesis as a loss term (Zbontar et al. 2021) | The dimension-contrastive anti-collapse family's founding instance and the wiki's most certifiable objective (its minimum is a stated matrix identity), at one coefficient flat over a 10× range; 73.2% ImageNet linear eval and best-in-wiki at 1% labels (55.0); the non-stackability result — adding BYOL's predictor + stop-gradient costs 10 points and its own 8192-wide projector costs BYOL 1.8 (T164); an anti-collapse locus that is a *normalisation* rather than a term (invariance-only ablation: 57.3%, not collapse); monotone gains in embedding width to 16384 above a fixed 2048-d representation, with a Gaussian-proxy-entropy-estimator mechanism that also explains flat performance to batch 256; and the negative-free counter-example to "negatives author the shortcut" (T165) |
| [[wiki/entities/lejepa.md]] | Latent-Euclidean JEPA: prove the isotropic Gaussian uniquely minimises linear-, k-NN- and kernel-probe risk over unknown downstream tasks, then enforce it with **SIGReg** — Epps–Pulley normality tests along `M` resampled random directions — plus a multi-view prediction term onto the global-view centroid; one coefficient, no predictor, no EMA teacher, no registers (Balestriero & LeCun 2025) | The primary source for SIGReg, which the wiki had only through [[wiki/entities/lewm.md]], and the only anti-collapse provision whose **target is derived rather than chosen**; the taxonomy-collapsing result that VICReg *is* SIGReg with a two-moment test, so the loss families are one moment-ladder; the label-free model-selection signal T164/T168 said nobody had (train loss ↔ downstream accuracy `ρ_s ≈ 0.85`, 0.99 under a `λ^0.4` rescaling); the predictor shown deletable and register tokens shown unnecessary; ~50 architectures across 8 families on one recipe, stable to ViT-g 1.8B; and a 21M ResNet trained from scratch on 11k galaxy images beating DINOv3, DINOv2 and a 630M I-JEPA (T154) ; **§Part 8** (Akbar 2026) adds the second-moment truncation **Weak-SIGReg**, SIGReg as a *supervised* optimisation stabiliser rescuing a collapsed ViT 20.73 → 72.02, and the moment-ladder head-to-head that the bottom rung wins (T171) |
| [[wiki/entities/vl-jepa.md]] | A JEPA whose prediction target is a **sentence embedding**: frozen V-JEPA 2 ViT-L X-Encoder + a query-conditioned 490M predictor (last 8 Llama-3.2-1B layers, causal mask off) predicting an EmbeddingGemma-300M Y-Encoder's embedding of the answer under bidirectional InfoNCE, with a text decoder invoked only on demand (Chen et al. 2025) | The wiki's first cross-modal JEPA and its first with a **trainable target branch in another modality**; the token-vs-embedding controlled comparison at matched encoder/data/batch (14.8 vs 7.1 CIDEr, 41.0 vs 27.2 top-5, at half the trainable parameters, T162); the target-encoder learning rate as a continuous knob with an interior optimum, where the two settings the lineage ships — jointly trained and frozen — cost 3.6 and 7.3 points (G34, T164); the horizon crossover that is the wiki's first measurement of prediction granularity vs anticipation time (+1.5 at 1 s to +4.6 at 10 s over V-JEPA 2 at matched encoder); relation-sensitive text hard negatives beaten with the caption *rate* unchanged (SugarCrepe++ 63.9 vs 58.6); WorldPrediction-WM inverse dynamics at 65.7 from 1.6B against GPT-4o's 52.0; and selective decoding as a priced event-boundary detector on a semantic stream (2.85× fewer decodes) |
| [[wiki/entities/lewm.md]] | LeWorldModel: a 15M-parameter joint-embedding predictive world model trained end-to-end from raw pixels with two loss terms — next-embedding prediction plus SIGReg, a random-projection normality penalty driving the embedding to `N(0, I)` — planning by cross-entropy-method MPC on a goal-embedding distance (Maes et al. 2026) | The wiki's third anti-collapse family (distribution matching) and the first priced in tuning surface (6 coefficients → 1, `O(n⁶)` grid → `O(log n)` bisection); a full plan in <1 s, ~48× faster than a frozen-foundation-encoder world model and ahead of it at matched FLOPs (G62); a violation-of-expectation signal that fires on teleportation (`p < 0.01`) and not on colour change; emergent temporal straightening the authors call collapse (T153); and the scale tension T154 |
| [[wiki/entities/adaworld.md]] | Action-aware world-model pretraining where the action condition is *induced*: a continuous latent action per consecutive frame pair from a β-VAE information bottleneck (β = 2×10⁻⁴), used as a universal control interface for a one-frame-at-a-time Stable Video Diffusion predictor pretrained on ~2000M frames from 1016+ environments (Gao et al. 2025) | The wiki's first at-scale induction of an **edge-label alphabet** (G4 → `PARTIAL`); zero-training action transfer across contexts (LIBERO FVD 767.0 vs 1545.2 action-agnostic); the sharpest G62 datum (a ~14% PSNR gap that is a total planning gap — 26.00% vs 56.67%, where 26.00% is chance); composition by vector average; and tension T148, continuous codes beating a VQ-8 codebook on transfer |
| [[wiki/entities/gcq.md]] | Grid-like Code Quantization: vector quantisation whose codebook is the frozen bump-attractor set of a toroidal continuous attractor network, selected by an **action-conditioned** template match over whole observation–action sequences, so spatial and temporal compression are one operation (Peng et al. 2025) | The wiki's first prestructured attractor scaffold used as a *quantiser* in a trained pipeline (relaxation = nearest-codeword `argmin`); path-consistency imposed on a **perception module** (G3); a fourth source of discretisation — finite neurons on an imposed continuum (G27); the first implemented instance of G39 anchoring as `argmax` over a symmetry group; a world model whose rollout error cannot compound because its transition was never fitted; and the ablation behind T150 — making the installed codebook learnable degrades it (FID_p 43.41 → 47.76) |
| [[wiki/entities/spelkenet.md]] | A 7B autoregressive flow-completion world model (LRAS backbone) probed by appending a single synthetic optical-flow token at one pixel; objects recovered as the pixels whose sampled flow responses correlate with the poke, plus the **SpelkeBench** benchmark of 500+50 motion-defined segments (Venkatesh et al. 2025) | The wiki's only entity individuator, and the learned cohesion detector core knowledge's entry condition presupposes (G23); the spatial half of G27; a world model used purely as a structure-extraction instrument; the label-free escape from probing's circularity (G17) |
| [[wiki/entities/kan-ode.md]] | A Neural ODE whose gradient-getter is a Kolmogorov-Arnold Network — `du/dt = KAN(u,θ)` with learned univariate basis functions on the edges, integrated by a standard ODE solver with adjoint gradients (Koenig et al. 2024) | The wiki's only *readable* transition function (symbolic regression recovers a PDE source term to within 0.5%) and its only continuous-time dynamics model; `N⁻⁴` error-vs-parameter scaling against an MLP's `N⁻²`; the grey-box `known operator + learned residual` composition — and the boundary: state variables given, no actions, noiseless synthetic data |
| [[wiki/entities/neuromatch.md]] | Neural approximate subgraph isomorphism: anchored `k`-hop neighbourhoods embedded by a GIN into an order-embedding space where `G_q ⊆ G_u` iff `z_q ≼ z_u` componentwise; queries answered by coordinate comparison plus neighbour voting | The wiki's only structural-retrieval architecture and its clearest case of structural amortisation: median 95.5 AUROC, ~100× faster than exact search, zero-shot transfer from random graphs to real domains — and 49% precision under natural class skew |
| [[wiki/entities/learningmatch.md]] | A Siamese network that amortises an *exactly computable* similarity — the noise-weighted match between two gravitational-wave templates — with shared 4×1024 encoders whose depth barely matters, feeding a 5×1024 head that also receives the pair's raw difference-squared **and its raw mean**, because the target metric's curvature varies over the manifold | The wiki's only priced amortisation ledger (≈33 CPU-h labelling + ≈31 GPU-h training vs 40 ms saved per query ⇒ break-even ≈5.8M queries), and its counter-case to geometric read-outs: the un-factorised head escapes the rank-`d` bilinear ceiling while the encoders stay cacheable |
| [[wiki/entities/differentiable-neural-computer.md]] | An LSTM controller coupled to an `N × W` memory matrix by three differentiable addressing modes — cosine-similarity content lookup, an `N × N` temporal link matrix over write order, and a usage-based free list — trained end to end (Graves et al. 2016) | The wiki's fully specified fast-store *addressing* scheme and its only machine allocate-vs-reuse gate emitted at runtime (G38); 98.8% subway traversal vs. 55.3% shortest path, 3.8% mean bAbI error, LSTM never clearing lesson 1; the plan-before-act result (first action decodable from memory ~60 steps early, 89% vs. 17%); and the boundary — every graph is handed over as input |
| [[wiki/entities/othellogpt.md]] | An 8-layer GPT trained only to predict legal Othello moves, probed and *steered*: board state linearly decodable at 99.6% once labelled relative to whoever moves ({Mine, Yours, Empty}), and a single vector addition installs a counterfactual board (Nanda et al. 2023 on Li et al. 2022's model) | The wiki's only machine world model that passes the intervention standard of [[wiki/concepts/representation-probing.md]]; the evidence base for [[wiki/concepts/linear-representation-hypothesis.md]]; the label-*basis* failure mode (T157) and the multiple-circuits caution (T158) |
| [[wiki/entities/maze-solving-transformers.md]] | Small decoder-only GPTs (1.2M / 9.6M parameters) trained by next-token prediction on token-serialised mazes, then interpreted with linear probes, direct logit attribution and the tuned lens | The wiki's cleanest separation of *discovery* from *use*: a whole maze linearly decodable from one token's residual stream at layer 2 (0.83–0.99 per wall), an adjacency head respecting in-context topology — and rollouts that still cross walls while reaching the target ≈100% of the time (T25) |
| [[wiki/entities/spiking-neural-networks.md]] | Networks whose units communicate by binary, time-located spikes; leaky integrate-and-fire dynamics, STDP-native, run on neuromorphic hardware (Loihi, TrueNorth, SpiNNaker) | The test case for the implementation-level exclusion ([[wiki/empirical-tensions.md]] T1); a substrate-level primitive for *directed* edges; why local learning stops being optional |
| [[wiki/entities/cscg.md]] | A hidden Markov model with a frozen per-observation clone pool, so learning transitions *is* learning a de-aliased state space; expectation-maximisation, local rules, planning by inference | The clone-cell answer to gap G2 made concrete; hippocampus-as-map; fast single-environment maps with zero transfer; the wiki's only model matched to a brain on its *learning trajectory* rather than its endpoint (Sun et al. 2025) |
| [[wiki/entities/tolman-eichenbaum-machine.md]] | A reusable path-integrating structural code plus a Hebbian relational memory binding it to this environment's sensory codes, trained only on next-observation prediction across many worlds (TEM; SMP is the pixel-input sibling) | The wiki's clearest built realisation of `p = f(g, x)` and of the meta/instance split; the model side of place, grid, splitter, lap and evidence cells; hippocampus-as-index; the derivation of grid codes from two memory-addressing constraints, and the two-dataset evidence that remapping is non-random |
| [[wiki/entities/tem-transformer.md]] | A transformer whose position encodings are generated by an action-driven recurrent operator (`e_{t+1} = σ(e_t W_a)`), with queries/keys reading only position and values only stimulus; shown algebraically to be TEM with a softmax | The proof that the wiki's `p = f(g, x)` architecture and the transformer are one computation: grid and band cells in the position encodings, place cells in the memory neurons, additive rather than multiplicative scaling in the number of bound cortical regions, and the export route for `g` into language (grammar as positional encoding) |
| [[wiki/entities/rolls-treves-hippocampal-model.md]] | The quantitative CA3-autoassociation theory: dentate competitive network → mossy-fibre randomization → single diluted CA3 attractor → reverse hierarchy of pattern associators back to cortex, with closed-form capacity at every stage | The wiki's only capacity accounting for a fast store (`p_max ≈ kC/(a ln(1/a))`); why recurrence is *diluted*; hash-on-write vs. embed-on-read; the backprojection fan-out constraint; `7 ± 2` from noise-driven attractor transitions |
| [[wiki/entities/hidden-state-inference-remapping.md]] | Place-field remapping as a posterior over hidden states under a Chinese Restaurant Process prior; the four remapping categories as one axis of log posterior odds | The normative *why* behind pattern separation/completion; `α` as the allocate-vs-reuse knob; posterior uncertainty read off population heterogeneity; anchoring as an argmax over rotational offsets nested inside retrieval |
| [[wiki/entities/stp-flickering-cann.md]] | Two overlapping place-cell maps as competing continuous attractors with Tsodyks–Markram short-term plasticity; theta-paced flickering after a cue switch produced by a synaptic rebound in the abandoned map, tested against the Jezek et al. 2011 teleportation data | The wiki's activity-silent short-term store (memory in `u, x` with no reverberatory trace); attractor selection as `input × gain` with a recency term; oscillation as a periodic reset of inhibitory competition; the mechanistic rival to theta-cycle posterior sampling (T56) |
| [[wiki/entities/stsp-working-memory-rnn.md]] | ~2000 recurrent networks (fixed-synapse `tanh`/ReLU, presynaptic Tsodyks–Markram STSP, excitatory-anti-Hebbian plastic weights, LSTM, GRU) trained on a distracted delayed-match-to-sample task and scored against macaque lateral prefrontal multi-unit recordings from the same task | The wiki's controlled test of activity-based against synaptic working-memory maintenance: only the plastic-synapse nets reproduce prefrontal cortex's loss of delay decodability, they tolerate >50% synaptic ablation where fixed-weight nets fail at 10–20%, and LSTM/GRU win both robustness measures while being least brain-like (brain-similarity ⊥ robustness) |
| [[wiki/entities/trnn.md]] | A 600-unit ReLU recurrent net made incapable of persistent firing by three edits — per-unit self-inhibition `τ_v dV/dt = −V + mr`, sparse recurrence, sensory/association/motor blocks — trained on a simulated olfactory delayed paired-association task and on PPO grid-world memory tasks, scored against Neuropixels recordings from >30 mouse brain regions via a *transient index* | The wiki's head-to-head test of transient-trajectory against persistent-activity working memory at matched parameter count: transient wins on distractors, on 2–6 items and on spatial navigation, has higher activity entropy and lower `⟨r²⟩`, and matches mouse recordings the persistent arm does not |
| [[wiki/entities/adaptive-cann.md]] | A continuous attractor network plus a slow negative-feedback adaptation current (spike-frequency adaptation or short-term depression); one scalar `m` against input strength `α` selects static bump, smooth/anticipative tracking, oscillatory tracking (Hopf bifurcation → theta sweeps) or travelling wave, with a 4-D motion-mode reduction and closed-form phase boundaries | The wiki's resolution of the stability-vs-mobility dilemma in attractor memory; a continuous control interface over simulation/search (G15) instead of a mode switch; speed-independent anticipation without a forward model; a closed-form exploration exponent `α = 1 + 2μ/γ²` for Lévy-flight search near a bifurcation |
| [[wiki/entities/temporal-context-model.md]] | A normalised leaky integrator of item-retrieved context as the sole retrieval cue; hippocampus reinstates past context states | The one equation that yields both the entorhinal place code and free-recall contiguity; graph distance recovered from co-occurrence alone; leaky-vs-perfect integration |
| [[wiki/entities/spiking-tem.md]] | The TEM generative/inference factorisation rebuilt from leaky integrate-and-fire neurons, binary Bernoulli-sampled latents, STDP memory and an 8 Hz theta rhythm, over a full LEC/MEC/DG/CA3/CA1 anatomy | The wiki's only implementation-level rewrite of an architecture it already holds, and so its sharpest test of T1: grid cells at 59.6% of MEC II units collapse to 0.0% without a neuromodulatory gain, 10.1% without STDP, 25.6% without theta inhibition; phase precession and phase locking dissociated by two named mechanisms; grid emergence peaks at *intermediate* sensory ambiguity; and the `g̃ ⊙ x̃` conjunction had to be deleted (T42) |
| [[wiki/entities/vicreg.md]] | Variance-Invariance-Covariance Regularization: squared distance between two views' embeddings, plus a per-dimension standard-deviation hinge (`γ = 1`) and an off-diagonal covariance penalty applied to **each branch separately**; ResNet-50 encoder + 3×8192 expander, no normalisation of the embeddings anywhere (Bardes et al. 2022) | The primary source for the dimension-contrastive family the wiki had carried by name since [[wiki/entities/h-jepa.md]]; the only anti-collapse provision that survives branches with different weights, architectures or **modalities** (image↔caption 33.6 R@1 vs a contrastive 30.3; waveform↔spectrogram 78.4 vs 72.7 supervised), 73.2% ImageNet linear eval; the 57.5/57.3 identification showing Barlow Twins' batch standardisation *is* this variance term implicitly; the correction that VICReg has one free coefficient scale rather than six; the composability counter-result (predictor + stop-gradient neutral here, variance term buys BYOL +0.9 — T166); and the collapse-inside-the-coefficient-space result that a certifiable minimum does not certify the trajectory |
| [[wiki/entities/vector-hash.md]] | A frozen grid-cell state set randomly projected into hippocampus, turning exponentially many prestructured states into convex uniform attractors, with content attached by a separate plastic layer and sequences stored as 2-D velocity shifts | The wiki's first *derived* memory capacity and its only graceful-overload store (memory continuum, no cliff); strong generalization from `O(MK_max)` visits to `O(K^M)` states; 11 maps with zero forgetting and no replay; the first circuit model of the memory palace |
| [[wiki/entities/mm-tem-hippoformer.md]] | TEM's relational memory rebuilt as a meta-learned MLP whose fast weights are written online by surprise-gated gradient descent with a learned forget gate (mm-TEM), plus a 32-step transformer run alongside it (Hippoformer) | The wiki's third rewrite of `p = f(g, x)` and its first fast store with a *learned* forgetting policy; the prediction horizon as the determinant of grid scale; the first quantitative link from grid score to reasoning accuracy and the counterexamples to it; the negative control showing a path-integrating positional encoding buys short-range accuracy and no structural transfer |
| [[wiki/entities/hopfield-network.md]] | The classical binary/bipolar associative memory: `N` units, symmetric zero-diagonal weights, asynchronous threshold updates, one-shot Hebbian outer-product write, and retrieval as descent on `E = −½ Σ w_ij s_i s_j` | The wiki's **baseline** — the object nine other memory pages quote as the thing they improve on; the one-line Lyapunov argument that makes retrieval identical to relaxation; the `≈0.14N` cliff, spurious minima and one-unit-per-bit cost restated as *one* quantity (≈0.14 bits/synapse, hence overlap between stored patterns as the single thing every successor reduces); the symmetry trade — a guaranteed convergence bought by an inability to move, which four later pages each buy back differently |
| [[wiki/entities/sparse-distributed-memory.md]] | A random-access memory for 1,000-bit words: ~10⁶ randomly sampled *hard locations*, a Hamming-ball activation rule, Hebbian up–down counters and a majority-vote read; addressing fixed, contents plastic | The wiki's earliest closed-form fast-store capacity (`τ ≈ 0.10·M`, independent of pattern size) and its only *load-dependent* sparsity law (`p = (2MT)^(−1/3)`, gap G38); the per-bit runtime confidence read-out `\|s_u\|` (G42); expansion recoding as derived pattern separation; the cerebellar granule/Purkinje/climbing-fibre mapping, with Marr's codon model and Albus' CMAC as special cases |
| [[wiki/entities/btsp-cam.md]] | Behavioral Timescale Synaptic Plasticity as a content-addressable memory: a two-layer feedforward CA3→CA1 model with **binary** weights flipped by an involution `Δw_i = ±1` on every active input inside a ≈10 s dendritic-plateau window, the plateau arriving independently of the input at rate `f_q ≈ 0.005`; no recurrence, one-step read, plus random Hebbian feedback for input reconstruction | The wiki's only one-shot store with 1-bit weights matching continuous-weight Hopfield recall; the only write rule with **no postsynaptic term**, hence uniform memory load and no retrograde degradation over 10,000 items; the only mechanism reproducing the human **repulsion effect** (similar items learned together are pushed apart by the depression branch); `f_q` as a single runtime scalar controlling the separation/completion bias (G38) |
| [[wiki/entities/dense-sequence-memory.md]] | Asymmetric Hopfield sequence memory (state → successor) with the pattern–state overlap passed through a steep nonlinearity `f` before it drives the next state; polynomial `f(x)=x^d` and exponential `f`, plus a `MixedNet` combining a symmetric hold term with an asymmetric transition term for variable dwell time | The wiki's only **sequence** capacity theory (polynomial-to-exponential in `N`, and a transition-vs-full-sequence capacity gap that can diverge); capacity as a read-out nonlinearity rather than a code property (G42); the demonstration that i.i.d.-pattern capacity bounds fail qualitatively on correlated real data; the bipartite two-body form whose hidden units are per-transition and therefore individually gateable |
| [[wiki/entities/context-modular-memory-network.md]] | A Hopfield network plus a discrete external *context* signal that imposes a binary mask on neurons (rows/columns) and synapses (entries), giving one effective connectivity and one energy landscape per context; masks set randomly at learning time or *refined* after learning by a per-synapse sign-agreement test `sgn(J_ij) = sgn(J_ij^(σ))` | The wiki's only treatment of memory **accessibility** (hiding a stored memory) as a design objective alongside capacity; retrieval as an externally imposed mask costing `O(1)` in library size (G37); capacity conditional on a control signal (≈7× Hopfield random neuronal, ≈30–40× refined, G42); the demonstration that arbitrary patterns can be stabilised in a **random** weight matrix, i.e. memory stored in the mask rather than the weights (T59); mask separation as a fifth continual-learning family |
| [[wiki/entities/c-ts-model.md]] | Context–Task-Set: an unbounded set of task-sets grown by a Chinese-restaurant prior where each cluster owns a whole stimulus→action *policy*, plus a two-loop cortex–basal-ganglia network whose meaningless prefrontal stripes become those task-sets under pure reinforcement learning | The wiki's only parameter-by-parameter mapping between a nonparametric clustering prior and a neural circuit (STN strength ↔ task-set selection noise `r=0.62`; context→PFC wiring ↔ clustering `α` `r=0.76`); and its evidence that humans build hierarchical structure when it costs them (6 links against 4), buying positive *and* negative transfer |
| [[wiki/entities/pbwm.md]] | Prefrontal cortex as a set of independently gated stripes, basal-ganglia Go/NoGo disinhibition as the write-enable, and a Pavlovian dopamine critic (PVLV) that trains the gating policy — an actor–critic working memory learned without backpropagation through time | The wiki's only fast store whose **write policy is itself learned**, and its concrete answer to delayed+structural credit assignment (`δ_j = snr_j·δ`); matches LSTM on 1-2-AX / SIR-2 / phonological-loop tasks, separates gated from non-gated architectures on shared-stimulus routing, and collapses to 0% success with dopamine modulation removed |
| [[wiki/entities/pfc-columnar-planning-model.md]] | A prefrontal network of cortical columns (one column = one place, its minicolumns = that place's state–action pairs) that learns a topological graph by sparsifying a redundant hippocampal place code, then plans by diffusing a reward signal backwards along learned reverse edges (Martinet et al. 2011) | Planning as an attenuating **wavefront** rather than a rollout: distance-to-goal is a firing rate, the noise floor sets the horizon (G24), and a second coarser column population — recruited at self-motion change-points — is what keeps the wave above noise in a 4× larger maze, so hierarchy is an SNR device with an ablation behind it. Also the wiki's tightest argument that Tolman's "insight" *is* the graph-vs-routes distinction and nothing more |
| [[wiki/entities/fcann.md]] | Whole-brain continuous-state Hopfield network whose couplings are read directly off resting-state fMRI (`J = −Σ⁻¹`, regularised partial correlations over 122 parcels), with two hyperparameters (inverse temperature `β`, noise `ε`), deterministic relaxation to find attractors and stochastic relaxation as a generative model of brain dynamics | The wiki's only attractor model fitted to a whole brain and its only empirical test of a *geometric* prediction of free-energy minimisation — attractors coinciding with the eigenvectors of `J` (a Kanter–Sompolinsky projector network, i.e. capacity-optimal orthogonal storage); the `J^S`/`J^A` decomposition that keeps an energy landscape under asymmetric couplings; task and disease reframed as perturbation of one landscape ("ghost attractors", one-region control at SNR ≈ 0.005, `r = 0.62`; autism flow prediction `r = 0.66`); attractors explaining more held-out variance (0.396) than the data's own principal components (0.364) |
| [[wiki/entities/boltzmann-machine.md]] | A Hopfield network with two changes — stochastic sampled-sigmoid updates `P(s_i=+1)=σ(2y_i/T)` at temperature `T`, and hidden units with no target — trained by the contrastive Hebbian rule `Δw_ij ∝ ⟨s_i s_j⟩_data − ⟨s_i s_j⟩_free`; the restricted (bipartite) form makes whole layers update in parallel | The wiki's only account of how an energy landscape becomes a **sampler**: the partition function `Z` cancels in the two-state ratio, so a globally normalised distribution is sampled by purely local updates; the negative (free-running) phase as the mechanism behind [[wiki/concepts/energy-based-models.md]]'s "pushing everything else up is the whole problem", and as a computational reason offline replay must sample from the model rather than the data; temperature as one scalar interpolating retrieval → generation; hidden units as the first invented (untargeted) code in the attractor lineage |
| [[wiki/entities/dendritic-ann.md]] | A two-hidden-layer feedforward net with two fixed boolean masks: each "dendrite" unit reads 16 input pixels (random, local or soma-clustered receptive field), each "soma" unit reads only its own disjoint set of dendrites; ordinary backpropagation with gradients masked to the same graph | The wiki's cheapest architectural prior: biology contributes a *connectivity graph* and nothing else (no dendritic nonlinearity, no local rule), and buys 1–3 orders of magnitude fewer trainable parameters at matched accuracy, no overfitting at any size tested, better noise and class-blocked-sequential robustness, and a flip from class-specific to **mixed-selective** hidden units — plus the counter-case that the advantage over randomly sparse nets narrows with scale (T64) |
| [[wiki/entities/hag-reservoir.md]] | Hebbian Architecture Generation: an echo state network whose recurrent matrix starts **empty** and is *grown* by a local unsupervised rule — a neuron outside a homeostatic band on its own mean rate or activity variance grows one incoming excitatory edge from the off-band neuron it correlates with most over a randomly-resampled window, and prunes a random one when over-active; only the ridge readout is trained | The wiki's only case of a learning rule whose object is the **connectivity graph** rather than the values on it, hence its one algorithmic answer to G29 (architecture search at the cost of a single unsupervised pass, step = one synapse); the measurement that expansion is not the objective — effective dimensionality and class separability come apart, and the best networks sit at *moderate* dimensionality (T75); and the demonstration that grown structure buys the decorrelation an excitatory–inhibitory network buys, using positive weights only |
| [[wiki/entities/simple-cycle-reservoir.md]] | Simple Cycle Reservoir: a linear reservoir whose entire recurrent matrix is a single ring of identical weight `λ` with `{−1,+1}` (or `{±1,±i}`) input signs, proved **constructively universal** over time-invariant fading-memory filters via Egerváry dilation → roots-of-unity perturbation → block-circulant lifting → sign-matrix quantisation (Li, Fong & Tiňo 2024) | The wiki's only universality theorem for a *constrained* architecture, and its only architecture **derived** rather than searched or hand-built (G29); the explicit exchange rate structure↔dimension (`n' ≈ (N+1)·n·k`, `k > nm`); the demonstration that universality cannot discriminate architectures, since C-SCR, SMCR and Twin SCR are all universal with one recurrent parameter each (T174 against [[wiki/entities/hag-reservoir.md]]); and the formal line under the whole `ρ(W) < 1` family — no contractive recurrent system carries a non-fading dependency at any width or wiring |
| [[wiki/entities/cfc.md]] | Closed-form Continuous-time network: the LTC with its ODE solver deleted — the remaining integral `∫₀ᵗ f(I(s))ds` is approximated by `f(I(t))·t` and `f(−I(t))` under a sharp bound, then the exponential decay is replaced by a sigmoid, giving `x(t) = σ(−f(x,I;θ_f)·t) ⊙ g(x,I;θ_g) + [1 − σ(−f·t)] ⊙ h(x,I;θ_h)` (Hasani et al. 2022, Nature Machine Intelligence) | The wiki's first gate whose argument is **elapsed time × a learned rate** rather than a value, which is how a discrete cell handles irregular sampling without an integrator; the already-built instance of [[wiki/entities/ltc.md]]'s proposed rate/content factorisation (G54) — with a **null** verdict, since the un-split Cf-S performs comparably; the vehicle for T175's missing compute-matched expressivity measurement, since there is no solver step count left to confound it; 1–5 orders of magnitude faster than ODE-based peers (8,752% on human activity, 160–220× on PhysioNet) at SOTA 98.09% on event-based sequential MNIST and +18% over transformers on irregular Walker2D; and the explicit price of deleting a differential equation — no invertibility, no vector field to read, the DCM causal reading conceded to be "less straightforward", and the LTC stability theorems not transferring |
| [[wiki/entities/ltc.md]] | Liquid Time-Constant Network: a continuous-time RNN whose state update is linear in `x` but whose decay rate is computed by a neural network from the current state and input — `dx/dt = −[1/τ + f(x,I,θ)]x + f(x,I,θ)⊙A`, so `τ_sys = τ/(1+τf)` is a learned, per-unit, per-instant memory horizon; solved by a fused explicit/implicit Euler step and trained by BPTT rather than the adjoint (Hasani et al. 2021) | The wiki's only architecture where the **timescale itself is a learned function of the input** rather than a constant, a hyperparameter or an external modulatory register; state and `τ_sys` provably bounded from the driving-force term `(A − x)` alone, with no energy function; the field's own statement that universality cannot separate architectures, followed by a trajectory-length measure that does — but at up to 133× more solver steps than the baseline, so the separation is compute-confounded (T175); and the concession that `τ_sys ≤ τ` keeps it inside the fading-memory class, confirming [[wiki/entities/simple-cycle-reservoir.md]]'s boundary from the nonlinear side |
| [[wiki/entities/ms-ssm.md]] | Multi-Scale State Space Model: a learned undecimated (stationary) wavelet cascade splits the stream into `S+2` band-limited copies, an independent diagonal SSM runs on each with its eigenvalues initialised into a **disjoint** interval (`diag(A^s) ~ U(−N(S+1−s), −N(S−s)]`, coarse bands near `|λ|=1`), and an input-dependent row vector `E_t = Linear_E(x_t)` mixes the bank back to one channel at matched total state (Karami et al. 2025, preprint) | The wiki's best hierarchical-reasoning result for a sequence model (ListOps **63.04%**, against S4 59.60 and Mamba 38.02) and the built version of [[wiki/entities/simple-cycle-reservoir.md]]'s never-exploited Def. 7 bank of horizons; the finding that at fixed state the **spread** of the spectrum is worth 25 points where doubling the state is worth 4; the sharpening of T177 from "which initialisation" to "which *measure* the initialisation is drawn from" (scale-stratified 63.04 vs Mamba's init 57.49); the separation of selection-over-timescales from selection-over-positions, with band-selection alone under a fully LTI recurrence beating Mamba's per-position gating by 24.8 points; and the closest thing here to G54's timing/content split — a frozen horizon bank chosen among by a content-derived cue that cannot alter it |
| [[wiki/entities/conceptor.md]] | Conceptor: the correlation matrix of the states a reservoir visits under a driver, regularised into `C = R(R+α⁻²I)⁻¹` — a positive semi-definite operator with singular values in `[0,1]`, i.e. a **soft subspace**. Inserted into the feedback loop (`x(n+1) = C tanh(Wx(n)+b)`) it selects one of many patterns superposed in a single weight matrix; because every `C` has the same type as the data it came from, the family is closed under `∧ ∨ ¬` and partially ordered by abstraction (Jaeger 2014) | The wiki's only object that is at once a filter on dynamics, a description of data and a term in a logic, hence its only mechanism where a symbol and its referent need no interface; the only store that **reads its own occupancy at run time** (`q = Σs_i(C¹∨…∨C^j)/N`, replicas cost zero quota, overload spills one item — G42, G38); the answer to [[wiki/concepts/autonomous-pattern-generation.md]]'s missing run-time conditioning variable and to the standing objection that a trajectory cannot leave an attractor; a rejector built by negation rather than trained (3.4 vs 8.4/5.9 misclassifications, G68); precision weighting derived without Bayes (inter-layer trust `τ`, confabulation at `τ→1`); and the class-learning effect — a store that crosses from rote recall to representing the parametric class **as it is overloaded** |
| [[wiki/entities/s4.md]] | Structured State Space sequence model: a *linear time-invariant* state-space layer `x′ = Ax + Bu`, `y = Cx`, with `A` initialised to a HiPPO continuous-time-memorisation matrix and reparameterised as normal-plus-low-rank so the same `5N` parameters evaluate three ways — as a continuous ODE (retune `Δ`), as an `O(N)`-per-step recurrence (constant-memory generation), or as one global FFT convolution (parallel training), all in `Õ(N+L)` | The wiki's first system to carry a 16,384-step dependency (Path-X 96.35% where every attention variant is at chance; Long Range Arena 86.09 avg, state of the art on all six tasks), done from *inside* the fading-memory class SCR's exclusion list covers — and the ablation that says the whole result is the **initialisation**, not the architecture: every init reaches 100% training accuracy and validation separates by >15% ([[wiki/empirical-tensions.md]] T177)
| [[wiki/entities/transformer.md]] | The original attention-only sequence transducer: `softmax(QKᵀ/√d_k)V` in `h=8` parallel `d_k=64` heads, residual + LayerNorm, a position-wise 2048-unit MLP, causal masking in the decoder, and order supplied by a sinusoidal vector **added** to the content embedding (Vaswani et al. 2017) | The primary source under ~30 pages that cite the architecture second-hand, and the correction to how the wiki reads it: the stated justification is **maximum path length** (`O(1)` vs recurrence's `O(n)`), not content addressing — the claim [[wiki/entities/s4.md]] falsifies at 16,384 steps (T179); multi-head as an anti-averaging repair with a measured interior optimum at constant compute (1 head 24.9, 8 heads 25.8, 32 heads 25.4 BLEU); the `d_k` ablation (64→16 costs 0.7 BLEU at fixed `d_v`) as the earliest datapoint for [[wiki/concepts/retrieval-capacity.md]]'s head-width bound, with the authors conceding the dot product may be too weak a compatibility function; positional code as **superposition** with content, and sinusoidal ≈ learned (25.8 vs 25.7); 28.4 BLEU EN→DE at ~100× less compute than the top ensembles; and 91.3 F1 constituency parsing from 40K sentences with no tuning |
| [[wiki/entities/meta-rl-agent.md]] | A fully connected LSTM taking `(o_t, a_{t-1}, r_{t-1})` and emitting a policy and a state value, its weights trained across a task *distribution* by a model-free dopamine-style reward-prediction error, then frozen (Wang et al. 2018) | The wiki's primary source for meta-RL and its existence proof that the fast learner can be *activity*: frozen-weight bandit performance competitive with Gittins/Thompson/UCB, an emergent volatility-dependent learning rate orders of magnitude above the outer loop's 0.00005, and model-based two-step behaviour and RPEs (`r² = 0.89`) produced by a model-free trainer — which is what breaks the two-step task as a diagnostic ([[wiki/empirical-tensions.md]] T105). Also the wiki's only architecture whose teaching signal is also an input (gap G57) |
| [[wiki/entities/spacetime-attractor.md]] | A prefrontal attractor network whose units code *(location, delay)* pairs, whose recurrent weights between consecutive delay-subspaces are copies of the environment adjacency matrix, and whose reward input is indexed by delay; its fixed points are entire future trajectories (Jensen et al. 2026) | The wiki's first circuit model that plans **by inference rather than by rollout**: depth is a neuron budget instead of a scheduling decision (G15/G24), the world model is instantiated in parallel rather than applied serially, structural generalisation is inhibitory gating of a learned transition superset with no plasticity (G47), and RNNs meta-trained on dynamic-reward planning converge on it (adjacency correlation 0.91 ± 0.07), which names the inner algorithm [[wiki/entities/meta-rl-agent.md]] leaves opaque |

| [[wiki/entities/thousand-brains-theory.md]] | Cortical uniformity taken literally: ~150,000 columns each running one algorithm — a layer-6 grid-like code path-integrating an efference copy from layer 5, bound in layer 2/3 to the layer-4 sensory feature by layer-1 dendritic priming — so an object is thousands of partial object-anchored models reconciled by lateral layer-2/3 voting; the hippocampal formation's what/where machinery miniaturised and replicated | The wiki's source theory for [[wiki/concepts/distributed-reference-frames.md]], stated at circuit level: the only functional assignment anyone has hung on the canonical laminar graph, the two-output architecture (L5 acts, L2/3 votes) that makes voting a channel rather than a metaphor, attention as a *motor* command, and abstract reasoning as movement through a reference frame — with no equation, no simulation and no result anywhere in it, and with L6-as-content against L6-as-modulator ([[wiki/empirical-tensions.md]] T66) |
| [[wiki/entities/hodgkin-huxley-model.md]] | The neuron as a leaky capacitor with voltage-gated leaks: `C dV/dt = −Σ ḡᵢ p(V,t)(V − Eᵢ)` plus two-state gate kinetics `dx/dt = α(V)(1−x) − β(V)x`, giving `I_K = ḡ_K n⁴(V − E_K)` and `I_Na = ḡ_Na m³h(V − E_Na)`; the action potential emerges from a timescale separation between gates, with no controller | The level below LIF, and the catalogue of unit-level primitives no rate model has: multiplicative conductance input, reversal-bounded saturation, sodium-inactivation `h` as per-unit adaptation state, conjunction (`n⁴`) as a cheap sharp nonlinearity; the biophysical derivation of shunting/divisive inhibition; a fitted exponent that predicted a molecular stoichiometry (T1) |
| [[wiki/entities/deep-active-inference-agent.md]] | Active inference with every distribution replaced by a neural network, built one network at a time (VAE → deep HMM → critical HMM → full DAI) and crossed with five one-step expected-free-energy definitions and three action-selection rules, against a deep Q-network control (Champion et al. 2023) | The wiki's implementation-side evidence on [[wiki/concepts/expected-free-energy.md]]: only the reward-only ablation solves the task, the self-consistency-signed epistemic term collapses the policy to a single action, 14 of 15 full agents diverge to `NaN`, and the deep estimator substitutes a generative factor for a variational one — plus the linear-CKA instrument and the review of eight prior implementations |
| [[wiki/entities/hami.md]] | Episodic control with a *discretised* key: two contrastively pretrained encoders (event, context) quantised independently by a cosine-similarity threshold into a growing symbol alphabet, a sliding window of event-in-context symbols as the experience, and an exact-match table (symbol window, action) → best return | The wiki's first primary source for episodic control, and its only fast store whose allocate-vs-reuse threshold is **swept and mapped** rather than assumed (G38 joined to G42: over-separation fails through capacity, and the event and context factors tolerate different biases). Also the wiki's only argument that a memory's *format* is set by the physics of the read — fixed-width and discrete so the store is one non-volatile content-addressable-memory cycle. Carries the **HiCoS** environment: context-cued sequence rules over Colored-MNIST, non-Markovian, where the same observation is correct or incorrect depending on a latent rule and unobservable history |
| [[wiki/entities/hisd.md]] | Unsupervised hierarchical skill discovery: optimal-transport temporal segmentation of unlabelled pixel trajectories into `K` skills, then Sequitur grammar induction over the symbol strings, with parse trees as the task hierarchy and rules deployed as composite options | The wiki's first built answer to option discovery (G33) that also produces the *nesting*; the compression route to temporal abstraction, needing no transition graph, actions, rewards or interaction |

## Benchmarks and datasets

| Page | What it is | Load-bearing for |
|---|---|---|
| [[wiki/entities/arc-agi.md]] | ~1,000 hand-authored grid transformation tasks, ~3 example pairs each, evaluation tasks unknown to the developer, priors enumerated as Spelke's four core systems | The wiki's first benchmark page and its only instrument for developer-aware generalization; the pure edge-label-latent case with a co-latent vocabulary |
| [[wiki/entities/arc-agi-2.md]] | Chollet et al. 2025/2026: the same grid format re-authored against version 1's measured flaws — brute-forceable tasks removed, 407-participant first-party human calibration to ≤1pp across subsets, difficulty moved to multi-rule/multi-step/contextually-gated/in-context-symbol tasks, and cost-per-task made a mandatory reporting axis | The wiki's only repaired benchmark; the four-axis taxonomy of compositional failure; the knowledge-overfitting contamination mode (T207) and the reported-score ambiguity (T206) |
| [[wiki/entities/arc-agi-3.md]] | ARC Prize Foundation 2026: an interactive turn-based benchmark of 64×64 grid environments where the action semantics, the mechanics **and the win condition** are all withheld, scored by RHAE — the agent's action count against a first-time human's, squared | The wiki's first benchmark to switch on hardness sources 2 and 4 deliberately; the first whose scoring function *is* the efficiency ratio; the goal-inference gap (G72) and the harness-transfer result (T208) |
| [[wiki/entities/neo-neural-theorizer.md]] (OTIB) | Observation-to-Theory Induction Benchmark: GridWorld / arithmetic factorization / CIFAR-10 image editing, with `α ∈ {0.33, 0.66, 1.00}` controlling what fraction of primitive compositions appear in training and some primitives never observed in isolation; scored by self-explainability, **transferability** (induce a program from a support pair, execute it on a query pair) and action primitiveness | The wiki's only working G17 instrument: a second query against the same representation that separates "fits this pair" from "recovered the mechanism", plus the only coverage metric for an induced vocabulary |
| [[wiki/entities/pcfg-set.md]] | A PCFG-generated sequence-to-sequence task over string-edit operations (length/depth distributions tuned to parsed English), plus **five task-independent behavioural tests** that split "compositional" into systematicity, productivity, substitutivity, localism and overgeneralisation — with a **consistency score** that compares the model against its own output under a meaning-preserving input transform rather than against a target (Hupkes et al. 2020) | The wiki's first measurement of systematicity, and its first evaluation instrument that needs no labels, no distribution shift and no ontology and is scoreable on a model that is failing the task (G17); the strongest form of G16 — data generated *by* the intended rule, memorisation designed out, 0.92 accuracy from a non-compositional solution; and the source of three measured knobs the wiki had only argued about (rule-vs-exception switching at 0.5% exceptions, length-indexed operators, local-vs-global composition, T196) |
| [[wiki/entities/spelkenet.md]] (SpelkeBench) | 500 EntitySeg images filtered in three stages to keep only physically movable, motion-defined entities, plus 50 hand-annotated OpenX-Embodiment robot frames; scored by Average Recall and mean Intersection-over-Union under a point prompt | The wiki's only benchmark whose ground truth is defined by *what moves together* rather than by category, and the only one whose annotation is stated to be unscalable in principle |

## Biological systems

| Page | What it is | Load-bearing for |
|---|---|---|
| [[wiki/entities/basal-ganglia.md]] | The striatal direct (D₁) / indirect (D₂) spiny-projection pathways and the pallidal–subthalamic–nigral chain they drive, as characterised in bacterial-artificial-chromosome transgenic mice that made the two populations separately recordable | The wiki's only opponent selector read at the mechanism level: one diffuse dopamine scalar producing *opposite* changes in intrinsic excitability **and** in the sign of corticostriatal plasticity, with the sign supplied by the receptor on the target rather than by the signal. Also the tonic-pacemaker trick that gives signed control from unsigned inhibitory projections, the Kir2 convergence gate that erases non-convergent cortical input, a thalamic burst-pause interrupt that opens a ~1 s window biased toward suppression, directed lateral inhibition whose direction is set by the current dopamine level, and the dendritic reach problem — back-propagating spikes die less than half way out, so most of the module's synapses get no somatic feedback at all. Now also the wiki's census of what the *computational* literature built on this anatomy (19 models, Helie et al. 2013): four unresolved design choices — how alternatives are cancelled, whether the module gates working memory or is its maintenance loop, whether automaticity distils into cortex or migrates within the striatum, and whether the indirect pathway is a veto or the exploration channel — and the headline negative result that no model spans one cognitive and one motor function. |
| [[wiki/entities/fly-central-complex.md]] | The *Drosophila* ellipsoid body: a toroidal neuropil whose columnar (EBw.s) neurons tile a ring and carry one heading bump, imaged as a complete population in a walking fly under closed-loop virtual reality | The wiki's only structural code observed in a *complete, identified* population, and its only direct measurement of the three regimes an integrator needs — landmark-driven anchoring (offset-preserving reset, winner-take-all between aliased landmarks), angular path integration with visible drift in darkness, and >30 s persistence with no input at all ([[wiki/empirical-tensions.md]] T46) |
| [[wiki/entities/hippocampal-prefrontal-channel.md]] | The projection between the episodic store and the control layer, studied as an object rather than as a wire: bidirectional plasticity, a dopaminergic terminal gain, an amygdala lock on its own writability, a stress-set operating point, gamma-at-encoding / theta-at-choice multiplexing, and two separately-typed directions (ventral hippocampus → ventral tier; anterior cingulate → dorsal CA1/CA3) | The specification behind gap **G52** — the wiki's only worked example of an inter-module edge with its own state, and the reason a return arrow is not a transpose |
| [[wiki/entities/medial-prefrontal-cortex.md]] | The rat medial frontal wall split along a dorsoventral axis: a dorsal tier (Fr2, dorsal anterior cingulate, dorsal prelimbic) wired to sensorimotor cortex, accumbens core and superior colliculus, and a ventral tier (ventral prelimbic, infralimbic, medial orbital) wired to hippocampus, amygdala, hypothalamus, autonomic brainstem and the monoaminergic nuclei | The wiki's only *factorized* controller — control split by output port rather than by computation, with an asymmetric ventral→shell→dorsal cascade, a layer-typed output channel that writes to the dopamine cells, and the dissociation between acquiring a rule and replacing one. Also carries the function that wiring computes — (context, event) → most adaptive response — and the recent/remote timeline showing that what consolidates into the controller is the *mapping*, not the terms; and the hippocampal input **edge** as an object with its own state — bidirectionally plastic, D1-gain-controlled, amygdala-locked, stress-detuned, and severable by asymmetric disconnection (gap G52); plus the phase dissociation between the edge (needed at encoding) and the region (needed at retrieval). **Also the human arm**: medial Brodmann areas 9/10/11 partition by *content domain* — social dorsally, self and value anteromedially, value and emotion ventrally — with only 17% of the tissue domain-selective at a strong threshold and one non-selective ventral cluster proposed to hold an integrated situation model. **And the monkey arm**: a posterior medial region individuated functionally rather than architectonically, coding which *response tactic* is running and carrying the task only while more than one tactic is live |
| [[wiki/entities/nucleus-reuniens.md]] | Midline thalamic nucleus carrying the indirect medial-prefrontal ↔ hippocampal route, with single neurons collateralising to both structures | The return arrow the direct anatomy denies: the controller reaches the episodic store only through this relay, which delivers a *goal-conditioned future path* rather than a retrieval query, and whose lesion reproduces a hippocampal–prefrontal disconnection with both endpoints intact |

---

## Referenced but not yet paged

Artefacts named on existing pages that have no page of their own. Listed so the reference is not lost; each gets a page when its source is ingested.

| Artefact | Referenced from | Queued source |
|---|---|---|
| Hippocampus / medial temporal lobe | [[wiki/concepts/complementary-learning-systems.md]], [[wiki/concepts/simulation-based-planning.md]], [[wiki/concepts/offline-replay.md]], [[wiki/concepts/pattern-separation-completion.md]], [[wiki/concepts/cognitive-map.md]] | `raw/liao-2024-single-shot-many-shot-hippocampus.md`, `raw/yassa-2011-pattern-separation-hippocampus.md`, `raw/bakker-2008-pattern-separation-ca3-dentate.md` |
| Prefrontal cortex (primate lateral / general) | [[wiki/concepts/working-memory.md]], [[wiki/concepts/meta-learning.md]] | Partly covered by [[wiki/entities/medial-prefrontal-cortex.md]] (rat medial wall). Both queued sources are now ingested but neither yielded an anatomy page: Friedman & Robbins 2021 became [[wiki/concepts/control-unity-and-diversity.md]] (a factor structure, not a region) and Wang et al. 2018 became [[wiki/entities/meta-rl-agent.md]] (a model that deliberately treats all of prefrontal cortex as one undifferentiated recurrent network). A lateral/primate page still needs a source with regional resolution — dorsolateral vs anterior cingulate vs orbitofrontal |
| Entorhinal grid cells | [[wiki/concepts/abstract-structural-codes.md]] | `raw/constantinescu-2016-gridlike-code-concepts.md` |
| Differentiable neural computer | [[wiki/concepts/working-memory.md]] | `raw/graves-2016-differentiable-neural-computer.md` |
| Deep Q-network / experience replay | [[wiki/concepts/complementary-learning-systems.md]], [[wiki/concepts/causal-model-building.md]] | *(no primary source in `raw/`; Lake et al. 2017 supplies the Frostbite sample-efficiency numbers and the re-goaling critique second-hand)* |
| Elastic weight consolidation | [[wiki/concepts/continual-learning.md]] | *(no source in `raw/` — acquisition task P3)* |
| Progressive networks | [[wiki/concepts/meta-learning.md]], [[wiki/concepts/continual-learning.md]] | *(no source in `raw/` — acquisition task P3)* |
| Episodic control (Model-Free Episodic Control; Neural Episodic Control) | [[wiki/concepts/complementary-learning-systems.md]], [[wiki/entities/hami.md]] | **Now covered second-hand** — `raw/poursiami-2025-hippocampal-rl-framework.md` states the MFEC store and update rule (`(s,a,G)` tuples, keep-the-max return, `k`-nearest-neighbour estimate) and runs it as a baseline; the primary sources (Blundell et al. 2016, Pritzel et al. 2017) are still not in `raw/` |
| Monte Carlo tree search / expert Go play | [[wiki/concepts/simulation-based-planning.md]], [[wiki/concepts/causal-model-building.md]] | *(no primary source in `raw/`; Lake et al. 2017 supplies AlphaGo's training volume — 28.4M expert positions + ~100M self-play games vs. Lee Sedol's ~50,000 — and the Go-variants re-goaling argument)* |
| Intuitive physics engine (Battaglia et al. 2013) / PhysNet (Lerer et al. 2016) | [[wiki/concepts/core-knowledge.md]], [[wiki/concepts/simulation-based-planning.md]] | *(no source in `raw/` — the simulation account of the object system, and the convolutional rival needing 100k–200k scenes for one judgement)* |
| Bayesian inverse planning / naive utility calculus (Baker et al. 2009; Jara-Ettinger et al. 2015) | [[wiki/concepts/core-knowledge.md]], [[wiki/concepts/simulation-based-planning.md]] | **Now paged in its hierarchical form:** [[wiki/entities/hbtom.md]] (Zhi-Xuan et al. 2022), **and in its automated form:** [[wiki/entities/autotom.md]] (Zhang et al. 2025), which supplies the belief latent HBToM omits and searches for the model structure instead of authoring it. The Baker 2009/2017 primaries remain unread — but their human confidence curves are now in the wiki second-hand as AutoToM's correlation targets (r = 0.93 full-obs. goal, 0.88 desire, 0.73 belief) |
| Baby Intuitions Benchmark (BIB) | [[wiki/entities/hbtom.md]], [[wiki/entities/irene.md]] | `raw/gandhi-2021-baby-intuitions-benchmark.md` — described second-hand on the HBToM page (5 task sets, 8 familiarization + 1 test trial, paired plausible/implausible scoring); gets its own page when that source is ingested |
| AGENT benchmark | [[wiki/entities/hbtom.md]] | `raw/shu-2021-agent-psychological-reasoning-benchmark.md` — named as the related benchmark on which minimal-bias deep models also fail to generalize human-like |
| Omniglot / the Characters Challenge | [[wiki/entities/bayesian-program-learning.md]], [[wiki/concepts/meta-learning.md]] | *(no source in `raw/` — the benchmark the one-shot results are measured on; row 2 of the latent-variable table in [[wiki/concepts/latent-graph-discovery.md]])* |
| ImageNet | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — the canonical "dataset ≠ ability" case: intended object recognition, largely solved by texture)* |
| ImageNet-A / ImageNet-C | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — natural worst case; 15 corruptions)* |
| ObjectNet | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — scientific controls over background, rotation, viewpoint)* |
| PACS | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — domain generalisation by construction)* |
| Shift-MNIST / biased CelebA / unfair dSprites | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — controlled injected shortcuts; the instrument gap G17 asks for)* |
| Winograd Schema Challenge | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — benchmark designed to remove shortcuts, later found to contain them)* |
| ARCT (Argument Reasoning Comprehension Task) | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — shortcut-removed variant)* |
| BERT | [[wiki/concepts/shortcut-learning.md]] | *(no source in `raw/` — cue-word shortcut, above chance from "not" alone)* |
| Predictive-coding networks | [[wiki/concepts/biologically-plausible-credit-assignment.md]] | `raw/whittington-2017-predictive-coding-approximates-backprop.md` |
| Spike-timing-dependent plasticity | [[wiki/concepts/biologically-plausible-credit-assignment.md]], [[wiki/concepts/synaptic-plasticity.md]] | `raw/bi-1998-spike-timing-dependent-plasticity.md` |
| Behavioral timescale synaptic plasticity | [[wiki/concepts/synaptic-plasticity.md]], [[wiki/entities/btsp-cam.md]] | `raw/liao-2024-single-shot-many-shot-hippocampus.md` (review; primary source Bittner et al. 2017 not in `raw/`), `raw/wu-2025-btsp-content-addressable-memory.md` (the functional memory-system theory) |
| Fly algorithm / random projection (Dasgupta et al.) | [[wiki/entities/btsp-cam.md]], [[wiki/entities/vector-hash.md]] | *(no source in `raw/` — a-priori binary random projection into a 40× expanded layer with global winner-take-all; the baseline BTSP is shown to beat, because a learned projection has basins and a frozen one does not)* |
| Sharp-wave ripple / replay | [[wiki/concepts/offline-replay.md]], [[wiki/concepts/complementary-learning-systems.md]] | `raw/liao-2024-single-shot-many-shot-hippocampus.md`, `raw/talk-nd-memory-gating.txt` |
| Hopfield network | [[wiki/concepts/synaptic-plasticity.md]], [[wiki/concepts/biologically-plausible-credit-assignment.md]], [[wiki/entities/vector-hash.md]], [[wiki/entities/sparse-distributed-memory.md]] | *(no primary source in `raw/`; Chandra et al. 2023 supplies the measurements the wiki uses — the memory cliff at ~`0.14N`, the `N²`-bits bound, and the ~50-step ceiling of the asymmetric sequence variant, across classical, sparse-weight and sparse-pattern versions; Keeler 1988, reported in Kanerva 1993, adds the equivalence that matters for design — same capacity *per storage element* as SDM, but Hopfield's is capped by the pattern size while SDM's is not)* |
| Cerebellar cortex (Marr codon model / Albus CMAC) | [[wiki/entities/sparse-distributed-memory.md]] | `raw/kanerva-1993-sparse-distributed-memory.md` (secondary presentation of Marr 1969 and Albus 1971; granule cells as address decoders with Golgi-set activation threshold, parallel-fibre→Purkinje synapses as counters, climbing fibre as the write line) |
| REINFORCE / node perturbation | [[wiki/concepts/synaptic-plasticity.md]] | *(no source in `raw/` — the policy-gradient estimator a biologically plausible three-factor rule turns out to implement)* |
| Eligibility propagation (e-prop) | [[wiki/concepts/biologically-plausible-credit-assignment.md]], [[wiki/entities/spiking-neural-networks.md]] | *(no source in `raw/` — forward-only local credit assignment for spiking nets)* |
| Feedback alignment / direct feedback alignment / sign-symmetry | [[wiki/concepts/biologically-plausible-credit-assignment.md]] | *(no source in `raw/` — sign-symmetry is the only variant matching backpropagation at ImageNet scale)* |
| Differentiable plasticity | [[wiki/concepts/meta-optimized-plasticity.md]] | *(no source in `raw/` — gradient-optimized plasticity coefficients, incl. neuromodulated variants)* |
| Evolvable Neural Units | [[wiki/concepts/meta-optimized-plasticity.md]] | *(no source in `raw/` — evolved somatic/synaptic compartments that rediscover spiking dynamics and RL-type rules)* |
| Loihi / TrueNorth / SpiNNaker | [[wiki/entities/spiking-neural-networks.md]] | *(no source in `raw/` — neuromorphic platforms; locality is a hardware constraint here, not a preference)* |
| CoinRun / Obstacle Tower | [[wiki/entities/arc-agi.md]] | *(no source in `raw/` — generalization tests whose level generators are public, so they measure local generalization: new samples from a known distribution, not a new task)* |
| Raven's Progressive Matrices | [[wiki/entities/arc-agi.md]] | *(no source in `raw/` — the 1930s IQ format ARC is styled after; item types are public and hard-codable, which is what makes it gameable for machines)* |
| OpenAI Five / DotA2 | [[wiki/concepts/skill-acquisition-efficiency.md]] | *(no source in `raw/` — 45,000 years of self-play, 16 of 100+ characters, reliably beaten by non-champion humans days after public release; the wiki's canonical case of skill bought with unlimited experience)* |
| Genie | [[wiki/entities/adaworld.md]] | *(no source in `raw/` — the discrete-latent-action lineage: a VQ codebook of 8 actions learned from unlabeled video for playability; reimplemented as the discrete baseline that loses cross-context transfer by 2× FVD, tension T148)* |
| Stable Video Diffusion / EDM | [[wiki/entities/adaworld.md]] | *(no source in `raw/` — the video latent-diffusion backbone repurposed to denoise one frame at a time, which is what turns clip generation into frame-level action control)* |
| Procgen / Gym Retro / LIBERO / VP² | [[wiki/entities/adaworld.md]] | *(no source in `raw/` — the procedural-game, retro-game, robot-manipulation and video-prediction-for-planning suites; Procgen goal-reaching is where an action-agnostic world model is measured at exactly chance)* |
| VQ-VAE / VAE / implicit rank minimisation / sparse coding | [[wiki/concepts/energy-based-models.md]] | *(no source in `raw/` — the four families of latent-capacity regulariser: quantisation, rank, sparsity, noise)* |
| Transformer | [[wiki/entities/h-jepa.md]] | *(no source in `raw/` — permutation-equivariant, hence the proposed predictor for object-level interaction; also the substrate for configuration-by-extra-token)* |
| Memory networks / entity networks | [[wiki/entities/h-jepa.md]], [[wiki/concepts/working-memory.md]] | *(no source in `raw/` — key-value stores holding one entry per world entity, the architecture the world-state memory is copied from)* |
| The C-Test (Hernández-Orallo) | [[wiki/entities/arc-agi.md]] | *(no source in `raw/` — AIT-grounded induction test; programmatically generated from a master program, which is Chollet's stated reason for hand-authoring ARC instead)* |
| Chinchilla / Llama 2 | [[wiki/concepts/prediction-compression-equivalence.md]] | *(no source in `raw/` — the pretrained text models measured as general-purpose compressors: Chinchilla 70 B reaches 48.0% on ImageNet patches and 21.0% on LibriSpeech, beating PNG and FLAC)* |
| gzip / LZMA2 / PNG / FLAC | [[wiki/concepts/prediction-compression-equivalence.md]] | *(no source in `raw/` — the classical baselines: tiny programs with long contexts and no adaptation, the opposite corner of the slow-**W** / fast-**M** trade)* |
| enwik8 / enwik9 (Hutter Prize) | [[wiki/concepts/prediction-compression-equivalence.md]] | *(no source in `raw/` — the 100 MB / 1 GB Wikipedia dumps used as the standard compression benchmark, and the source of the "compression = generalization" framing the wiki inherits)* |
