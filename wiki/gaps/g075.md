# G75 — Nothing chooses how to parse the input; the object decomposition is fixed before the task is seen

**Status:** `OPEN`  
**Kind:** `part`  
**Level:** `L0`  
**Registry:** [[wiki/architectural-gaps.md]]  
**Closes when:** A model chooses its object decomposition per task from an open space rather than a fixed menu, and its parse choices match the human per-task convergence on the same ambiguous inputs. Special case of `G73`.

## Why it blocks the target

The symbolisation step every symbolic account presupposes is itself an inference. In ARC the parse is genuinely ambiguous — occlusion, and whether a set of cells is one object or two — and humans re-decide it *per task*, with their action traces converging on bottleneck states that are the task's own objects (Johnson et al. 2021). Machines do not: every solver in the wiki carries one parse rule, chosen at design time, and its errors then violate the objectness prior the benchmark declares (elongated shapes, wrap-around) whatever its score. The cost is that a wrong parse is indistinguishable from a wrong rule — the search reports failure, not that it was searching over the wrong objects. This is G73 arriving *inside* a format that supposedly fixes the framing

## Best current answer

Enumerate parses and search over them: [[wiki/entities/arc-vsa-solver.md]] treats the object parse as one of six hypotheses and selects jointly with the rule, which is the only instance in the wiki where the parse is a variable rather than a constant, and it is a fixed finite menu. [[wiki/entities/spelkenet.md]] computes an objectness segmentation from video rather than declaring one, but the segmentation is not task-conditioned. Human behaviour says the choice is made *after* seeing the demonstrations and is therefore a function of the task, which neither supplies. **And a third answer that splits the row in two** ([[wiki/entities/resonator-network.md]], Frady et al. 2020): encode a scene as a superposition of `colour ⊙ shape ⊙ v ⊙ h` products and decode it by factorization plus deflation — converge on one object, subtract it, reset, repeat. The **number of parts is a free variable** determined by when the loop stops finding one, with no menu and no hypothesis enumeration, which is the first mechanism here that does not fix the part count in advance. The **axes** are untouched and are worse than a menu: the four codebooks are declared before the task and cannot be re-chosen. So the row is really two questions — *how many parts* (answered, by a dynamical stopping condition) and *along which factors* (unanswered, and now clearly the harder half, since it is the same question as G4's vocabulary co-discovery). Two costs come with it: which object the network reads out first is uncontrolled, and non-convergence is silent

**And the account that says the row has been asking the wrong question** ([[wiki/concepts/visual-routines.md]], Ullman 1984). Every answer above computes a parse *of the input*: a menu ranked by a score, a segmentation read off a probe, a factorization loop that stops when it stops finding parts. Ullman's claim is that there is no parse independent of the question — a spatial relation is established by running a **routine** assembled per goal from a small fixed instruction set (shift of processing focus, indexing to an odd-man-out, bounded activation, boundary tracing, marking) over an unarticulated base representation, and the "objects" are whatever that routine's bounded activation happened to enclose, retained only in a goal-dependent *incremental representation*. Three things this does to the row. (i) It supplies, for the first time, a **specification of what the open space is made of**: not a larger menu of parses but the closure of an operation set under composition, which makes [[wiki/entities/arc-vsa-solver.md]]'s six hypotheses a *degenerate compilation* — six pre-compiled routines — and tells you that enlarging the menu is the wrong repair. (ii) It splits off a requirement no wiki architecture meets and that is cheaper than the row's `Closes when`: the parse must be **query-relative**, so the same input under two goals must yield two different retained structures, which is directly testable and never tested. (iii) The residue moves: the operations are named, the **assembly mechanism is not** — how a goal is turned into a sequence, and how routines are stored skeletonised and expanded, is stated as an open problem in 1984 and is still open, which is this row and `G73` restated as a program-synthesis problem over a perceptual instruction set rather than as a selection problem over parses. The account is a computational-theory paper with no implementation, so it adds no evidence, only the shape the evidence would have to take.

**And the account that answers the row's first half and shows why its second half cannot be answered the way it is posed** ([[wiki/concepts/visual-indices.md]], Pylyshyn 2001). The *how many parts* question gets a hard number with a mechanism behind it — 4–5 sticky pointers seized by feature clusters, so the parse is bounded by a pool of referring devices rather than by a slot count or a stopping condition — and it is a bound on what can be *simultaneously referred to*, which is what a relational predicate needs: `Collinear(X₁…Xₙ)` is computable only for the `n` its arguments can be bound for. Two contributions and one obstruction. **(i)** The obstruction is the important one: objecthood here is defined **operationally, by trackability** — a sequence of locations is one object if early vision groups it that way — and the paper concedes that what qualifies as an indexable object "remains an open empirical question", offering a list of cases (simple figures yes; some well-defined feature clusters no; occlusion-form disappearance yes; shrink-and-regrow no) rather than a rule. That is a criterion a designer cannot use, because it names the mechanism being designed, and it is the same residue as [[wiki/entities/spelkenet.md]]'s learned segmenter one level down. **(ii)** It contradicts Ullman's query-relativity on the point this row now treats as settled: an index is assigned **before any question is asked**, by the stimulus, and the paper's assumption 5 makes the indexed set the *only* thing downstream cognition can operate on. So the two accounts the row leans on jointly imply a two-stage parse — the stimulus proposes ≤5 candidate individuals, the goal chooses among and elaborates them — which neither states as such and which changes what the row's `Closes when` should measure: not whether the retained structure differs per goal, but whether the *candidate set* does (`T387`). **(iii)** It also supplies the row's cheapest unrun instrument: no ARC result in the wiki reports how many individuals a solver had to hold at once, and the human bound is a small integer that a solver with unbounded addressing never faces.

**And the first implementation of any of it, which moves the row's residue rather than the row** ([[wiki/entities/multiscale-tracing-network.md]], Mollard, Bohte & Roelfsema 2026). Two of Ullman's five elemental operations — bounded activation and boundary tracing — turn out to be *one* operation at four scales, and it is learned from a binary reward with no segmentation supervision anywhere in the recurrent group, generalising from a 7-pixel curriculum to 30-pixel curves at 100% and transferring from curve tracing to 2-D region parsing in under 100 trials. Three consequences. **(i)** The objection that the operation set is too expensive to learn is removed: ~23,200 trials of binary reward buy a routine with a distance-independent fixed point, and the property that makes it generalise is an *inhibitory circuit motif* (VIP⊣SOM⊣pyramidal bounds released activity by the cell's own feedforward drive) rather than a loss term — replace it with direct excitation and the tag either runs away or attenuates with distance. **(ii)** The row's core demand is untouched and now sharply visible: **there is exactly one routine here and nothing selects it**. The parse is not query-relative, not composed, and not chosen; the seed location is handed to the model. So what a decade of this row called "the parse problem" splits cleanly into an *execution* half that is now solved and an *assembly* half on which nothing has moved since 1984. **(iii)** It supplies the row's first cost curve. The number of timesteps to arrival predicts human reaction times directly (`R² = 0.55` against a `0.67` noise ceiling), which is the falsifiability standard `G75`'s other candidates cannot meet — a menu, a probe and a factorization loop each return a parse with no prediction about what it should have cost. **What it does not supply is the criterion.** Collinearity-and-connectedness is a supervised label on the frozen feedforward gate, so grouping is still declared by the designer, merely relocated into a pretraining objective; joint learning of criterion and routine worked at one scale in earlier work and failed at four here.

## From

Johnson et al. 2021; [[wiki/entities/resonator-network.md]] (Frady et al. 2020); [[wiki/concepts/visual-routines.md]] (Ullman 1984); [[wiki/concepts/visual-indices.md]] (Pylyshyn 2001); [[wiki/entities/multiscale-tracing-network.md]] (Mollard, Bohte & Roelfsema 2026)

## Status

`OPEN` **Overlap flagged at the 284-source duplicate audit: this row is the parse-specific case of `G73`** (nothing constructs a problem representation). Kept separate because the parse is the one element with per-task human evidence behind it.
