# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

**Status (lint pass, 264 sources).** Wave 11 (neuromorphic implementation) closed at 12 and wave 12 (large-scale brain organisation) is at **17 of 38** — the network and default-mode sub-waves are running, and the consciousness, thalamocortical and visual-cortex sub-waves are unstarted. This pass ran at **20 ingests**, honouring S2 for the fourth time running (19, 22, 20, 20). Mechanical health: **no orphans, no unindexed pages, no dead index entries, no broken `[[wiki/...]]` links, no duplicate or missing `G`/`T` identifiers, no page under 75 lines, every page carrying a `## Connections` block, and 3 asymmetric edges out of 4,832** — 0.15 per ingest against 0.10 at the last pass. All three were repaired here. One malformed table row was found and fixed: **`G80`** carried an unescaped restriction bar in `(λ_I,j − λ_V,j)|_{t_k + d_ji}`, which silently shifted its `From` and `Status` cells one column left. S4 caught it; nothing else could have.

**The finding of this pass is that the wiki's two open-problem registries have outgrown the format they are written in, and that the cost is now paid by every ingest rather than by this file.**

1. **The registries are 27% of the wiki by bytes.** [[wiki/architectural-gaps.md]] is **448 KB in 336 lines**; [[wiki/empirical-tensions.md]] is **543 KB in 266 lines**. Together they are **991 KB against the wiki's 7.27 MB**. Line counts hide this completely — the files look mid-sized and are the two largest objects in the repository by an order of magnitude. Gap rows average **3,434 bytes**, tension rows **2,114**. This matters because CLAUDE.md requires both files to be updated *at every ingest* and rewritten *at every lint pass*: an operation that must read 991 KB before it can add one row cannot be performed faithfully, and has not been for some time. The evidence that it has not is `L10` below.
2. **The `Status` column stopped being a status.** The tensions key declares three tokens. The column holds **108 KB of prose across 256 rows** — 195 rows exceed 120 characters, **20 exceed 1,200**, and the largest is a 4 KB essay on wave-12 default-mode evidence. The gap table has the same defect in eight rows. A status field that cannot be read by `sort | uniq -c` is not a field, and the consequence is that **nothing in the wiki can currently answer "which tensions are live?" without reading half a megabyte**. Two token errors surfaced once the column was parsed: `T235` was marked `OPEN`, which belongs to the *gap* key and not this one (fixed, now `LIVE`), and three rows use `BOTH`/`LIKELY BOTH` to say something the key had no token for — both positions hold in different regimes. That is a real state, so the key gained the token rather than the rows being flattened.
3. **`architectural-gaps.md` carries 139 KB of provenance blockquotes** — **109 blocks, 31% of the file**, appended one per ingest, **out of chronological order** (the 212-ingest lint note sits at line 93, ahead of the "second ingest" note at line 107), and duplicating the `INGEST` commit messages almost verbatim. CLAUDE.md says *"Gitlog is changelog."* This is the one place the wiki keeps a second one.

**Step 0 (source integrity).** Not verifiable this pass, for the second time running. `https://status.claude.com/` was not reachable through the available fetch path, so no incident window could be checked against the wave-12 ingest dates. Nothing was discarded; recorded as an unverified step rather than a clean one.

**Six published self-counts were wrong, which is `S10` failing on the pass after it was written.** [[wiki/overview.md]] said 245 sources (derivable: **264**), 98 concept pages (**115**), 105 entity pages (**108**), 79 gaps (**89**), 237 tensions (**256**), and wave 11 "10 of 12, in progress" (**12, complete**). Every one had a one-line command available and none was run. The paragraph is rewritten with the commands inlined beside the figures, so the next pass can diff rather than recount.

Five things changed at this pass:

1. **`L12` opened, and it is the largest standing debt in the wiki.** [[wiki/overview.md]]'s thematic body stops at **section 14 (wave 11)**. Wave 12's seventeen ingests — whole-brain network models, the structure-to-function map, connectome topology, the default-mode network — have pages, gap rows and tension rows but **no synthesis section**. This is the fifth consecutive violation of `S5`, and the first where the miss is a whole wave rather than a count.
2. **`L13` opened** — the registry restructure, specified below. It is the successor to `L10`, which has been treating the same decay one cell at a time for four passes.
3. **`L10` re-measured, and it lost ground.** The four cells it watches all *grew* since the last pass: **G42 8,709 → 14,729 (+69%)**, **G38 8,483 → 11,140 (+31%)**, **G34 12,687 → 14,506 (+14%)**, **G37 12,671 → 14,267 (+13%)**. A carried task that is losing 20% a pass is not a task, it is a symptom; `L13` is the response.
4. **Three `Connections` reciprocals repaired** — `structured-flows-on-manifolds → learnable-synaptic-delays`, `hag-reservoir → effective-connectivity`, `virtual-brain-twin → attractor-dynamics`. Each got a return edge stating the relation from the target's side rather than a mirrored sentence.
5. **`G80` unescaped-pipe repair**, the tensions status key extended with `BOTH`, and `T235` moved from the wrong key's token to `LIVE`.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P1 | **Write [[wiki/overview.md]] section 15 for wave 12** (`L12`) | **Promoted to the top of the list.** Seventeen ingests of large-scale brain organisation are on disk with pages, gap rows (`G81`–`G90`) and tension rows, and nothing joins them. Wave 12 is the first wave whose subject is the *organisation of a whole system* rather than a mechanism, which means the synthesis is not optional bookkeeping — it is where the wiki finds out whether its architecture has network-level properties at all. The previous P1 (**finish wave 11**) closed: all twelve sources ingested, `G78` and `G80` moved to `PARTIAL`, `G76`/`G77` still open | Ready to attempt |
| P1b | **Restructure the two open-problem registries** (`L13`) | 991 KB across two files that every ingest must touch. The tensions file first — larger, more uniform, and no provenance block to adjudicate. Parts (a)–(c) are mechanical and can be scripted; part (d), deleting the 139 KB of provenance blockquotes, removes content and needs the curator's assent | (a)–(c) ready to attempt; (d) blocked on human confirmation |
| P2 | **Ablate the interface** — a learnable hybrid unit against a fixed converter, at matched architecture | T235 is `LIVE — B asserted, not measured`. Every hybrid result in wave 11 beats a single-paradigm baseline and none isolates the interface, so the wiki's newest structural idea (a module boundary with its own parameters and its own objective, [[wiki/concepts/cross-paradigm-interface.md]]) rests on nothing. One control run decides the whole `G52`/`G21` cluster | Ready to attempt |
| P3 | **Report units-to-competence for anything** | If the spiking substrate's proven advantage is unit *count* rather than expressive power ([[wiki/concepts/circuit-size-separation.md]], T234), that is the only admissible evidence for it — and no benchmark in the wiki reports it. It is derivable from runs that already exist | Ready to attempt |
| P4 | **Run `I8` and `I9` against the wiki's own entity pages** | *Carried.* The two instruments that need nothing but the model itself: the support/query split and the wrong-items-only consistency score. The inventory's cost ordering is its main claim and is still untested *as a claim* | Ready to attempt |
| P5 | **Collect a per-item human error *profile* on one benchmark the wiki already uses** | *Carried.* Job 4 at [[wiki/concepts/human-baseline.md]] and `F4`: aggregate parity is compatible with an orthogonal mechanism (.90 vs .91 accuracy at **0.06** profile correlation), so the wiki's central claim — that the biological solution is worth importing — has no available evidence form | Human curation, or a re-analysis request |
| P6 | **Watch L6** — the last remaining oversized-*page* item | *Reduced again.* `L5` is downgraded to a watch: [[wiki/entities/medial-prefrontal-cortex.md]] grew 434 → **439 lines** over twenty ingests, which is a page that is large because its subject is, not one that is accreting. What remains is seventeen joint-embedding entity pages with no shared comparison table. **The oversized-page problem has migrated**: the wiki's size decay is no longer in `wiki/concepts/` or `wiki/entities/` — no page there is under 75 lines or over 439 — it is in the two registries (`L13`) | — |
| P7 | **Build the write-mask experiment (G52), and build G64 with it** | *Carried.* Still the cheapest concrete architecture proposal in the wiki: a third module emitting a binary licence on a learned interface's plasticity, scored on continual-learning interference, with `δ_j = snr_j · δ` as the credit-side twin. P2 is now its prerequisite measurement | — |
| P8 | **Run the `g`/`x` objective search, with the collapse filter applied first** | *Carried.* `G30` remains the wiki's deepest gap. A candidate must separate a real structural code from a constant `g`, which satisfies path-consistency exactly ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P9 | **Score architectures against the six hardness sources**, per the placeholder in [[wiki/concepts/latent-graph-discovery.md]] | *Carried, unmoved for seven passes.* 105 entity pages and the comparison instrument is still empty. The fix wave 10 supplies: a **facet vector** per architecture, not a scalar, because conclusion 1 of [[wiki/concepts/certification-instruments.md]] says any single number is under-determined — and each facet now has a named instrument | Ready to attempt |
| P10 | **Re-price G37 against the retrieval result** | *Carried.* Wave 9 says the cost of analogy is in retrieval, not mapping (88% vs 12% cued retrieval; ~86% transfer once cued), and every amortisation entry in the wiki is written as an approximation to the *mapper*. MAC/FAC supplies the first candidate of the right shape | Ready to attempt |
| P11 | **Run the two experiments wave 7 left specified and unrun.** (a) Probe and ablate OthelloGPT's candidate end-game features (T157/T158). (b) Run LeJEPA's rescaled-loss checkpoint ranking against a **patch-token** probe on a long run | *Carried.* Both are named inside ingested sources, need no new material, and each attacks a `LIVE` tension with no procedure | Ready to attempt |
| P12 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | *Carried, unmoved for seven passes.* Named as the obvious next model by more than one source. This pass adds a reason to do it: the clone-pool construction is the rate-level answer to element distinctness, which one spiking neuron computes in a single unit (`G80`, [[wiki/concepts/circuit-size-separation.md]]) | — |
| P13 | **Run the `γ_effective` readout** — fit a discount kernel to a trained agent's choices, fit a second to a unit's activations, test covariance *and* zero mean offset | *Carried.* T141 is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error on a quantity with no stationary value function | Ready to attempt |
| P14 | **Instrument one existing store with a read log** | *Carried.* The cheapest experiment [[wiki/concepts/memory-read-and-erase.md]] names: log when and at what a store was read, then score the schedule separately from the contents | Ready to attempt |
| P15 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | *Carried.* `G23`: every machine prior applies unconditionally while every core system carries an entry test, and the gate would also supply `G12`'s router | Human curation or a web-search pass |
| P16 | Acquire Liao et al. 2022 — the inhibitory-plasticity model of the replay filter | *Carried.* [[wiki/concepts/offline-replay.md]] asserts inhibitory plasticity as the filter's substrate on modelling grounds; T30 cannot be adjudicated from a review | Human curation |
| P17 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) and for ObjectNet / ImageNet-C | *Narrowed at this pass.* `I1` is the first instrument in the inventory and the wiki still has no primary source for its image half — but wave 10.24 supplied one for the language half ([[wiki/entities/shortcut-suite.md]]), whose construction is label-preserving *by proof* rather than by a rater. That is the standard the image sources should be read against | Human curation or a web-search pass |
| P18 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | *Carried.* Named on five concept pages and in [[wiki/index-entities.md]] with no source behind them; every claim about them is second-hand | Human curation or a web-search pass |

## Structural work identified at this lint pass

| # | Task | Why |
|---|---|---|
| L5 | **Watch [[wiki/entities/medial-prefrontal-cortex.md]]** (**439 lines** — still the largest page in the wiki, by 40 lines over [[wiki/concepts/biologically-plausible-credit-assignment.md]]) | *Carried, +5 lines over 20 ingests.* An anatomical region carrying six primary sources. Growth has essentially stopped, which weakens the case for a split rather than strengthening it: the page is large because the region is, not because it is accreting. The likely seam is still by projection target rather than by source. **Downgrade to a watch item** unless wave 12's default-mode sources push it past 480 |
| L6 | **Decide whether the joint-embedding entity pages should stay separate** | *Carried.* Seventeen of them, several now deferring their central claim to [[wiki/concepts/representational-collapse.md]]. Not merge candidates — each carries distinct measured results — but the shared comparison table the schema asks for is missing from most |
| L8 | **The `Connections` block is the wiki's only navigational structure and it is unweighted** | *Carried.* **4,586 edges over 202 pages**, all of the same type, none marked as the *primary* relation. This pass added 486 and made the problem 12% worse — the four restructures each added reciprocal edges and not one of them could mark which edge was the point. Cheapest fix unchanged: mark one edge per page as primary |
| L10 | **Audit the remaining oversized gap cells** | *Carried for a fourth pass, re-measured, and **losing ground**.* All four watched cells grew since the last pass: **G42 8,709 → 14,729 (+69%)**, **G38 8,483 → 11,140 (+31%)**, **G34 12,687 → 14,506 (+14%)**, **G37 12,671 → 14,267 (+13%)**. The rule the task was written on still holds — *a gap cell that is enumerating candidates rather than naming one is a concept page* — but treating the cells one at a time has not kept pace with the accretion rate. **Superseded by `L13`**, which attacks the container instead of the contents; keep `L10` open only for the four named splits |
| L11 | **The wiki has no derivable count of itself** | *Carried, and it fired.* `S10` was written at the last pass and six published figures were still wrong at this one — see the status note. The remaining hole is that nothing *runs* the commands: they are written down in `S10` and consulted by whoever remembers. The fix is a `tools/wiki-stats.sh` emitting the six numbers, called at the top of every lint and quoted verbatim into the overview |
| L12 | **[[wiki/overview.md]]'s thematic body stops at wave 11 — write section 15 for wave 12** | **New, and the largest standing debt in the wiki.** Seventeen wave-12 ingests have concept and entity pages, gap rows and tension rows, and no synthesis anywhere. The wave is also the first whose subject is *organisation at the scale of a whole system* rather than a mechanism, so the section has to answer a question none of sections 1–14 asks: what does the wiki's architecture look like as a network — what are its nodes, what sets its edge weights, and which of `G81`–`G90` are properties of the graph rather than of any module. Fifth consecutive `S5` violation, and the first where the miss is a whole wave rather than a count |
| L13 | **Restructure both open-problem registries from one giant table into sectioned records** | **New, and the finding of this pass.** [[wiki/architectural-gaps.md]] (448 KB / 89 rows) and [[wiki/empirical-tensions.md]] (543 KB / 256 rows) are **991 KB — 27% of the wiki** — in two files CLAUDE.md requires to be updated at every ingest. The proposed shape, which is the wiki's own index/page pattern applied one level down: **(a)** a compact table at the top of each file — identifier, one-line statement, bare status token, and the pages that carry it, one line per row, so the whole registry is readable in a single screen and `grep`-able by status; **(b)** the long-form content moved below it into `### G42 — …` / `### T168 — …` sections with the same named fields the columns had, so an ingest edits one `sed` range instead of rewriting a 448 KB file; **(c)** the `Status` column reduced to its token, with the 108 KB of prose it currently holds moved into an explicit `Evidence` field of the section; **(d)** the 109 provenance blockquotes (139 KB, out of chronological order, duplicating the `INGEST` commit messages) deleted, per CLAUDE.md's *"Gitlog is changelog"* — this is the only part that removes content, and it is the part that needs the curator's assent before it happens. Do the tensions file first: it is larger, its rows are more uniform, and it has no provenance block to adjudicate |

*Closed at this pass:* **L9** — the 33,311-character coverage note, restructured by layer. **L1, L2, L4 and L7** — the four source-ordered pages, all carried since at least the 190-ingest pass and all resolved here:

| Was | Now | What the reorganisation exposed |
|---|---|---|
| `expected-free-energy.md`, 461 lines, six primary sources | 245 lines + **[[wiki/concepts/epistemic-value.md]]** (266) | Four incompatible quantities were circulating under one name (`E1`–`E4`), and **the one the ablations find carries the effect is excluded by both derivations** — visible only once the theory and the measurements were on separate pages |
| `cognitive-map.md`, 329 lines | 227 lines + **[[wiki/concepts/nonspatial-maps.md]]** (148) | The map's three elements transfer *unequally* off the plane: the code and the search generalise across fourteen domains and **anchoring has no non-spatial instance anywhere**, which is the real content of `G39` and was unstatable while the spatial and non-spatial evidence sat in one document |
| `analogical-mapping.md`, two source-shaped halves | reorganised on the retrieval → mapping → transfer spine, no content removed | The human results are *constraints on the stages*, not a second account of them — and the page's own headline finding (the cost is in retrieval) had been filed under the source rather than under the stage it prices |
| `biologically-plausible-credit-assignment.md`, one section per ingest | two parts: where the error signal comes from, and what a local rule is worth | The page had stated "the models are motifs" for six passes and was still filed by author. Sorted by mechanism, the four classes reduce to one containing frame plus three parameter settings of it |

**L3** remains closed as the split precedent.

## Standing

| # | Task | Why |
|---|---|---|
| S1 | Keep `Connections` bidirectional | 2 asymmetric edges over 20 ingests, one of them carried from the previous pass. The check costs nothing and is essentially always clean |
| S2 | Lint after ~every 20 ingests | Honoured four times running (19, 22, 20, 20). The predicted correlation holds: shorter intervals produce smaller content debt, and the two defects found here were both *reporting* errors rather than accreted content |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | The search index is what the next ingest reads first; a stale index hides existing pages and causes duplicates |
| S4 | **Check table rows by column count, not by grep** | *Fired again, and the naive form of the check is worse than useless.* At this pass it flagged 24 rows; **23 were false positives** — ordinary `\|` escapes inside code spans, which `awk -F'\|'` splits on regardless of the backslash. The 24th was real: `G80`'s restriction bar `(λ_I,j − λ_V,j)|_{t_k + d_ji}`, unescaped, shifting two cells. **The check must strip `\|` before counting**, or a 96% false-positive rate trains the reader to ignore it. Corrected form: `gsub(/\\\|/,""); n=gsub(/\|/,"")-1`. *Previously vindicated at 244:* For each table, take the pipe count of the separator row as the expected width and flag any row that differs. Four rows flagged, three of them ordinary `\|` escapes inside code spans and **one a missing cell** (T228's Status) that no escaping rule could see. Never grep for the symptom |
| S5 | **Stop writing the overview rule; check it.** `[ "$(grep -o '\*\*[0-9]* sources ingested\*\*' wiki/overview.md \| grep -o '[0-9]*')" = "$(grep -c '^- \[x\]' _work/ingest-queue.md)" ]` | Written at 171, violated at 190; rewritten at 190, violated at 212; rewritten at 212 as a *step of INGEST*, violated again at wave 10's close; **written as a one-line check at 244 and violated at 264, by 19 sources and a whole wave**. **Four written rules, five violations.** The diagnosis is now firm and it is not about discipline: the check was never wired to anything that runs. Writing it a fifth time is the failure mode. `L11` is the actual fix — put the comparison in `tools/wiki-stats.sh` and have `LINT` step 1 and `INGEST` both call it, so the count is *emitted* rather than remembered |
| S6 | **Write abbreviations the way the glossary lists them** | 6 added at this pass against 3 at the last. The rise is a property of wave 11: a hardware-adjacent literature names its baselines by acronym and the wiki inherits them (STBP, SNU, TSSL-BP, SHD, SSC) |
| S7 | **A concept that reaches ~50% of a host page is a page — and the host need not be a page** | Demonstrated seven times (equilibrium propagation at 128, representational collapse at 171, memory read/erase at 190, certification instruments at 212, and at this pass the layer table, the epistemic-value split and the non-spatial-maps split). This pass adds the level *below* it: a variable load-bearing on two pages and owned by neither is a **gap row**, not a page — that is `G80` |
| S8 | **Check that every cited `G`/`T` identifier has a row, and that no row shares an identifier** | Clean at this pass; `G79`'s four surviving mentions are all historical prose in this file and in the gaps provenance notes, which is correct, and `G80` was numbered to keep it that way. `I1`–`I24` and `F1`–`F14` are also clean |
| S9 | **A number quoted against a human is incomplete without its protocol** | *Extended at this pass, in the direction nobody expected.* The rule was written about human baselines. It applies to the wiki's numbers about **itself**: "212 sources" had no protocol either, and when one was supplied the figure moved by 12 |
| S10 | **Every count the wiki publishes about itself must name the command that produces it** | **Written at the last pass and violated at this one, in six places at once** — 245/264 sources, 98/115 concepts, 105/108 entities, 79/89 gaps, 237/256 tensions, and a wave marked in-progress that had closed. Naming the command is not enough; something has to run it (`L11`). The overview now carries the commands inline beside the figures. Originally: nd the general form of S9. Sources: `grep -c '^- \[x\]' _work/ingest-queue.md`. Pages: `ls wiki/concepts \| wc -l`, `ls wiki/entities \| wc -l`. Gaps and tensions: `grep -cE '^\| G[0-9]+ \|'`, `grep -cE '^\| T[0-9]+ \|'`. Edges: the `Connections` parse in the lint skill. A published figure with no command behind it is a claim, and this wiki has now shipped two false ones |
