# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

**Status (lint pass, 190 ingests).** Wave 8 (sequence architectures) is complete at 7 and wave 9 (analogy and program induction) is at 12 and running. This pass ran at **19 ingests** — the interval S2 asks for, and the first pass in the wiki's history to do so. The mechanical health held: no orphans, no unindexed pages, no broken links, no dead index entries, and **5** asymmetric `Connections` edges out of 3473 (0.26 per ingest, against 0.09 at the last pass over a longer interval — the rate rose but the absolute number is unchanged, so this is noise, not decay).

**Step 0 (source integrity).** The status page lists degraded-performance incidents for Opus 5 on 16–19 August 2026. All of waves 8 and 9 ran on 21 August, outside every listed window; the material written inside the window (waves 0–4) has already had its one known artefact repaired (`cef1cf4`, malformed table rows). Nothing was discarded.

Four things changed at this pass:

1. **The read/erase cluster got a page, and it was the wiki's largest homeless one.** [[wiki/concepts/memory-read-and-erase.md]] is split out of [[wiki/concepts/working-memory.md]] (L3, carried across four passes). The seam was the one priority-tasks had already named: the host page is about *carriers* — what holds an item, what holding costs, where the ceiling is — and three gaps (G48, G49, G60) are about the *interface* to a carrier. All three cited the carrier page for want of anywhere else. The new page states the six-component specification they jointly imply and names the one machine store supplying any of it.
2. **Two identifier defects, both invisible to every check the wiki had.** `G79` was cited in three places and has no row — the wave-8.6 driver-bank knob belongs to **G67**, and is now filed there. Two different tensions shared the identifier **T121**; the locality-vs-forgetting row is renumbered **T198**. Neither shows up as a broken link, an orphan or an index drift, which is why S8 is new below.
3. **[[wiki/overview.md]] was two waves stale again** — the identical failure S5 was written to prevent, at the identical interval. Brought to 190 with sections 11 and 12.
4. **The source-ordered defect is now appearing at birth, not by accretion.** [[wiki/concepts/analogical-mapping.md]] was created inside this interval and already carries a 130-line "The human process, priced (Holyoak 2012)" section with nine numbered subsections. L1–L4 describe pages that *became* source-ordered over many ingests; this one started that way, which changes what the fix is (see L7).

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P1 | **Ingest the ARC benchmark set already in `raw/`** — the `arcprize-*` files (ARC-AGI-1/2/3, the Kaggle rules and overview, the ARC-AGI-3 paper), plus `chollet-2024/2025-arc-prize-report.md` and `chollet-2025-arc-agi-2.md` | *Carried, and wave 9 raised the cost of not doing it.* [[wiki/entities/arc-agi.md]] still describes the 2019 original while three wave-9 sources ([[wiki/entities/arc-vsa-solver.md]], [[wiki/entities/corethink-compositional-reasoner.md]], [[wiki/entities/ilp-arc-synthesizer.md]]) report scores **against ARC-AGI-2 and its variants**, so the wiki now holds numbers whose instrument it does not describe | Nothing. Ten files are present and unread |
| P2 | **Split the three remaining source-ordered pages** (L1, L2, L4) | L3 is closed at this pass and is the second worked example: the seam was already named in this file, the material was already written, and the operation cost one page plus 13 reciprocal edges. The remaining three have the same property | — |
| P3 | **Build the write-mask experiment (G52), and build G64 with it** | Still the cheapest concrete proposal the wiki has: a third module emitting a binary licence on a learned interface's plasticity, scored on continual-learning interference, with `δ_j = snr_j · δ` as the credit-side twin ([[wiki/entities/hippocampal-prefrontal-channel.md]], [[wiki/concepts/reward-prediction-error.md]]) | — |
| P4 | **Run the `g`/`x` objective search, with the collapse filter applied first** | G30 remains the wiki's deepest gap. A candidate must separate a real structural code from a constant `g`, which satisfies path-consistency exactly ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P5 | Write the **controller/interface** synthesis | *Reduced in scope by this pass.* The interface half now has a home for three of its ten rows ([[wiki/concepts/memory-read-and-erase.md]]); what remains is to state the missing-controller shape (G15, G33, G37, G38) and the residual interface rows (G52–G54, G59) as one problem with the runtime-computable observable each already has | — |
| P6 | **Re-price G37 against the retrieval result** | Wave 9 says the wiki had the cost of analogy backwards — mapping is domain-blind at ~86% transfer once cued, and the entire distance penalty is in retrieval (88% vs 12%). Every amortisation entry in the wiki is written as an approximation to the *mapper*. MAC/FAC supplies the first candidate of the right shape and the right certificate type (one-sided error, derived from the exact algorithm's own workload), and nothing in the wiki has been re-scored against it | Ready to attempt — the material is ingested |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P7 | **Run the two experiments wave 7 left specified and unrun.** (a) Probe and ablate OthelloGPT's candidate end-game features and show end-game accuracy falls (T157/T158). (b) Run LeJEPA's rescaled-loss checkpoint ranking against a **patch-token** probe on a long run | Both are named inside ingested sources, need no new material, and each attacks a `LIVE` tension with no procedure ([[wiki/entities/othellogpt.md]], [[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P8 | Score architectures against the six hardness sources, per the placeholder in [[wiki/concepts/latent-graph-discovery.md]] | 80 entity pages and the comparison instrument is still empty. Wave 9 supplies a fifth column the table now needs: **demonstration-fit vs. query-solve**, which three independent protocols have now shown to separate | Ready to attempt |
| P9 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | Named as the obvious next model by more than one source and never done; unmoved for five lint passes | — |
| P10 | **Run the `γ_effective` readout** — fit a discount kernel to a trained agent's choices, fit a second to a unit's activations, test covariance *and* zero mean offset | T141 is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error on a quantity with no stationary value function | Ready to attempt |
| P11 | **Instrument one existing store with a read log** | The cheapest experiment the new page names, and it needs no new architecture: log when and at what a key-value or attention store was read, then score the schedule separately from the contents. The prefrontal error pattern says the two dissociate ([[wiki/concepts/memory-read-and-erase.md]], G49) | Ready to attempt |
| P12 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | G23: every machine prior applies unconditionally while every core system carries an entry test, and the gate would also supply G12's router. Wave 9 sharpened the gap — an overlearned schema silently re-parses a formally isomorphic problem and takes transfer from 89% to 0% | Human curation or a web-search pass |
| P13 | Acquire Liao et al. 2022 — the inhibitory-plasticity model of the replay filter | [[wiki/concepts/offline-replay.md]] asserts inhibitory plasticity as the filter's substrate on modelling grounds; T30 cannot be adjudicated from a review | Human curation |
| P14 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) and for ObjectNet / ImageNet-C | The only listed instruments where the intended rule is known by construction (G17). [[wiki/entities/pcfg-set.md]] now shows what such an instrument buys, so the case is demonstrated rather than argued | Human curation or a web-search pass |
| P15 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | Named on five concept pages and in [[wiki/index-entities.md]] with no source behind them; every claim about them is second-hand | Human curation or a web-search pass |

## Structural work identified at this lint pass

| # | Task | Why |
|---|---|---|
| L1 | **Restructure [[wiki/concepts/biologically-plausible-credit-assignment.md]] by mechanism, not by source** | *Carried from the 67-, 125-, 128- and 171-ingest passes.* 365 lines, still one section per ingest. The page states the organising principle it does not follow: "the models are motifs" |
| L2 | **Split [[wiki/concepts/expected-free-energy.md]]** (460 lines — the largest concept page) | *Carried.* Six primary sources, each with its own "what this changes for the wiki" subsection. The seam: the decomposition and its convex-MDP reading versus the four ablation literatures testing whether the epistemic term pays |
| L4 | **Split [[wiki/concepts/cognitive-map.md]]** (329 lines) | *Carried.* Three distinct operations (spatial coding, anchoring, route planning) plus a "Beyond space" section that is the wiki's main statement on non-spatial maps and is large enough to stand alone |
| L5 | **Watch [[wiki/entities/medial-prefrontal-cortex.md]]** (433 lines — the largest page in the wiki, and an *entity*) | *Carried.* An anatomical region carrying six primary sources, on the trajectory L1–L4 describe. The likely seam is by projection target rather than by source, which is what the page's own load-bearing result argues for |
| L6 | **Decide whether the JEPA entity pages should stay separate** | *Carried.* Twelve joint-embedding entity pages, several now deferring their central claim to [[wiki/concepts/representational-collapse.md]]. Not merge candidates — each carries distinct measured results — but the comparison table the schema asks for is missing from most and would be one shared table |
| L7 | **Restructure [[wiki/concepts/analogical-mapping.md]] before it accretes further** | New at this pass, and a different case from L1–L4: the page is 316 lines *at birth*, with the model (Slipchenko & Rachkovskij) and the human data (Holyoak) as two source-shaped halves rather than as one account of retrieval → mapping → transfer. Fixing it now costs a rewrite of one page; fixing it after wave 9 finishes will cost what L1 costs |
| L8 | **The `Connections` block is now the wiki's only navigational structure and it is unweighted** | New at this pass. 3473 edges over 161 pages, all of the same type, none marked as the *primary* relation. The indexes list pages, the gap table lists problems, and nothing states which two or three edges a reader should follow first from a given page. Cheapest fix: mark one edge per page as primary |

*Closed at this pass:* **L3** (the working-memory split), the `G79`/`G67` identifier defect, and the duplicate `T121`.

## Standing

| # | Task | Why |
|---|---|---|
| S1 | Keep `Connections` bidirectional | 5 asymmetric edges over 19 ingests. The absolute number has been 4–5 at each of the last three passes regardless of interval length, which suggests a fixed per-pass cost rather than a rate; the check stays because it is free |
| S2 | Lint after ~every 20 ingests | Honoured for the first time at this pass (19), and the content debt was correspondingly smaller — one split rather than one split plus a two-wave overview rewrite plus a promotion |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | The search index is what the next ingest reads first; a stale index hides existing pages and causes duplicates |
| S4 | **Escape `\|` inside code spans in table cells** | No occurrences at this pass, against five at the last — the first clean interval |
| S5 | **Refresh [[wiki/overview.md]] at the wave boundary, not at lint time** | Written at the last pass and immediately violated: two wave boundaries passed inside this interval and neither triggered a refresh. The schema says every ~10 ingests; the observed behaviour is still every ~20, i.e. only when a lint pass forces it |
| S6 | **Write abbreviations the way the glossary lists them** | 13 more added at this pass (ILP, LSSL, SRN, DORA, LPN, LAPS, AUC, SMDP, SPRT/MSPRT, BIP-ALM/LIMP, NEF, PSD, QK). The analogy and program-synthesis vocabulary arrived faster than the glossary did, exactly as the machine-learning vocabulary did in wave 7 |
| S7 | **A concept that reaches ~50% of a host page is a page** | Demonstrated three times now (equilibrium propagation at 128, representational collapse at 171, memory read/erase at 190). The early test is not page length — it is *a section with primary sources of its own* |
| S8 | **Check that every cited `G`/`T` identifier has a row, and that no row shares an identifier** | New at this pass, which found one of each. Neither defect is visible to the orphan, index-drift or broken-link checks, and both silently break the gap table's only function — being the thing a page can point at. Two greps, run at lint time |
