# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

**Status (lint pass, 171 ingests).** Wave 6 (normative theories) is complete at 22 and wave 7 (world models / self-supervised) is complete at 28; `priority-ingest.md` has moved on to wave 8 (sequence architectures). 133 of the 291 files in `raw/` are still unread. This pass ran at **43 ingests** — twice the interval the standing item asks for — and the mechanical health held anyway: no orphans, no unindexed pages, no broken links, and **4** asymmetric `Connections` edges out of 2292 (against 5 at the last pass over a much shorter interval). The audit therefore spent its budget on content debt rather than on repair.

Four things changed at this pass:

1. **One implicit concept promoted, and it was the largest one the wiki has had.** [[wiki/concepts/representational-collapse.md]] is split out of [[wiki/concepts/energy-based-models.md]]. It met the strict test twice over: ~60% of its host page, 374 mentions across 76 pages, six structurally different anti-collapse loci with no page stating them side by side, one gap (G34) and five tensions (T164, T166, T167, T168, T171) hanging off it. The host page is back to 145 lines and is about energy again.
2. **The consequence for the gap table is a re-ordering, not a new row.** G34 is not a neighbour of G30 — it is a **filter G30 must pass first**, because a constant structural code is path-consistent trivially. That sentence is now in the thesis, in the gap table and on the new page.
3. **[[wiki/overview.md]] was two waves stale** (125 sources, "wave 6, 4 of 22, in progress") and has been brought to 171 with two new understanding sections. Per the schema this should have happened three times inside the interval; see S5.
4. **The carried structural debt did not move and is now the dominant item.** L1–L4 below have survived four lint passes. Three of them are the same defect — a page organised by source rather than by mechanism — and the wave-7 ingests made two of them larger.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P1 | **Ingest the wave 10 benchmark set already in `raw/`** — the `arcprize-*` files (ARC-AGI-1/2/3, the Kaggle rules and overview, the ARC-AGI-3 paper), plus `chollet-2024/2025-arc-prize-report.md` and `chollet-2025-arc-agi-2.md` | [[wiki/entities/arc-agi.md]] still describes the 2019 original only, while the benchmark has been revised **twice** specifically to remove shortcuts the first version admitted — G17's revision treadmill happening to the wiki's own instrument, and the wiki is carrying the stale version of its most-cited benchmark. Promoted from P5 | Nothing. Ten files are present and unread |
| P2 | **Split the four source-ordered pages** (L1–L4 below) | Carried across four passes and now the largest single block of debt. The collapse split at this pass is the worked example: the seam was already visible, the material was already written, and the operation cost one page plus 27 reciprocal edges | — |
| P3 | **Build the write-mask experiment (G52), and build G64 with it** | Still the cheapest concrete proposal the wiki has: a third module emitting a binary licence on a learned interface's plasticity, scored on continual-learning interference, with `δ_j = snr_j · δ` as the credit-side twin — one experiment, two readouts ([[wiki/entities/hippocampal-prefrontal-channel.md]], [[wiki/concepts/reward-prediction-error.md]]) | — |
| P4 | **Run the `g`/`x` objective search — now with the collapse filter applied first** | G30 remains the wiki's deepest gap, and this pass sharpened what a candidate has to survive: constrained code length, a path-commutativity residual, supervised bidirectional retrieval, `J(θ)`, the precision ratio `Σ^(0)`, the convex term of expected free energy — **each scored against a constant `g`, which satisfies path-consistency exactly**. Any candidate that cannot separate those two is not a candidate ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P5 | Write the **controller** synthesis — one page or section stating the missing-controller shape (G15, G33, G37, G38) *and* the missing-interface shape (G48, G49, G52–G54, G59, G60) as one problem, with the runtime-computable observable each already has | The gap table records ten instances of two shapes separately. Every one has a label-free observable already identified and nothing descends any of them | — |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P6 | **Run the two experiments wave 7 left specified and unrun.** (a) Probe and ablate OthelloGPT's candidate end-game features (`Is-Surrounded-By-Mine`, `Is-Border`) and show end-game accuracy falls — the successor to the closed causal-intervention task, and the only route to T157/T158. (b) Run LeJEPA's rescaled-loss checkpoint ranking against a **patch-token** probe on a long run, i.e. point the one label-free monitor at the one failure it has never been calibrated on | Both are named inside ingested sources, need no new material, and each attacks a `LIVE` tension that currently has no procedure ([[wiki/entities/othellogpt.md]], [[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P7 | Score architectures against the six hardness sources, per the placeholder in [[wiki/concepts/latent-graph-discovery.md]] | 66 entity pages and the comparison instrument is still empty. It needs four columns: i.i.d., o.o.d. (G17), *probe*, and *trajectory* (G46) — and wave 7 supplied a counterexample against the probe column, which is a reason to build the table, not to defer it | Ready to attempt |
| P8 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | Named as the obvious next model by more than one source and never done. The wiki's cheapest concrete architectural proposal, needing no new ingest — and unmoved for four lint passes | — |
| P9 | **Run the `γ_effective` readout** — fit a discount kernel to a trained agent's choices, fit a second to a unit's activations, and test both covariance *and* zero mean offset | T141 is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error term on a quantity with no stationary value function and the wiki's rollout scoring inherits the problem. Kable & Glimcher's two-part test is directly runnable on an artificial agent and nobody has run it | Ready to attempt |
| P10 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | G23: every machine prior applies unconditionally while every core system carries an entry test, and the gate would also supply G12's router | Human curation or a web-search pass |
| P11 | Acquire Liao et al. 2022 — the inhibitory-plasticity model of the replay filter | [[wiki/concepts/offline-replay.md]] asserts inhibitory plasticity as the filter's substrate on modelling grounds; T30 cannot be adjudicated from a review | Human curation |
| P12 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) and for ObjectNet / ImageNet-C | The only listed instruments where the intended rule is known by construction, so they are what turns "did this architecture recover structure?" into a measurement (G17) | Human curation or a web-search pass |
| P13 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | Named on five concept pages and in [[wiki/index-entities.md]] with no source behind them; every claim about them is second-hand | Human curation or a web-search pass |

## Structural work identified at this lint pass

| # | Task | Why |
|---|---|---|
| L1 | **Restructure [[wiki/concepts/biologically-plausible-credit-assignment.md]] by mechanism, not by source** | *Carried from the 67-, 125- and 128-ingest passes.* 364 lines, still one section per ingest. The page itself states the organising principle it does not follow: "the models are motifs" |
| L2 | **Split [[wiki/concepts/expected-free-energy.md]]** (459 lines — the largest concept page) | *Carried, and it grew by wave 6.* Six primary sources, each with its own "what this changes for the wiki" subsection, which is exactly the source-ordered structure L1 exists to remove. The seam: the decomposition and its convex-MDP reading versus the four ablation literatures testing whether the epistemic term pays |
| L3 | **Split [[wiki/concepts/working-memory.md]]** (397 lines) | *Carried across three passes.* The seam is unchanged: "two designs for holding information over time" → "maintenance has a price" is the **store**; "reading is a separate operation" → "removal is at least three operations" is the **read/erase protocol**, which is its own gap cluster (G48, G49, G60) and still has no home |
| L4 | **Split [[wiki/concepts/cognitive-map.md]]** (329 lines) | *Carried across three passes.* Three distinct operations (spatial coding, anchoring, route planning) plus a "Beyond space" section that is the wiki's main statement on non-spatial maps and is large enough to stand alone |
| L5 | **Watch [[wiki/entities/medial-prefrontal-cortex.md]]** (433 lines — the largest page in the wiki, and an *entity*) | New at this pass. An anatomical region carrying six primary sources is on the same trajectory L1–L4 describe, and entity pages have no precedent here for splitting. The likely seam is by projection target rather than by source, which is what the page's own load-bearing result argues for |
| L6 | **Decide whether the JEPA entity pages should stay separate** | New at this pass. Twelve joint-embedding entity pages (BYOL, Barlow Twins, VICReg, LeWM, LeJEPA, I-JEPA, V-JEPA 2, VL-JEPA, HiT-JEPA, H-JEPA, DINOv2, DINOv3), several of which now defer their central claim to [[wiki/concepts/representational-collapse.md]]. They are not merge candidates — each carries distinct measured results — but the *comparison table* the schema asks entity pages to carry is missing from most of them and would be one shared table |

*Closed at this pass:* the JEPA-family ingest (P1 across three passes — all five named files are in), and the representational-collapse promotion (new, and the largest implicit concept the wiki has had).

## Standing

| # | Task | Why |
|---|---|---|
| S1 | Keep `Connections` bidirectional | 4 asymmetric edges over 43 ingests — ~0.09 per ingest, down from ~1.7 at the last two passes. The ingest skill has evidently improved on this; the check stays because it is free |
| S2 | Lint after ~every 20 ingests | This pass ran at 43. Nothing mechanical broke, but the overview went two waves stale and one implicit concept grew to 60% of its host page — the failure mode of a long interval is **content** debt, not link rot, which the interval rule should be read as protecting against |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | The search index is what the next ingest reads first; a stale index hides existing pages and causes duplicates |
| S4 | **Escape `\|` inside code spans in table cells** | Markdown splits a table cell on a raw `\|` even inside backticks. Five occurrences were mis-rendering at this pass — a tension row, two conditional entropies on [[wiki/concepts/divergence-objectives.md]] and [[wiki/concepts/cross-modal-grounding.md]], and `\|θ\|` on [[wiki/entities/kan-ode.md]] — all fixed. Same rate as the last pass, so the ingest skill is not catching this one |
| S5 | **Refresh [[wiki/overview.md]] at the wave boundary, not at lint time** | Upgraded from S5's earlier form. The schema says every ~10 ingests; the observed behaviour is every ~45, i.e. only when a lint pass forces it. A wave boundary is a natural, self-announcing trigger and there were two of them inside this interval |
| S6 | **Write abbreviations the way the glossary lists them** | 15 more added at this pass (OOD, SGD, BN, MAE, EMA, CHMM, SIGReg, SEC, BLA, PFv+o, OTIB, SSv2, HBToM, plus the IntPhys/InfLevel/GRASP not-an-abbreviation note). The wave-7 machine-learning vocabulary arrived faster than the glossary did |
| S7 | **A concept that reaches ~50% of a host page is a page** | New at this pass, and now demonstrated twice (equilibrium propagation at 128, representational collapse at 171). The test that catches it early is not page length — it is *a section with primary sources of its own*, which both had for several ingests before the split |
