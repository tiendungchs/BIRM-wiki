# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

**Status (lint pass, 125 ingests).** Waves 0–5 are complete (`_work/ingest-queue.md`); wave 6 (normative theories) is 4 of 22 in, with a Kerjan et al. 2026 ingest currently uncommitted in the working tree. 175 of the 291 files in `raw/` are still unread. Four things changed the shape of this list at this pass:

1. **The gap table's centre of gravity moved from codes to interfaces.** Eleven of the fourteen gaps opened since the last lint (G47–G60) are about a read, an edge, or a level boundary. Several of them are implementable in an afternoon and none has been tried — which is the first time in this wiki's history that the cheapest open items are *building* tasks rather than acquisition tasks.
2. **The wiki lost its behavioural certificate.** T105 shows a model-free agent reproducing the canonical model-based signature, so every "the model plans" claim in the gap table now needs an algorithmic check rather than a behavioural one. This raises the cost of every future ingest that reports a behavioural result and raises the value of G46's trajectory instrument.
3. **The machine side has fallen further behind.** Waves 2–5 added 102 sources of neuroscience against a machine-model inventory that is still mostly wave 0. Waves 7 (JEPA / self-supervised, 26 sources), 8 (sequence architectures, 8), 9 (symbolic layer, 16) and 10 (benchmarks, 41) are entirely unread, and all of them sit in `raw/`.
4. **The last pass's structural work went undone for 58 ingests.** L1, L2 and L3 below are re-stated from the 67-ingest pass. Only the largest of them was done at this pass (the medial-prefrontal split), and the two page-level restructurings are unchanged.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P1 | **Commit the in-flight Kerjan et al. 2026 ingest and tick it in `_work/ingest-queue.md`** | `wiki/concepts/energy-based-models.md` and `wiki/concepts/predictive-coding-free-energy.md` carry a full treatment of it in the working tree, and `raw/kerjan-2026-predictive-coding-imagenet-eqprop.md` is untracked — so by the wiki's own convention (uncommitted `raw/` files are not ingested) the largest scaling result in the credit-assignment story is currently unfiled | — |
| P2 | Finish wave 6 — the active-inference block (`friston-2009-free-energy-principle-rough-guide.md`, `cooper-2026-efe-belief-dependent-utility.md`, `nuijten-2026-*`, `champion-2023-deconstructing-deep-active-inference.md`), then the rl-planning block (`gerfen-2011`, `helie-2013`, `doya-2002`) | [[wiki/concepts/expected-free-energy.md]] is 7 connections deep and rests on one source; it is the wiki's best current candidate for the empty objective slot (G30) and it needs the rest of its own literature. The rl-planning block is also what would create the two pages the lint found implicit — a basal-ganglia entity page (referenced on 10 pages, no page) and a reward-prediction-error concept page (dopamine appears on 30 pages, and PVLV contests the standard reading with nothing to arbitrate on) | Wave order |
| P3 | **Build the write-mask experiment (G52)** — two modules with a learned interface, a third module emitting a binary licence on that interface's plasticity, scored on continual-learning interference | The cheapest concrete proposal the wiki has ever had: every other row of the edge-state table costs an architecture change and this one costs a mask. [[wiki/entities/hippocampal-prefrontal-channel.md]] specifies it end to end and nothing in the literature has run it | — |
| P4 | Ingest the JEPA family — `raw/bardes-2022-vicreg.md`, `raw/assran-2023-i-jepa.md`, `raw/assran-2025-v-jepa-2.md`, `raw/balestriero-2025-lejepa.md`, `raw/garrido-2025-intuitive-physics-v-jepa.md` (wave 7, all present) | [[wiki/entities/h-jepa.md]] is the wiki's most complete design and carries **zero** evidence; JEPA is referenced on 22 pages and has no concept page for the objective family at all. This is the largest evidence deficit on any page, and it was P2 at the last lint pass and did not move | Wave order — worth jumping the queue |
| P5 | Write the **controller** synthesis — one page or section stating the missing-controller shape (G15, G33, G37, G38) *and* the missing-interface shape (G48, G49, G52–G54, G59, G60) as one problem, with the runtime-computable observable each already has | The gap table records ten instances of two shapes separately. Every one has a label-free observable already identified (adaptation gain vs. input strength; the lure bias score; posterior responsibility; completion error; output entropy over recent trials) and nothing descends any of them | — |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P6 | Ingest the wave 10 benchmark set already in `raw/` — the `arcprize-*` files (ARC-AGI-1/2/3, the Kaggle rules, the ARC-AGI-3 paper) | [[wiki/entities/arc-agi.md]] describes the 2019 original only, while the benchmark has been revised twice specifically to remove shortcuts the first version admitted — G17's revision treadmill happening to the wiki's own instrument | Wave order; the files are present |
| P7 | Acquire and ingest a source with a **causal intervention** on a probed representation — Li et al. 2022 / Nanda et al. 2023 on Othello-GPT are the named precedents | [[wiki/concepts/representation-probing.md]] still arrives with its acceptance test unmet, and T105 has now shown the *behavioural* alternative admits a counterexample, so the intervention is the only surviving route | Human curation or a web-search pass — nothing in `raw/` covers it |
| P8 | Run the `g`/`x` objective search against the five candidates now on the table — constrained code length, a path-commutativity residual, supervised bidirectional retrieval, the free-energy difference `J(θ)`, the precision ratio `Σ^(0)` | G30, the wiki's deepest gap. It was "candidate terms rather than none" at the last pass and the list has since doubled; what has not happened is any of them being *run* against path-consistency | Ready to attempt — a writing and reasoning task, not an acquisition one |
| P9 | Score architectures against the six hardness sources, per the placeholder in [[wiki/concepts/latent-graph-discovery.md]] | The wiki now has 43 entity pages and the comparison instrument is still empty. It needs four columns, not two: i.i.d., o.o.d. (G17), *probe*, and — new at this pass — *trajectory* (G46), which is the only one of the four with no counterexample against it | Ready to attempt |
| P10 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | Named as the obvious next model by more than one source and never done. The wiki's cheapest concrete architectural proposal, needing no new ingest — and unmoved for two lint passes | — |
| P11 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | G23: every machine prior applies unconditionally while every core system carries an entry test, and the gate would also supply G12's router. Still nothing in `raw/` addresses it | Human curation or a web-search pass |
| P12 | Acquire Liao et al. 2022 — the inhibitory-plasticity model of the replay filter | [[wiki/concepts/offline-replay.md]] asserts inhibitory plasticity as the filter's substrate on modelling grounds; T30 cannot be adjudicated from a review, and Sun et al. 2023 answers a different question (how much to consolidate, not what to replay) | Human curation |
| P13 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) and for ObjectNet / ImageNet-C | The only listed instruments where the intended rule is known by construction, so they are what turns "did this architecture recover structure?" into a measurement (G17) | Human curation or a web-search pass |
| P14 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | Named on five concept pages and in [[wiki/index-entities.md]] with no source behind them; every claim about them is second-hand | Human curation or a web-search pass |

## Structural work identified at this lint pass

| # | Task | Why |
|---|---|---|
| L1 | **Restructure [[wiki/concepts/biologically-plausible-credit-assignment.md]] by mechanism, not by source** | *Carried from the 67-ingest pass, and worse: 298 → 358 lines, still one section per ingest (Whittington & Bogacz, Millidge, Bengio, Shervani-Tabar, Bartunov). The reorganising axis is stated inside the page itself — "the models are motifs"* |
| L2 | **Split [[wiki/concepts/cognitive-map.md]]** (68 KB) | *Carried from the 67-ingest pass.* It carries three distinct operations (spatial coding, anchoring, route planning) plus a "Beyond space" section that is the wiki's main statement on non-spatial maps and is large enough to stand alone. It is no longer the largest page — [[wiki/entities/medial-prefrontal-cortex.md]] was, and was split at this pass — but it is the largest *concept* page |
| L3 | **Promote the equilibrium-propagation section out of [[wiki/concepts/energy-based-models.md]]** | *Carried, and the deferral reason has now expired.* It is ~45% of that page, has three primary sources including the ImageNet result, its own tension (T69) plus the nudge-strength disagreement the in-flight Kerjan ingest opens, and is a *learning rule* inside a page about a model class. It was deferred at the last pass for being referenced from only three pages; it is now the substrate of the wiki's best answer to G30 |
| L4 | **Split [[wiki/concepts/working-memory.md]]** (60 KB, 374 lines) | The next page to hit the size at which mPFC and cognitive-map became unreadable, and it has a natural seam: "two designs for holding information over time" through "maintenance has a price" is the *store*, while "reading is a separate operation" through "removal is at least three operations" is the *read/erase protocol* — which is now its own gap cluster (G48, G49, G60) and would give that cluster a home |
| L5 | **Give theta its own page, or fold it into [[wiki/concepts/inter-areal-synchrony.md]] explicitly** | Theta appears 150 times across 24 pages with no page of its own; the closest home carries 23 of those mentions and is one of the thinner concept pages (116 lines). Either it is a concept or it is a measurement detail, and the wiki currently treats it as both |
| L6 | **Refresh the `From` column of [[wiki/architectural-gaps.md]] for the wave-6 rows** | Provenance notes now run through wave 6.3, which is current — but G52's `Best current answer` and `From` cells were empty until this pass, and the same should be checked for each new row at ingest time rather than at lint time |

## Standing

| # | Task | Why |
|---|---|---|
| S1 | Keep `Connections` bidirectional | A one-way link is a maintenance defect. **49 asymmetric edges** had accumulated across 58 ingests and were repaired at this pass; that is roughly one per ingest, so the ingest skill is creating them faster than lint catches them |
| S2 | Lint after ~every 20 ingests | This pass ran at 58 ingests since the last one, and the cost showed: `overview.md` was stale by two entire waves, `priority-tasks.md` still described wave 4 as in progress, and three markdown tables were silently mis-rendering |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | The search index is what the next ingest reads first; a stale index hides existing pages and causes duplicates |
| S4 | **Do not keep a per-ingest log in `overview.md`** | The format broke at ~11 sources and left the page unmaintained for 56 more. Per-source detail belongs in the git log and in the gap table's `From` column; the overview is thematic |
| S5 | **Escape `\|` inside code spans in table cells** | Markdown table cells split on a raw `|` even inside backticks. 21 cells across 14 files were mis-rendering at this pass — mostly absolute-value bars (`\|s_u\|`) and conditional probabilities |
