# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

**Status (lint pass, 67 ingests).** Wave 0 (foundations), wave 1 (latent graph), wave 2 (hippocampal-entorhinal) and wave 3 (circuit substrate) are complete; wave 4 (learning rules) is 9 sources in, with `lindsey-2024-recall-gated-plasticity.md` next in `_work/ingest-queue.md`. The wiki is no longer survey-bound — the concept skeleton now rests on primary sources almost everywhere, and the binding constraint has moved. Three things changed the shape of this list at this pass:

1. **The gaps cluster into one shape.** G15, G37, G38 and G33 are all *characterised mechanism + exogenous free parameter*. The wiki's next unit of progress is a controller, not another mechanism.
2. **The objective slot (G30) is the wiki's deepest hole** and it now has candidate terms rather than none — orthogonalization under soft winner-take-all (T53), supervised bidirectional retrieval `x → g` / `g → g`, the Helmholtz free-energy difference `J(θ)` that equilibrium propagation ascends exactly. None has been run against path-consistency.
3. **The machine side has fallen behind the biology side.** Waves 2–3 added 44 sources of neuroscience against a machine-model inventory that is still mostly wave 0. Nothing in the wiki carries a JEPA empirical result, a causal intervention on a probe, or a modern program-synthesis system, and all three sit in `raw/` unread.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P1 | Continue wave 4 in `_work/ingest-queue.md` — next `sadtler-2014-neural-constraints-learning.md`, `bartunov-2018-biologically-motivated-dl-scalability.md`, `golub-2018-neural-reassociation-learning.md` | Recall-gated plasticity and BTSP-as-content-addressable-memory are both ingested; neither closed G19, and the second sharpened it — an uninformative write gate is what buys uniform memory load, so selectivity and non-interference are in competition ([[wiki/entities/btsp-cam.md]], [[wiki/empirical-tensions.md]] T73). Bi & Poo 1998 is now ingested: the measured curve arrives with three dependencies the `Δw(Δt)` equation omits (current weight, postsynaptic cell type, causal responsibility for the spike), one of which contradicts the multiplicative window [[wiki/concepts/temporal-coding.md]] relies on ([[wiki/empirical-tensions.md]] T76). The three remaining plasticity sources are all *constraint* results — what a real circuit will and will not learn — which is the side of the family the wiki has least of | — |
| P2 | Ingest the JEPA family — `raw/bardes-2022-vicreg.md`, `raw/assran-2023-i-jepa.md`, `raw/assran-2025-v-jepa-2.md`, `raw/balestriero-2025-lejepa.md` (wave 7, all present in `raw/`) | [[wiki/entities/h-jepa.md]] is the wiki's most complete design and carries **zero** evidence: that non-contrastive training scales, that stacking produces an abstraction hierarchy, that planning works on the result, are all currently the author's expectation. This is the largest evidence deficit on any page | Wave order — worth jumping the queue |
| P3 | Write the **controller** synthesis — a page or a section that states the four instances of the missing-controller shape (G15, G33, G37, G38) as one problem, with the observables each one already has | The gap table records them separately and the lint pass found them to be the same shape. Every one of them has a *runtime-computable, label-free* observable already identified (adaptation gain vs. input strength; the lure bias score; posterior responsibility; completion error) and nothing descends any of them | — |
| P4 | Acquire and ingest a source with a **causal intervention** on a probed representation — Li et al. 2022 / Nanda et al. 2023 on Othello-GPT are the named precedents | [[wiki/concepts/representation-probing.md]] still arrives with its acceptance test unmet: the maze work stops at correlation, so "the model uses the world model it contains" is unestablished, and T25 turns on exactly this evidence | Human curation or a web-search pass — nothing in `raw/` covers it |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P5 | Run the `g`/`x` objective search: find a source proposing an objective sensitive to **path-consistency**, or argue none exists | G30, the wiki's deepest gap. The three shapes now on the table — constrained code length, a commutativity residual computable from trajectories alone, and the free-energy difference `J(θ)` — are all wiki-side brainstorms with no source behind them | A source on structure-sensitive or disentanglement objectives |
| P6 | Score architectures against the six hardness sources, per the placeholder in [[wiki/concepts/latent-graph-discovery.md]] | The wiki now has 30 entity pages and the comparison instrument is still empty. It needs three columns, not two: i.i.d., o.o.d. (G17 — an i.i.d. result may not enter), and *probe* (which scores the graph estimate separately from the navigation over it) | Ready to attempt — this is now a writing task, not an acquisition one |
| P7 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools (fast, local, no transfer) against path-integrated position (transfers, slow to acquire) | Named as the obvious next model by more than one source and never done. It is the wiki's cheapest concrete architectural proposal and needs no new ingest | — |
| P8 | Ingest the wave 10 benchmark set already in `raw/` — the `arcprize-*` files (ARC-AGI-1/2/3, the Kaggle competition rules, the ARC-AGI-3 paper) | [[wiki/entities/arc-agi.md]] describes the 2019 original only, while the benchmark has been revised twice specifically to remove the shortcuts the first version was found to admit — which is G17's revision treadmill happening to the wiki's own instrument | Wave order; the files are present |
| P9 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | G23: every machine prior applies unconditionally while every core system carries an entry test, and the gate would also supply G12's router. Still nothing in `raw/` addresses it | Human curation or a web-search pass |
| P10 | Acquire Liao et al. 2022 — the inhibitory-plasticity model of the replay filter (**Sun et al. 2023 is done**, [[wiki/concepts/generalization-optimized-consolidation.md]]) | [[wiki/concepts/offline-replay.md]] asserts inhibitory plasticity as the filter's substrate on modelling grounds, with rival models reproducing replay without it; T30 cannot be adjudicated from a review. Sun et al. 2023 is now ingested and answers a *different* question than T30 asks — how much to consolidate, not what to replay — so T30 still needs Liao et al. 2022 for the substrate claim, and the new disagreement it created (recurrence vs. predictability as the consolidation criterion) is tracked as T81 | Human curation for Liao |
| P11 | Acquire the primary sources behind [[wiki/concepts/synaptic-plasticity.md]]'s BTSP section — Bittner et al. 2017, Milstein et al. 2021, Gonzalez et al. 2023 | The single-shot rule rests on one review, and the kernel shape sets what counts as "one event" — a quantity no page states | Partly unblocked by `wu-2025-btsp-content-addressable-memory.md` (P1) |
| P12 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) and for ObjectNet / ImageNet-C | The only listed instruments where the intended rule is known by construction, so they are what turns "did this architecture recover structure?" into a measurement (G17) | Human curation or a web-search pass |
| P13 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | Named on five concept pages and in [[wiki/index-entities.md]] with no source behind them; every claim about them is second-hand | Human curation or a web-search pass |

## Structural work identified at this lint pass

| # | Task | Why |
|---|---|---|
| L1 | **Restructure [[wiki/concepts/biologically-plausible-credit-assignment.md]] by mechanism, not by source** | At 298 lines it has grown one section per ingest (Whittington & Bogacz, Millidge, Bengio, Shervani-Tabar), which is the shape a page takes just before it stops being readable. The reorganising axis is already stated inside it — "the models are motifs" |
| L2 | **Split [[wiki/concepts/cognitive-map.md]]** (66 KB, the wiki's largest page) | It carries three distinct operations (spatial coding, anchoring, route planning) plus a "Beyond space" section that is now the wiki's main statement on non-spatial maps and is large enough to stand alone |
| L3 | **Consider promoting the equilibrium-propagation section out of [[wiki/concepts/energy-based-models.md]]** | It is ~45% of that page, has two primary sources, its own tension (T69), and is a *learning rule* sitting inside a page about a model class. Deferred at this pass: it is currently referenced from only three pages, so a split would create a near-orphan |
| L4 | **Refresh the `From` column of [[wiki/architectural-gaps.md]] for waves 4.1–4.9** | Provenance notes stop at Litman 2025; Whittington & Bogacz 2019, Bengio et al. 2015, Gerstner et al. 1996, Scellier & Bengio 2017, Millidge et al. 2020 and Shervani-Tabar & Rosenbaum 2023 all landed content in [[wiki/empirical-tensions.md]] without a matching gap-table note |

## Standing

| # | Task | Why |
|---|---|---|
| S1 | Keep `Connections` bidirectional | A one-way link is a maintenance defect; caught at each lint pass |
| S2 | Lint after ~every 20 ingests | Structural decay accumulates silently. This pass found the two files that decay fastest are `overview.md` (it had gone 56 ingests without a rewrite) and this one |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | The search index is what the next ingest reads first; a stale index hides existing pages and causes duplicates |
| S4 | **Do not keep a per-ingest log in `overview.md`** | The format broke at ~11 sources and left the page unmaintained for 56 more. Per-source detail belongs in the git log and in the gap table's `From` column; the overview is thematic |
