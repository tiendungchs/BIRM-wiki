# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P4 | **Ablate the interface** — learnable hybrid unit vs fixed converter, matched architecture | *Carried.* `T235` is `LIVE — B asserted, not measured`. [[wiki/concepts/transthalamic-context-routing.md]] says a real inter-module edge carries **two cargoes on two routes**, so the thing to ablate may be the second channel | Ready to attempt |
| P12 | **Re-price `G37` against the retrieval result** | **Promoted from `Next`, and the continual-learning evidence is why.** [[wiki/entities/cn-dpm.md]] gives `G37` its first number: forgetting **0.0**, gating **48.18%** at five components and **31.14%** at twenty. `G37` is also the fastest-growing cell in the file (15,386 → **17,027**, +11%) | Ready to attempt |
| P6 | **Score one joint-embedding model on held-out *transformations*** | *Carried.* [[wiki/concepts/manifold-untangling.md]]'s criterion is strictly stronger than the linear probes seventeen entity pages report; one protocol change re-prices all of them and supplies `L6`'s missing column | Ready to attempt |
| P5 | **Report units-to-competence for anything** | *Carried.* The only admissible evidence for the spiking substrate if its advantage is unit *count* (`T234`). Derivable from existing runs | Ready to attempt |
| P22 | **Run the effective-model ladder on one wiki architecture** | **New, from the interpretability sources.** [[wiki/concepts/multi-token-embedding.md]] supplies the wiki's first instrument that *prices* an interpretation: replace each component with the simplest stand-in, re-run end to end, report accuracy at every rung (100 → 98 → 90 → 94 → 85%). Directly runnable against [[wiki/entities/othellogpt.md]], which is `P13`(a) | Ready to attempt |
| P7 | **Run `I8` and `I9` against the wiki's own entity pages** | *Carried.* The two instruments needing nothing but the model itself. The inventory's cost ordering is still untested *as a claim* | Ready to attempt |
| P23 | **Re-score one benchmark null under a second frame** (`T291`) | **New, from the comparative-cognition sources, and the cheapest re-analysis in the file.** The comparative literature gets opposite answers from the same subjects under cooperative vs competitive framings. Every null in the wiki was collected under exactly one frame, so every one of them is ambiguous between *absent capability* and *undeployed capability* (`G102`) | Ready to attempt |
| P8 | **Collect a per-item human error *profile* on one benchmark** | *Carried.* Job 4 at [[wiki/concepts/human-baseline.md]] and `F4`: aggregate parity is compatible with an orthogonal mechanism (.90 vs .91 accuracy at **0.06** profile correlation) | Human curation, or a re-analysis request |
| P9 | **Build the write-mask experiment (`G52`), and build `G64` with it** | *Carried.* Still the cheapest concrete architecture proposal here. `P4` is its prerequisite. **The continual-learning sources supply its baseline**: uniform reservoir sampling, which beat every priority rule tested | — |
| P10 | **Run the `g`/`x` objective search, collapse filter first** | *Carried.* `G30` remains the deepest gap. A candidate must separate a real structural code from a constant `g` ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P29 | **Run the four-cell valence × action design on a wiki agent** (`G116`) | **New this pass, and it is the cheapest experiment the file has ever carried: it needs no neuroscience and no new environment.** `G116` says every result the wiki's dopamine disputes (`T122`, `T85`) are adjudicated on populates only the two congruent cells — reward×Go and punishment×No-Go — where a valence signal and an action signal are numerically identical. Score any agent with a Go/No-Go gating architecture ([[wiki/entities/pbwm.md]] first) on all four cells at matched difficulty; the row predicts a deficit specific to `reward × No-Go` and `punishment × Go` | Ready to attempt |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P24 | **Give one architecture a growth schedule** (`G100`) | **New, from the comparative-cognition sources, and it stopped being a metaphor.** [[wiki/concepts/developmental-heterochrony.md]] measures the shape: a **vector of per-module phase offsets** over a trajectory whose form is conserved, sparse (~4% of components), gray-matter-specific. Not "train it slower". Nothing in the wiki has any growth schedule at all | Ready to attempt |
| P11 | **Score architectures against the six hardness sources** | *Carried, unmoved for nine passes.* 118 entity pages, instrument still empty. A **facet vector**, not a scalar (`G13`, now cited from [[wiki/concepts/latent-graph-discovery.md]]) | Ready to attempt |
| P13 | **Run the two specified-and-unrun world-model experiments.** (a) Probe/ablate OthelloGPT's end-game features (`T157`/`T158`). (b) LeJEPA rescaled-loss checkpoint ranking against a patch-token probe | *Carried.* **`P22` supplies the missing method for (a)** and `T285` supplies the reason to run it: the wiki's record says every "not decomposable" verdict so far dissolved under a better frame | Ready to attempt |
| P14 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | *Carried, unmoved for nine passes.* The clone-pool construction is the rate-level answer to element distinctness, which one spiking neuron computes in a single unit (`G80`) | — |
| P15 | **Run the `γ_effective` readout** | *Carried.* `T141` is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error on a quantity with no stationary value function | Ready to attempt |
| P16 | **Instrument one existing store with a read log** | *Carried, and the continual-learning sources give it a number to beat.* [[wiki/entities/cn-dpm.md]]'s gate scores 31.14% at twenty components. Log *which* structure was retrieved and score the schedule separately from the contents | Ready to attempt |
| P17 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | *Carried, and the continual-learning sources sharpened it into a live disagreement.* `T283`: must the router be Bayes' rule over generative models, or may it be a discriminative network? Two wiki entities take opposite sides and have never been run head-to-head. Also supplies `G12`'s and `G91`'s router | Human curation or a web-search pass |
| P19 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) | *Carried, narrowed.* **ObjectNet and ImageNet-C are closed** — [[wiki/entities/objectnet.md]] (Barbu et al. 2019) and [[wiki/entities/imagenet-c.md]] (Hendrycks & Dietterich 2019) are paged from primary sources and supply `I26`, `I27`, `I29` and `F15`. **`I1`'s image half is now closed too** — [[wiki/entities/waterbirds.md]] (Sagawa et al. 2020) is paged from the primary source and supplies the instrument with a planted confound at a *chosen* rate (95%), a worst-group read-out, the interpolation no-op, and the regularisation reversal (60.0 → 21.3 under ERM against 60.0 → 84.6 under group DRO). What remains of this row is Shift-MNIST and unfair dSprites, which are now redundant with it rather than blocking. `P6` sharpens what to read them for | Human curation or a web-search pass |
| ~~P20~~ | ~~Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search~~ | *Three closed* — [[wiki/entities/dqn.md]] (Mnih et al. 2015), [[wiki/entities/progressive-neural-networks.md]] (Rusu et al. 2016) and [[wiki/entities/elastic-weight-consolidation.md]] (Kirkpatrick et al. 2017) are now paged from primary sources. *Episodic control closed too* — [[wiki/entities/neural-episodic-control.md]] (Pritzel et al. 2017) is paged from the primary source, which also carries MFEC's mechanism and numbers as its baseline. *Monte Carlo tree search closed too* — [[wiki/entities/alphazero.md]] (Silver et al. 2017) is paged, and it is a **negative** result for the framing: the graph is handed over as a perfect simulator and the state/action discretisation is declared domain knowledge, so it isolates search, valuation and policy compilation and bears on discovery not at all (`G27`, `T14`). ***MFEC closed too*** — [[wiki/entities/model-free-episodic-control.md]] (Blundell et al. 2016) is paged from the primary source, which retires the last second-hand artefact on this row. It also delivered more than the citation debt it was opened for: the `max`-vs-expectation write contract as a stated restriction of the environment class (`T352`), and the wiki's only exact-state-revisit statistics for a benchmark (10–60% on Atari against <0.1% on Labyrinth, `T353`), which reprice every sample-efficiency claim here. **`P20` is now fully closed.** | — |
| P25 | Acquire a source on **edge-density scaling** (`G101`) | **New, from the comparative-cognition sources.** 2.75× cortical volume on 1.25× the neurons — the difference is neuropil, i.e. connections. Every scaling argument in the wiki is about units, and no result in it says what a higher synapse-per-unit ratio buys | Human curation or a web-search pass |
| P26 | Acquire a primary source on **curriculum / training-order** (`L20`) | **New this pass.** The data-schedule half of `G100`, used on 36 pages and owned by none. Candidates: Bengio et al. 2009 (curriculum learning, the artefact the term traces to) paired with a developmental input-statistics source, so the machine lever and the biological schedule land together rather than the lever alone | Human curation or a web-search pass |
| ~~P27~~ | ~~**Ingest the five clippings that arrived during this pass**~~ | **Closed this pass.** All five were ingested in the stated order (Francioni before its two secondary treatments), plus a sixth unqueued file — the motor-cortex transcript. Two new concept pages came out of it: [[wiki/concepts/vectorized-instructive-signals.md]] and [[wiki/concepts/output-null-subspace.md]]. Six ingests, `S15` reconciling, no `Connections` debt: the wiki's first clean entry of a whole batch | — |
| P28 | **Write the four missing overview sections** | *Carried, and it grew again — this is the row that gets worse every pass by definition.* The thematic body still ends at **section 17** while the corpus stands at **448**, so roughly **94 sources plus 8 pending clippings** sit outside it, up from 64 last pass. Three sections were owed and a fourth is now owed: (i) the router and the edge (mixture-of-experts end to end, the deep reinforcement-learning artefacts, the continual-learning baselines, invariant risk minimisation, the robustness instruments); (ii) the per-tension adjudications; (iii) the arrangement, the objective, and the parts that reject — still the easiest to write and the most owed, since it landed the primary sources under machinery running everywhere (InfoNCE, the tensor-product binder, the information bottleneck, ICM, the disentanglement theorem); and **(iv), new this pass: the cortical channel and its rulers** — the 14 ingests of waves 22–23 are a single argument and nothing in the overview says it. What they changed about the picture is specific: the hierarchy's depth coordinate is a *continuous, uncertain* measurement that stops working above the ventral stream (`T378`), its direction may be a carrier frequency rather than a named channel (`T379`), the cortical invariant unit may be the cell class and not the layer (`T371`), the association site is a *port inside one cell* that any area can write to (`T374`, `T376`), the residual is one signed scalar inside a cell and two rectified channels between cells (`T341`), and cortex — not the store — owns the first leg of the consolidation dialogue and predicts its content (`T377`). **That is a revision to the wiki's central thesis, not an addition to it**, and it exists only as registry rows and per-page `Connections`. `P28` has been carried for ten passes; the fourth section is the argument for writing (iv) first, because it is the one whose sources are still fresh enough to synthesise without re-reading them | Ready to attempt |
| P30 | **The queue is no longer empty — the next operation is an `INGEST`, and the wave that fills it is already chosen** | *Rewritten this pass; the premise it carried has expired.* `S15` reconciles at **448 ingested + 1 skipped + 8 pending = 457**, and all 8 pending entries were verified genuinely unread this pass (see `L32`). Wave 23 is one coherent block — the **posterior exit from the hippocampal formation**: retrosplenial cortex (`vann-2009`, `alexander-2015`, `alexander-2023`), posterior cingulate as three nodes rather than one (`foster-2023`), the anterior thalamic nuclei as a third core component (`aggleton-2022`), the subiculum's output stage typed by cell class (`cembrowski-2018`, `kinman-2026`), and one implemented model that performs the egocentric↔allocentric transform the wiki only names (`bicanski-2018`). Read `bicanski-2018` first: `G39` and `G43` have **no** candidate mechanism before it, and the three retrosplenial reviews are what keep it honest. **The two long-carried acquisition rows are untouched by this wave and still blocked**: `P26` (`L20`, curriculum / training order) and `P25` (`G101`, edge-density scaling), plus `G116`'s three crossed-design targets from the previous want-list | Ready to attempt (`INGEST`) |

## Structural work

| # | Task | Why |
|---|---|---|
| ~~L15~~ | ~~**27 tension rows are cited by no page**~~ | **Closed this pass.** All 27 (`T3 T14 T19 T54 T60 T66 T70 T71 T72 T73 T74 T87 T95 T101 T102 T103 T115 T163 T181 T194 T198 T200 T243 T244 T246 T255 T268`) are now wired into the page that carries them — three by extending an existing bullet that already stated the content without the row id, the rest as a new judged bullet in that page's `Open problems` or `Limitations`. `tools/registry-index.py` now reports **0 of 302** |
| ~~L18~~ | ~~**[[wiki/entities/default-mode-network.md]] has decayed into a per-source log**~~ | **Closed this pass.** All eight paper-named sections (`Menon 2023 — …`, `Raichle et al. 2001 — …`, `Paquola et al. 2025 — …`, …) are renamed to the claim each one makes and reordered **primary evidence before synthesis**: founding measurement → medial-prefrontal tiers → state-dependent membership → cytoarchitecture → anticorrelation → the shape reading → the twenty-year review → the developmental layer. Content is verbatim; each section keeps its provenance blockquote, so attribution moved from the heading to the quote where the schema puts it. Six positional cross-references (`Buckner et al. 2008 above`, `Satpute et al. 2026 above`, `everything above`) were repaired by naming the section rather than the direction — **that repair is the real cost of the format**, and the reason a per-source page is expensive to fix later rather than merely ugly |
| ~~L19~~ | ~~**The wiki has no page for confidence calibration and 12 pages depend on one**~~ | **Closed this pass, exactly as specified.** All three queued primary sources landed and they produced *two* pages rather than one, which is the more useful outcome: [[wiki/concepts/confidence-calibration.md]] (Guo et al. 2017 — ECE, its free bin count `M`, and the proof that temperature scaling cannot touch the ranking) and [[wiki/concepts/selective-prediction.md]] (Geifman & El-Yaniv 2017 — the risk–coverage curve and the wiki's first distribution-free competence guarantee), with [[wiki/concepts/metacognitive-efficiency.md]] (Fleming & Lau 2014) supplying the third quantity the wiki had been conflating with both. `T181`'s "score as precision rather than ranking" now has the vocabulary it was missing, and `T320` and `T325` are the new rows the three pages opened. **The lesson is `S7`'s converse holding:** the page written from three primary sources states things no synthesis of 36 scattered mentions could have — that `M` is a free parameter nobody reports, and that MCE can move *opposite* to ECE on identical predictions |
| L20 | **Training *order* is a design variable the wiki uses and does not own** | *Carried, unmoved — it is an acquisition and no source landed.* `curricul*` is now on **39** pages, up 1, again from an ingest that used the word as a lever. **This pass found its twin by the same scan:** `inductive bias` sits on **37** pages and reaches 3+ mentions on only six ([[wiki/concepts/three-component-framework.md]] at 8, then [[wiki/concepts/objective-identifiability.md]], [[wiki/concepts/disentanglement.md]], …) — the identical broad-and-shallow signature, and it is what `P17`'s acquisition would have to own. `gating` is the counter-case worth recording: **111** pages, but genuinely four different mechanisms (working-memory gate, mixture-of-experts route, consolidation gate, ion channel), so breadth there argues for glossary disambiguation and *against* a page. `curricul*` appears on 36 pages and ≥3 times on eight ([[wiki/concepts/skill-acquisition-efficiency.md]], [[wiki/entities/irene.md]], [[wiki/concepts/subgraph-matching.md]], [[wiki/concepts/intelligence-density.md]], [[wiki/entities/byol.md]], …), always as an assumed lever and never as a claim with evidence behind it. `G100` covers the **capacity** schedule (a model that grows) and nothing covers the **data** schedule (what it is shown, in what order). The two are the same design question from opposite sides and `P24` needs both. **Do not write it from the mentions** — the same argument as `L19`; the acquisition target is a primary source on curriculum learning or on developmental input statistics, and it belongs on the next acquisition want-list |
| ~~L21~~ | ~~**A gap's own provenance pages do not know the gap exists — 88 such edges**~~ | **Closed this pass, all 88.** 87 were wired by inserting the row id into a sentence the carrying page already had — the fix is one clause, never a new claim, which is why the debt was invisible to every reading check: the content was right and only the *handle* was missing. The 88th was the exception that names the rule: `G49`'s `## From` listed [[wiki/concepts/representation-probing.md]], which carries no evidence for a scheduled read or a relevance-addressed erase at all, so the repair was to **delete the provenance edge** rather than to manufacture a citation. That is the shape of the residue `S18` was designed to tolerate — a `From` entry naming a page as context — and it turned out to be 1 edge in 88, not the large fraction the check's `tracked, not enforced` status assumed. **Two findings worth keeping.** (i) The debt concentrates on the wiki's *hub* pages: [[wiki/concepts/complementary-learning-systems.md]] and [[wiki/concepts/attention.md]] carried three uncited rows each, and 19 pages carried two — a page cited by many gaps is a page that cites few. (ii) Wiring the id forced a judgement the prose had dodged in about a fifth of cases, because a page that states a gap's content without its id usually states it *approximately*; [[wiki/entities/autotom.md]] is the clearest — it is the wiki's nearest approach to both `G5` and `G29` and had to be written down as a near-miss on each, which no reading of the page alone would have produced |
| L22 | **`S4` has fired ten passes running, and this pass's violation is outside the class `L22` describes** | *Carried, and the rule needs a second clause.* `S4` fired once on entry: [[wiki/concepts/complementary-learning-systems.md]]:178, a **3-column table with a 2-cell row** — the `Why cortex is the slow one` row of the fast-write/slow-write table, where one sentence had been written into a single cell that the header promises two of. **Not an escaping defect at all.** `L22`'s rule — *inside a table cell, escape every `\|` that is not a column separator* — would have caught **0 of 1** this pass, exactly as the conditional-probability rule caught 1 of 7 last pass. The repair was to *split* the cell along the seam the sentence already had (`Not the rule — both forms exist in both structures` \| `LTP is harder to induce in cortex in vivo…`), which is the third distinct repair shape in three passes: add an escape, add a trailing bar, split a cell. **So the class `S4` detects is not 'unescaped bars' but 'the row disagrees with its header', and the three repairs are not variants of one rule.** The corollary matters more than the rule: `S4` is a *column-count* check, and every attempt to replace it with an authoring rule has under-covered it, so the check earns its place in the tool permanently rather than as a bridge until the rule lands. **This pass also broke `S4` itself, in the glossary, from a scripted insertion** — a Python tuple written without its comma became a string, so `ml_rows[0] + ml_rows[1]` inserted the two characters `\|` and a space instead of two rows. `S4` caught it on the next run; nothing else would have |
| ~~L26~~ | ~~**Two `L3` demotion candidates, and both look like mis-levelled rows rather than regrowth** — *needs a ruling*~~ | **Closed this pass, by the ruling the previous pass escalated — and the user took neither of the two options it drafted.** `T4` went to **`L0-INSTR`**, not `L2`: the previous pass read it as a claim about what the architecture may assume is present, but *neither position in the row is a fact about tissue*. Position A is a reading of one instrument — the `cos(6[θ−φ])` fit, which measures directional statistics and by construction cannot measure location — and Position B is the objection that the same instrument does not license the object-invariant, relational claim drawn from it. That is measurement validity, which the ladder works at `L0` priority. The re-level came with its own repair: the row is now wired into [[wiki/concepts/hexadirectional-signal.md]], the instrument page it was always about, which also takes it off the ≤1-citer list. `T284` was **retired to [[wiki/tensions/closed/t284.md]]**, not re-levelled: one paper, one architecture, and [[wiki/entities/continual-dreamer.md]] already holds both halves in prose, which is exactly where the ladder puts realization material. **The generalisable finding:** the previous pass's two proposals were both `L2`, and both were wrong in the same direction — a row's level was being read off *what it would let the architecture do* rather than off *what the row is a claim about*. Levelled by subject rather than by consequence, one row moved up two bands and the other left the registry |
| L27 | **The ladder's population, tracked for a fifth pass** | *Carried, re-measured.* Gaps `L0=28 L0-INSTR=13 L1=17 L2=42 L3=18 L4=1 META=1`; tensions `L0=36 L0-INSTR=64 L1=64 L2=73 L3=73 L4=16 META=6`. `L3`/`L4` = **108, unchanged for a fifth pass** across the largest wave the rule has seen: **23 rows opened** (`G121`, `T359`–`T380`) and every one landed at `L0`, `L1`, `L2` or `L0-INSTR`. `S19` passes with the grandfather count exact at 108. **Demotion candidates: none, on either screen.** No `L3`/`L4` detail file was created since lint(24), and the ≤1-citer tail is **51** (9 gaps, 42 tensions, down from 55) with **not one `L3`/`L4` row in it** — five passes of both alarms returning empty. **The finding this pass adds is about the rule's cost, not its effect:** 23 rows at admissible levels is the first evidence that the ladder is not merely holding a line but is cheap to comply with at volume — no ingest in the wave had to argue a level, because the cortical sources open rows about *what a circuit is a claim about* (cell class vs layer, cortex vs thalamus, order vs named channel), which is `L1`/`L2` material by subject and needs no ruling |
| L31 | **`T276` and `T344` are one wire with four proposed cargoes** — *needs a ruling* | *Carried unchanged, still escalated, still not merged.* No source in this wave touches either row, so nothing has moved the evidence; the two remain sibling-linked with disjoint candidate sets. **What this wave adds is a precedent for the alternative to merging.** `S12` found four more asymmetric row-pairs this pass and resolved every one with a *sibling note* rather than a merge (`T373`↔`T377`, `T374`↔`T376`, `T378`↔`T379`, `T341`↔`T380`) — in each case because the two rows contest different objects on one mechanism and a merge would have to discard one `Closes when`. `T276`/`T344` is the same shape at a larger size, which is an argument for leaving the merge unmade rather than for making it |
| ~~L30~~ | ~~**The ingest queue stopped being maintained 13 ingests ago, and every self-count in the wiki agreed with it because they all read the same stale file**~~ | **Closed as a repair and reopened as `L32` — the repair held for exactly zero waves.** The 28 boxes this row ticked stayed ticked; the next six ingests wrote none of their own, and the *one* entry lint(22) and lint(23) both certified as genuinely pending (`derdikman-2009`) had been ingested on 09-11 by a commit that says `INGEST` in its subject line. The row's diagnosis was right and its remedy was a hand-repair of a file, which is why it did not survive contact with the next wave |
| L32 | **The queue check `L30` specified was never written** | *Carried, and for the first time the defect did not recur — which is the least informative way it could have gone.* Run by hand this pass: all **8** pending entries (`vann-2009`, `aggleton-2022`, `foster-2023`, `alexander-2023`, `kinman-2026`, `bicanski-2018`, `alexander-2015`, `cembrowski-2018`) have **zero** filename mentions anywhere under `wiki/`, so all 8 are genuinely unread and the boxes are honest. `S15` reconciles at `448 + 1 + 8 = 457`. **But the clean result is not evidence the class is closed**, and the reason is structural: the 8 entries were written by an `ACQUIRE` pass that had ingested none of them, which is the one configuration in which the failure *cannot* occur. The defect is created when an `INGEST` forgets to tick, and this wave's 14 ingests were all from the previous queue block, whose boxes lint(24) had just hand-repaired. The check is still three lines of `tools/wiki-stats.sh` searching all of `wiki/` — not `wiki/concepts` and `wiki/entities`, which is how `derdikman-2009` survived two passes — and it is still not in the tool. **Tooling is not this pass's to change**, so the row carries with its remedy unchanged and its evidence weakened |
| L33 | **Row-to-row references inside the registries have no symmetry check, and 4 of 4 audited pairs were one-way** | **New this pass, from `S12`.** `S1` verifies that a `Connections` edge between two *pages* is returned. Nothing verifies the same for a reference between two *rows*, and the registries are full of them. The `S12` audit read this wave's 23 new rows against the registry and found four pairs contesting one mechanism from different sides; in **every** pair the reference existed in exactly one direction, and in **three of four** it was the newer row citing the older (`T377`→`T373`, `T376`→`T374`, `T380`→`T341`), with `T378`/`T379` — two rows opened by the same wave from two papers — linked in neither. **The mechanism is write order, the same one `S18` has:** a row is written while its source is being read, so it can cite what already exists and cannot be cited by it. That is the exact signature `S1` was built for, one level down. All four pairs were repaired by hand this pass (sibling notes, both directions). The check is mechanical and cheap — for every `` `Tnnn` `` or `` `Gnnn` `` appearing in a detail file's body, assert the named file mentions the citing row's own id — and like `L32`'s it belongs in `tools/wiki-stats.sh`, not in a lint row. Not applied here, because the tooling is not this pass's to change. **Second-order finding:** the four one-way references were all *substantive* — each named the sibling's evidence or its `Closes when` — so the missing direction is not a formatting lapse but a row that does not know it is being depended on, which is what makes the dependent row's resolution invisible from the row that would be re-priced by it |
| L29 | **[[wiki/concepts/population-geometry.md]] has the shape `L18` closed on [[wiki/entities/default-mode-network.md]], and it is no longer the worst case** | *Carried into a third pass, and the judgement half is untouched.* population-geometry is **108 KB** over 18 sections, four of them per-source logs by another name. **The new finding is that it has been overtaken:** [[wiki/concepts/latent-graph-discovery.md]] is now **136 KB**, the largest page in the wiki by 26%, and it is the one page the schema makes every decision route through (`Decision-making`, CLAUDE.md). A core-framing page that nobody can read in one sitting fails differently from an overgrown concept page — it does not decay into a log, it decays into an *unread* contract, and no check in the tool can see that because every one of its sections is well-formed. Size alone is not the argument; the argument is that a page cited as the tiebreaker for structural decisions has to be navigable at the moment of a decision. Cheapest probe before proposing a split: count how many of its 18+ sections are reachable from its own opening framing, and whether the latent-bit table ([[wiki/overview.md]] `Master Problem Framing`) is now the page's real index |
| L6 | **Decide whether the joint-embedding entity pages should stay separate** | *Carried.* Seventeen of them, several deferring their central claim to [[wiki/concepts/representational-collapse.md]]. Not merge candidates; the shared comparison table is missing from most. `P6` supplies its one comparable column |
| L8 | **The `Connections` block is the wiki's only navigational structure and it is unweighted** | *Carried.* **7,228 edges over 317 pages**, all one type, none marked *primary*. The edge count is up 17% in three passes on 5% more pages, so the block is densifying faster than the wiki grows — which is the argument for weighting, not against it. Cheapest fix unchanged: mark one edge per page as primary. Note it is now verified bidirectional by `S1` in the script rather than by hand |
| L16 | **The glossary's section discipline is enforced only by a duplicate check** | *Carried.* `S13` catches duplicate keys; **nothing catches a row filed under the wrong heading**, which is the failure that put 64 machine-learning rows under `## Benchmarks`. Section membership is a judgement, so the mechanical proxy is the append point: entries must be appended inside their section, not at end of file. Cheapest fix: have `INGEST` insert by section rather than by append. This pass added 8 rows by section and hit the duplicate check once (`ACME`, already present 378 rows earlier) — which is the check working, and evidence the append-point discipline is not enough on its own |
| L17 | **The `Closes when` fields are written and unaudited** | *Carried, and further discharged.* This pass read the **18 pre-existing tension criteria the wave's own ingests edited** (`T28 T30 T33 T34 T81 T82 T97 T99 T118 T132 T133 T244 T252 T259 T269 T334 T341 T347`) against the evidence those 14 ingests delivered. **None is retirable, and for the fifth pass running the reason is uniform:** every one closes on an experiment nobody has run, not on a reading nobody has done. `T341` is the sharpest case and the one worth recording — the ingest that edited it (`jordan-2020`) ran *the measurement the row was opened for*, moved it to `LEANING`, and left the `Closes when` untouched and unmet, because the criterion asks for mismatch responses **and** visual tuning in the same cells and whole-cell recording time cannot reach both. **A criterion can be addressed by the very paper it was written for and still not be met**, which is the cleanest available evidence that these fields were written against experiments rather than against papers. `T244` is the file's one permanent exception, `BOTH` by construction. Remaining debt: the ~290 criteria still unread against their own evidence |

## Standing

| # | Rule | State at this pass |
|---|---|---|
| S1 | Keep `Connections` bidirectional | **Violated on entry — 10 asymmetric edges, and 7 of them on one page.** [[wiki/entities/remerge.md]] was created by this wave with eleven outbound entries and reciprocals on only four; [[wiki/concepts/schema-assimilation.md]] pointed at it and got nothing back; plus two unrelated one-ways ([[wiki/concepts/valuation-system-decomposition.md]]→[[wiki/entities/ventral-tegmental-area.md]], [[wiki/entities/amygdala.md]]→[[wiki/entities/medial-prefrontal-cortex.md]]). All 10 repaired; **8,419 → 8,429 edges, clean on exit.** **This ends the previous pass's positive evidence that the fix belongs at page creation.** Last pass six new pages arrived symmetric and the conclusion drawn was that the discipline had landed; this pass one new page arrived with 64% of its return trips missing. One clean wave was a wave with no hub-adjacent page in it — `remerge` connects to eight existing pages, and the cost of a new page's symmetry scales with how well-connected it is, which is exactly when the author is least likely to finish the round trip |
| S2 | Lint after ~every 20 ingests | Seventeen passes: 22, 20, 20, 20, 18, 17, 15, 20, 6, 0, 13, 17, 15, 0, 6, **14**. Run on request one wave after the previous pass. The `S2` refinement stands unimplemented for a seventh pass |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | A stale index hides existing pages and causes duplicates |
| S4 | Check table rows by column count — **reset the expected width at every table boundary** | **Violated on entry (1 cell) and once more mid-pass, by this pass's own scripted glossary edit.** The entry defect was a *missing* cell, not an unescaped bar — see `L22`, where the rule this row has been waiting for is shown to catch 0 of 1. Clean on exit. The check has now caught three structurally different defects in three passes and no authoring rule proposed for it has covered more than one of the three |
| S5 | Overview's source count is checked and exits non-zero | **Violated on entry, 418 vs 448 — the largest drift the check has ever reported, and this time the queue was innocent.** Last pass the check read a stale queue and agreed with itself; this pass the queue was correct at 448 and the *overview* was 30 behind, because 14 ingests wrote their pages and none touched the paragraph. So the two failure modes are now both observed and they are opposite: the check can be fooled by a stale authority, or it can be right and ignored. Repaired. **The three unchecked siblings in the same sentence were stale again** (188/161/119 against 203/165/120) and were found only because `S5` dragged the audit onto them, for the second pass running — which is now a pattern rather than an anecdote and settles the argument for checking all five |
| S5b | **Overview's tension count likewise** | **Violated on entry, 311 vs 332 — 21 rows behind.** Repaired in the same sentence as `S5` and in the one other place the number is published ([[wiki/overview.md]] section on the tension registry, `(311 rows` → `(332 rows`). Clean on exit. Note the second occurrence was *not* checked by the script — `S5b` reads one site, and the same figure is published twice |
| S6 | Write abbreviations the way the glossary lists them | **Six rows added, the largest batch in nine passes, and every one is from this wave's cortical vocabulary.** `FF / FB` (FeedForward / FeedBack — 4 pages each, bare, the wiki's most-used unexpanded initials), `TEO` (7 pages), `LIP` (4), `DA` (6, always inside an arrow chain), `HER` (3, expanded only in its home page's provenance line), and `PC` — which is the interesting one: **11 pages, two unrelated senses** (Percent Correct in the learning-progress family, Principal Component in the geometry family), neither expanded at most sites. Filed as an explicit two-sense row, the `gating` remedy `L20` proposed, and the first time the glossary carries a disambiguation rather than an expansion. Rejected at 2 pages by the standing threshold: `SWS`, `LPM`/`CPM` (both expanded on their defining page). `AC` was a false positive — 45 mentions, almost all the model name `V-JEPA 2-AC` or the formula `AC(t_j)`, which is the shape a bare-initials scan will keep producing |
| S7 | A concept reaching ~50% of a host page is a page — and the host need not be a page | Scan re-run on unchanged pages: empty again, as it must be. Nothing to add |
| S8 | Every cited `G`/`T` identifier has a row; no row shares an identifier | Clean, and `S14` is now a hard fail at **0 uncited gap rows** for the second pass running |
| S9 | A number quoted against a human is incomplete without its protocol | **Extended to models by `T291`**: a capability number is incomplete without its *frame*. A null under one framing does not measure the capability |
| S10 | Every published self-count names the command that produces it — **and something runs it** | Violated on entry again (`S5`, `S5b`). The refinement this pass: **a self-count can be right while the sentence around it is wrong**. a progress fraction sat next to a correct-looking source list and was fifteen ingests stale; nothing in the script reads prose. Cheapest partial fix: derive every published fraction from the queue's own checkboxes, which are already the authority `S15` trusts |
| S11 | Check source integrity through `https://status.claude.com/api/v2/incidents.json`, not the status page | Held for the tenth pass. Incidents on the status page since lint(24) are 09-15 (Mythos/Fable 5.1), 09-14 (Cowork on Windows), 09-16 (Google Play) and 09-10 (API latency); **none names Opus**, and the wave's commits fall outside every window. Nothing to discard |
| S12 | Check both registries for cross-topic duplicates by reading, not by `grep` | Run on the wave's **23** new rows (`G121`, `T359`–`T380`) against the 452-row registry. **Zero merges and four sibling links** — `T373`↔`T377` (cortex/thalamus for the *window* against cortex/hippocampus for the *content* of one consolidation event: three structures, two pairwise contests), `T374`↔`T376` (one apical junction at cell grain and column grain), `T378`↔`T379` (the *depth* ruler and the *direction* ruler of one hierarchy, and `T379`'s ordering rule inherits `T378`'s domain restriction either way), `T341`↔`T380` (how many populations carry the residual, against whether there is a population at all or a sliding detection threshold). **Every one was a merge candidate on subject and none on criterion:** in all four the two rows close on different measurements, and merging would have to discard one `Closes when` — which is the general test this pass proposes for the escalated `L31`. The audit's real product is `L33`: 4 of 4 pairs referenced each other in exactly one direction |
| S13 | **No glossary key may appear twice** | Clean on entry and on exit across six added rows, checked against 907 existing keys. Last pass's two duplicates (`NAc`, `VTA`) came from an ingest re-glossing a term the wiki already had; this pass's rows were added by a lint scan that reads the glossary first, which is the configuration in which the failure cannot happen — so this is a clean run without evidence about the rule, the same caveat `L32` carries |
| S14 | **A registry row and its carrying pages must cite each other, both directions** | Held at zero for the eighth pass on both registries. The ≤1-citer tail is **51** (9 gaps, 42 tensions), down from 55, none at `L3`/`L4`. Extended this pass by `L33` to the case `S14` does not cover: a row and the *other rows* that depend on it |
| S15 | **The queue must reconcile exactly against `raw/`** | **Reconciled arithmetically and was wrong about every term** — `411 + 1 + 7 = 419` balanced while all 7 "pending" entries were already ingested. Repaired to `418 + 1 + 0 = 419`. Six were this wave's own ingests; **the seventh, `derdikman-2009`, was ingested on 09-11 by a commit titled `INGEST Derdikman…` and was certified as the one genuine pending entry by lint(22) *and* lint(23)** — so the box has now survived two audits that looked straight at it |
| S18 | **A gap's `## From` names its carrying pages; each should cite the gap back** | **0 → 4 in 14 ingests, all repaired, 0 → 0 on exit.** `G33` and `G61` both named [[wiki/concepts/eigenoption-discovery.md]], which carries the evidence for each and named neither; `G121` named [[wiki/concepts/amortized-inference.md]] and [[wiki/concepts/valuation-system-decomposition.md]], whose devaluation-based inferences are what the gap attacks. **The regrowth rate fell from 1.2 edges per ingest to 0.29**, and the composition changed with it: last pass every edge was on a gap the wave had just opened, this pass two were on gaps **28 and 60 rows old** that a new page was added to the `From` list of. That is a different defect with the same signature — not *the gap file was written after the page*, but *the page was written long after the gap and the gap's own provenance list was updated while the page's prose was not*. Unlike lint(23)'s 88, **none of the four was a bad edge**: all four pages really do carry their row, and all four repairs were one clause. `G33` and `G61` each needed a *judged* clause rather than an id insertion, because the page's content bears on the gap only partially — the library is discovered but never sequenced (`G33`), and exploration gets a learned inventory but not a learned rate (`G61`) |
