# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| ~~P31~~ | ~~**Run `ACQUIRE` for wave 26 — the queue is empty for the first time in the file's history**~~ | **Closed this pass, and it ran the way the row asked.** `1b2fadf` wrote a want-list of 16 targets against the sibling project's three open requests *before* the clips were fetched, `74b37a8` filed all 16 with queue rows, and all 16 were ingested in queue order. `_work/ingest-queue.md` now reconciles at `496 + 1 + 0 = 497`, `S20` confirms no pending row is false, and the wave produced five gap rows (`G125`–`G129`), ten tension rows (`T395`–`T404`) and the wiki's first owned treatment of how a want is built. **`L34`'s defect did not recur** — this is the first wave since the row opened with an acquire-side rationale behind every queue entry | — |
| P32 | **Run `ACQUIRE` for wave 27 — the queue is empty again** | **New this pass, and it is again the only row that blocks an *operation*.** `496 + 1 + 0 = 497`: nothing is pending. The want-list is now three rows rather than four — `P25` (`G101`, edge-density scaling), `P17` (`T283`, gated/conditional inductive bias, a live two-sided disagreement) and `P19`'s remainder (Shift-MNIST, unfair dSprites) — because `P26` closed on inspection rather than on an acquisition (see `L20`). Wave 26 is the precedent to copy: want-list first, rationale written against a named row, then clip | Ready to attempt (`ACQUIRE`) |
| P33 | **Back one agent's subgoal currency, and give it a balance** (`G125`, `G127`) | **New this pass, and it is the cheapest architecture experiment the file has carried since `P29`.** `G125` states the change in one line: give a hierarchical agent a scalar `w_o ∈ [0,1]` per option, deliver `r̃ = w_o` on termination, and update `w_o` by a Pavlovian rule — raise it when primary reward follows termination inside the trace, lower it when it does not. Three read-outs, all already specified: whether the farming failure a constant `r̃` permits disappears, whether `w_o` converges to the option's empirical probability of leading to reward, and whether devaluing the primary leaves `w_o` standing until the option is actually run unbacked. `G127` adds the second half for two more lines — accumulate `r̃` in a register, expose it to the policy, deliver it to the critic on its own schedule — and the closing test is the ED/UD preference reversal. Both rows are `L1` and neither needs any neuroscience to run | Ready to attempt |
| P4 | **Ablate the interface** — learnable hybrid unit vs fixed converter, matched architecture | *Carried.* `T235` is `LIVE — B asserted, not measured`. [[wiki/concepts/transthalamic-context-routing.md]] says a real inter-module edge carries **two cargoes on two routes**, so the thing to ablate may be the second channel | Ready to attempt |
| P12 | **Re-price `G37` against the retrieval result** | **Promoted from `Next`, and the continual-learning evidence is why.** [[wiki/entities/cn-dpm.md]] gives `G37` its first number: forgetting **0.0**, gating **48.18%** at five components and **31.14%** at twenty. `G37` is also the fastest-growing cell in the file (15,386 → **17,027**, +11%) | Ready to attempt |
| P6 | **Score one joint-embedding model on held-out *transformations*** | *Carried.* [[wiki/concepts/manifold-untangling.md]]'s criterion is strictly stronger than the linear probes seventeen entity pages report; one protocol change re-prices all of them and supplies `L6`'s missing column | Ready to attempt |
| P5 | **Report units-to-competence for anything** | *Carried.* The only admissible evidence for the spiking substrate if its advantage is unit *count* (`T234`). Derivable from existing runs | Ready to attempt |
| P22 | **Run the effective-model ladder on one wiki architecture** | **New, from the interpretability sources.** [[wiki/concepts/multi-token-embedding.md]] supplies the wiki's first instrument that *prices* an interpretation: replace each component with the simplest stand-in, re-run end to end, report accuracy at every rung (100 → 98 → 90 → 94 → 85%). Directly runnable against [[wiki/entities/othellogpt.md]], which is `P13`(a) | Ready to attempt |
| P7 | **Run `I8` and `I9` against the wiki's own entity pages** | *Carried.* The two instruments needing nothing but the model itself. The inventory's cost ordering is still untested *as a claim* | Ready to attempt |
| P23 | **Re-score one benchmark null under a second frame** (`T291`) | **New, from the comparative-cognition sources, and the cheapest re-analysis in the file.** The comparative literature gets opposite answers from the same subjects under cooperative vs competitive framings. Every null in the wiki was collected under exactly one frame, so every one of them is ambiguous between *absent capability* and *undeployed capability* (`G102`) | Ready to attempt |
| P8 | **Collect a per-item human error *profile* on one benchmark** | *Carried.* Job 4 at [[wiki/concepts/human-baseline.md]] and `F4`: aggregate parity is compatible with an orthogonal mechanism (.90 vs .91 accuracy at **0.06** profile correlation) | Human curation, or a re-analysis request |
| P9 | **Build the write-mask experiment (`G52`), and build `G64` with it** | *Carried.* Still the cheapest concrete architecture proposal here. `P4` is its prerequisite. **The continual-learning sources supply its baseline**: uniform reservoir sampling, which beat every priority rule tested | — |
| P10 | **Run the `g`/`x` objective search, collapse filter first** | *Carried.* `G30` remains the deepest gap. A candidate must separate a real structural code from a constant `g` ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |
| P29 | **Run the four-cell valence × action design on a wiki agent** (`G116`) | **New this pass, and it is the cheapest experiment the file has ever carried: it needs no neuroscience and no new environment.** `G116` says every result the wiki's dopamine disputes (`T122`, `T85`) are adjudicated on populates only the two congruent cells — reward×Go and punishment×No-Go — where a valence signal and an action signal are numerically identical. Score any agent with a Go/No-Go gating architecture ([[wiki/entities/pbwm.md]] first) on all four cells at matched difficulty; the row predicts a deficit specific to `reward × No-Go` and `punishment × Go` | Ready to attempt |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P24 | **Give one architecture a growth schedule** (`G100`) | **New, from the comparative-cognition sources, and it stopped being a metaphor.** [[wiki/concepts/developmental-heterochrony.md]] measures the shape: a **vector of per-module phase offsets** over a trajectory whose form is conserved, sparse (~4% of components), gray-matter-specific. Not "train it slower". Nothing in the wiki has any growth schedule at all | Ready to attempt |
| P11 | **Score architectures against the six hardness sources** | *Carried, unmoved for nine passes.* 118 entity pages, instrument still empty. A **facet vector**, not a scalar (`G13`, now cited from [[wiki/concepts/latent-graph-discovery.md]]) | Ready to attempt |
| P13 | **Run the two specified-and-unrun world-model experiments.** (a) Probe/ablate OthelloGPT's end-game features (`T157`/`T158`). (b) LeJEPA rescaled-loss checkpoint ranking against a patch-token probe | *Carried.* **`P22` supplies the missing method for (a)** and `T285` supplies the reason to run it: the wiki's record says every "not decomposable" verdict so far dissolved under a better frame | Ready to attempt |
| P14 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | *Carried, unmoved for nine passes.* The clone-pool construction is the rate-level answer to element distinctness, which one spiking neuron computes in a single unit (`G80`) | — |
| P15 | **Run the `γ_effective` readout** | *Carried.* `T141` is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error on a quantity with no stationary value function | Ready to attempt |
| P16 | **Instrument one existing store with a read log** | *Carried, and the continual-learning sources give it a number to beat.* [[wiki/entities/cn-dpm.md]]'s gate scores 31.14% at twenty components. Log *which* structure was retrieved and score the schedule separately from the contents | Ready to attempt |
| P17 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | *Carried, and the continual-learning sources sharpened it into a live disagreement.* `T283`: must the router be Bayes' rule over generative models, or may it be a discriminative network? Two wiki entities take opposite sides and have never been run head-to-head. Also supplies `G12`'s and `G91`'s router | Human curation or a web-search pass |
| P19 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) | *Carried, narrowed.* **ObjectNet and ImageNet-C are closed** — [[wiki/entities/objectnet.md]] (Barbu et al. 2019) and [[wiki/entities/imagenet-c.md]] (Hendrycks & Dietterich 2019) are paged from primary sources and supply `I26`, `I27`, `I29` and `F15`. **`I1`'s image half is now closed too** — [[wiki/entities/waterbirds.md]] (Sagawa et al. 2020) is paged from the primary source and supplies the instrument with a planted confound at a *chosen* rate (95%), a worst-group read-out, the interpolation no-op, and the regularisation reversal (60.0 → 21.3 under ERM against 60.0 → 84.6 under group DRO). What remains of this row is Shift-MNIST and unfair dSprites, which are now redundant with it rather than blocking. `P6` sharpens what to read them for | Human curation or a web-search pass |
| ~~P20~~ | ~~Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search~~ | *Three closed* — [[wiki/entities/dqn.md]] (Mnih et al. 2015), [[wiki/entities/progressive-neural-networks.md]] (Rusu et al. 2016) and [[wiki/entities/elastic-weight-consolidation.md]] (Kirkpatrick et al. 2017) are now paged from primary sources. *Episodic control closed too* — [[wiki/entities/neural-episodic-control.md]] (Pritzel et al. 2017) is paged from the primary source, which also carries MFEC's mechanism and numbers as its baseline. *Monte Carlo tree search closed too* — [[wiki/entities/alphazero.md]] (Silver et al. 2017) is paged, and it is a **negative** result for the framing: the graph is handed over as a perfect simulator and the state/action discretisation is declared domain knowledge, so it isolates search, valuation and policy compilation and bears on discovery not at all (`G27`, `T14`). ***MFEC closed too*** — [[wiki/entities/model-free-episodic-control.md]] (Blundell et al. 2016) is paged from the primary source, which retires the last second-hand artefact on this row. It also delivered more than the citation debt it was opened for: the `max`-vs-expectation write contract as a stated restriction of the environment class (`T352`), and the wiki's only exact-state-revisit statistics for a benchmark (10–60% on Atari against <0.1% on Labyrinth, `T353`), which reprice every sample-efficiency claim here. **`P20` is now fully closed.** | — |
| P25 | Acquire a source on **edge-density scaling** (`G101`) | **New, from the comparative-cognition sources.** 2.75× cortical volume on 1.25× the neurons — the difference is neuropil, i.e. connections. Every scaling argument in the wiki is about units, and no result in it says what a higher synapse-per-unit ratio buys | Human curation or a web-search pass |
| ~~P26~~ | ~~Acquire a primary source on **curriculum / training-order** (`L20`)~~ | **Closed this pass, and it was already closed when it was written.** [[wiki/concepts/curriculum-learning.md]] (Bengio, Louradour, Collobert & Weston 2009, `raw/bengio-2009-curriculum-learning.md`, 25 KB) has existed since `482c93a` and existed at the previous lint commit — the page even opens by naming `L20` as the row it discharges. The acquisition this row asked for had landed before the row was opened. What the page does **not** own is the developmental input-statistics half, which is a different acquisition and belongs to `P24`/`G100` rather than here. See `L38` for why the row survived a pass | — |
| ~~P27~~ | ~~**Ingest the five clippings that arrived during this pass**~~ | **Closed this pass.** All five were ingested in the stated order (Francioni before its two secondary treatments), plus a sixth unqueued file — the motor-cortex transcript. Two new concept pages came out of it: [[wiki/concepts/vectorized-instructive-signals.md]] and [[wiki/concepts/output-null-subspace.md]]. Six ingests, `S15` reconciling, no `Connections` debt: the wiki's first clean entry of a whole batch | — |
| P28 | **Write the five missing overview sections** | *Carried, and it grew again — this is the row that gets worse every pass by definition.* The thematic body still ends at **section 17** while the corpus stands at **496**, so roughly **142 sources** sit outside it, up from 126 last pass, with the queue at zero and no pending clippings to blame any of it on. Four sections were owed and a fifth is now owed: (i) the router and the edge (mixture-of-experts end to end, the deep reinforcement-learning artefacts, the continual-learning baselines, invariant risk minimisation, the robustness instruments); (ii) the per-tension adjudications; (iii) the arrangement, the objective, and the parts that reject; (iv) the cortical channel and its rulers (waves 22–23: the depth coordinate is a *continuous, uncertain* measurement that stops working above the ventral stream (`T378`), its direction may be a carrier frequency (`T379`), the invariant unit may be the cell class and not the layer (`T371`), the association site is a *port inside one cell* (`T374`, `T376`), the residual is one signed scalar inside a cell and two rectified channels between cells (`T341`), and cortex owns the first leg of the consolidation dialogue (`T377`) — a revision to the central thesis, not an addition to it); (v) what a store's discreteness is a property of (wave 25: `K^max = α_n N^{n−1}` with an interior optimum in `n`, the attention layer as an associative read, `T389` `T392` `T393` `T394` `G123` `G124`); **and (vi), new this pass: how a want is built, and what it costs.** The sixteen wave-26 ingests are one argument and the thesis has no sentence for any of it. Every internally-generated reward in the wiki is a fixed function of state — immune to its own delivery (`G125`), with no opponent term (`G126`), spent the instant it is earned (`G127`) — while every cost term is a fixed function of load with no write port (`G128`) and every intrinsic reward is exhausted by the learning it drives (`G129`). That is **five rows saying the same thing about five different quantities**: the wiki's self-generated motivational machinery has no update rule anywhere in it, on either side of the subtraction. The measured half arrived with it (a 0.7/0.4/0 exchange rate, a complete preference reversal driven by the redemption schedule alone, a thirty-year yoked-control literature on trainable effort cost) and the architecture half arrived as four option-discovery families the census did not contain. **`P28` has been carried for twelve passes; (vi) is the one whose sources are still warm** | Ready to attempt |
| ~~P30~~ | ~~**Only wave 25 is pending**~~ | **Closed this pass.** All thirteen wave-25 sources were ingested on 2026-09-19 in queue order, and the wave delivered more than `G42`: three concept pages ([[wiki/concepts/attractor-identification.md]], [[wiki/concepts/key-value-memory.md]], [[wiki/concepts/topological-latent-decoding.md]]) and five entity pages. `G42` did get its derived bound — `K^max = α_n N^{n−1}` with an *interior optimum in `n`* at finite `N`, and a separation-based bound computable from the keys alone with no query — but the row does not close, because both are bounds on a **store**, and `G42`'s `Closes when` asks a store to report its own occupancy. **What the wave actually decided is Axis 1 of [[wiki/concepts/attractor-dynamics.md]]**: `T389`, `T392`, `T393`, `T394`, `G123` and `G124` are all one question — what a store's discreteness is a property of — and none of them existed before this wave | — |

## Structural work

| # | Task | Why |
|---|---|---|
| ~~L15~~ | ~~**27 tension rows are cited by no page**~~ | **Closed this pass.** All 27 (`T3 T14 T19 T54 T60 T66 T70 T71 T72 T73 T74 T87 T95 T101 T102 T103 T115 T163 T181 T194 T198 T200 T243 T244 T246 T255 T268`) are now wired into the page that carries them — three by extending an existing bullet that already stated the content without the row id, the rest as a new judged bullet in that page's `Open problems` or `Limitations`. `tools/registry-index.py` now reports **0 of 302** |
| ~~L18~~ | ~~**[[wiki/entities/default-mode-network.md]] has decayed into a per-source log**~~ | **Closed this pass.** All eight paper-named sections (`Menon 2023 — …`, `Raichle et al. 2001 — …`, `Paquola et al. 2025 — …`, …) are renamed to the claim each one makes and reordered **primary evidence before synthesis**: founding measurement → medial-prefrontal tiers → state-dependent membership → cytoarchitecture → anticorrelation → the shape reading → the twenty-year review → the developmental layer. Content is verbatim; each section keeps its provenance blockquote, so attribution moved from the heading to the quote where the schema puts it. Six positional cross-references (`Buckner et al. 2008 above`, `Satpute et al. 2026 above`, `everything above`) were repaired by naming the section rather than the direction — **that repair is the real cost of the format**, and the reason a per-source page is expensive to fix later rather than merely ugly |
| ~~L19~~ | ~~**The wiki has no page for confidence calibration and 12 pages depend on one**~~ | **Closed this pass, exactly as specified.** All three queued primary sources landed and they produced *two* pages rather than one, which is the more useful outcome: [[wiki/concepts/confidence-calibration.md]] (Guo et al. 2017 — ECE, its free bin count `M`, and the proof that temperature scaling cannot touch the ranking) and [[wiki/concepts/selective-prediction.md]] (Geifman & El-Yaniv 2017 — the risk–coverage curve and the wiki's first distribution-free competence guarantee), with [[wiki/concepts/metacognitive-efficiency.md]] (Fleming & Lau 2014) supplying the third quantity the wiki had been conflating with both. `T181`'s "score as precision rather than ranking" now has the vocabulary it was missing, and `T320` and `T325` are the new rows the three pages opened. **The lesson is `S7`'s converse holding:** the page written from three primary sources states things no synthesis of 36 scattered mentions could have — that `M` is a free parameter nobody reports, and that MCE can move *opposite* to ECE on identical predictions |
| ~~L20~~ | ~~**Training *order* is a design variable the wiki uses and does not own**~~ | **Closed this pass — the row was false when it was last carried.** [[wiki/concepts/curriculum-learning.md]] exists, is 25 KB, is built from the primary source the row named as its target (Bengio et al. 2009), and states the definition the row said was missing: a curriculum is a *sequence of training criteria* obtained by reweighting the target distribution — formally a continuation method, so its payoff is an **optimisation** payoff and should vanish wherever the target objective is already easy to optimise. The page existed at the previous lint commit and the previous pass still recorded *“no source landed”*. The `inductive bias` twin the row identified by the same scan is **not** closed and now belongs to `P17`, which is where it is tracked |
| ~~L21~~ | ~~**A gap's own provenance pages do not know the gap exists — 88 such edges**~~ | **Closed this pass, all 88.** 87 were wired by inserting the row id into a sentence the carrying page already had — the fix is one clause, never a new claim, which is why the debt was invisible to every reading check: the content was right and only the *handle* was missing. The 88th was the exception that names the rule: `G49`'s `## From` listed [[wiki/concepts/representation-probing.md]], which carries no evidence for a scheduled read or a relevance-addressed erase at all, so the repair was to **delete the provenance edge** rather than to manufacture a citation. That is the shape of the residue `S18` was designed to tolerate — a `From` entry naming a page as context — and it turned out to be 1 edge in 88, not the large fraction the check's `tracked, not enforced` status assumed. **Two findings worth keeping.** (i) The debt concentrates on the wiki's *hub* pages: [[wiki/concepts/complementary-learning-systems.md]] and [[wiki/concepts/attention.md]] carried three uncited rows each, and 19 pages carried two — a page cited by many gaps is a page that cites few. (ii) Wiring the id forced a judgement the prose had dodged in about a fifth of cases, because a page that states a gap's content without its id usually states it *approximately*; [[wiki/entities/autotom.md]] is the clearest — it is the wiki's nearest approach to both `G5` and `G29` and had to be written down as a near-miss on each, which no reading of the page alone would have produced |
| L22 | **`S4` fired for an eleventh pass, and this is the first pass where the escaping rule would have caught most of it** | *Carried, and the rule finally earns its keep.* Seven violations on entry, and **six are the escaping class exactly**: an unescaped `\|` inside a table cell from conditional-probability and set-size notation — `log q_φ(z\|s)` ([[wiki/concepts/temporal-abstraction-options.md]]:80), `q_φ(z\|s_t)` and `q_φ(ẑ\|τ*)` ([[wiki/entities/diayn.md]]:75), `R^{\|a\|×k}` and `p(s_{t+c}\|s_t,g)` ([[wiki/entities/feudal-networks.md]]:19, :122), `α·H[a\|s]` ([[wiki/glossary.md]]:494), `e^{\|β\|}` ([[wiki/concepts/information-sampling-vs-search.md]]:86). The seventh is the other class the row already names — a 3-column table with a 2-cell row ([[wiki/concepts/token-reinforcement.md]]:96, a study row whose Design cell was never written) — repaired by splitting, not escaping. **So the tally over three passes is 1 of 7, 0 of 1, then 6 of 7**, and the pattern is legible: the escaping class arrives with *notation-heavy* waves and the short-row class arrives with any wave. Both are real, neither subsumes the other, and `S4`'s column count is the only thing that sees both. The wave-26 signature is worth recording for the next notation-heavy ingest: **every one of the six was a conditional, cardinality or absolute-value bar inside inline code**, which is where an author's attention is on the maths rather than on the table |
| ~~L26~~ | ~~**Two `L3` demotion candidates, and both look like mis-levelled rows rather than regrowth** — *needs a ruling*~~ | **Closed this pass, by the ruling the previous pass escalated — and the user took neither of the two options it drafted.** `T4` went to **`L0-INSTR`**, not `L2`: the previous pass read it as a claim about what the architecture may assume is present, but *neither position in the row is a fact about tissue*. Position A is a reading of one instrument — the `cos(6[θ−φ])` fit, which measures directional statistics and by construction cannot measure location — and Position B is the objection that the same instrument does not license the object-invariant, relational claim drawn from it. That is measurement validity, which the ladder works at `L0` priority. The re-level came with its own repair: the row is now wired into [[wiki/concepts/hexadirectional-signal.md]], the instrument page it was always about, which also takes it off the ≤1-citer list. `T284` was **retired to [[wiki/tensions/closed/t284.md]]**, not re-levelled: one paper, one architecture, and [[wiki/entities/continual-dreamer.md]] already holds both halves in prose, which is exactly where the ladder puts realization material. **The generalisable finding:** the previous pass's two proposals were both `L2`, and both were wrong in the same direction — a row's level was being read off *what it would let the architecture do* rather than off *what the row is a claim about*. Levelled by subject rather than by consequence, one row moved up two bands and the other left the registry |
| L27 | **The ladder's population, tracked for an eighth pass** | *Carried, re-measured, and the grandfathered block still did not move.* Gaps `L0=28 L0-INSTR=13 L1=23 L2=44 L3=18 L4=1 META=1`; tensions `L0=38 L0-INSTR=64 L1=75 L2=84 L3=73 L4=16 META=6`. `L3`/`L4` = **108, unchanged for an eighth pass**, every one grandfathered in `S19` with no stale entries. The fifteen rows opened since lint(27) (`G125`–`G129`, `T395`–`T404`) are **all `L0`, `L1` or `L2`** — the admission rule has now held for three consecutive waves without an exception request. The `≤1`-citer demotion criterion returns **empty of `L3`/`L4` rows for a fourth pass**: 8 gap rows and 39 tension rows sit at one citer, and not one of them is below `L2`. **Eight flat passes is now a finding about the two criteria rather than about the wiki** — `S19` holds the entry point and neither demotion criterion can reach the grandfathered block, so the block will not shrink until some pass reads it directly |
| L31 | **`T276` and `T344` are one wire with four proposed cargoes** — *needs a ruling* | *Carried unchanged, still escalated, still not merged.* No source in this wave touches either row, so nothing has moved the evidence; the two remain sibling-linked with disjoint candidate sets. **What this wave adds is a precedent for the alternative to merging.** `S12` found four more asymmetric row-pairs this pass and resolved every one with a *sibling note* rather than a merge (`T373`↔`T377`, `T374`↔`T376`, `T378`↔`T379`, `T341`↔`T380`) — in each case because the two rows contest different objects on one mechanism and a merge would have to discard one `Closes when`. `T276`/`T344` is the same shape at a larger size, which is an argument for leaving the merge unmade rather than for making it |
| ~~L30~~ | ~~**The ingest queue stopped being maintained 13 ingests ago, and every self-count in the wiki agreed with it because they all read the same stale file**~~ | **Closed as a repair and reopened as `L32` — the repair held for exactly zero waves.** The 28 boxes this row ticked stayed ticked; the next six ingests wrote none of their own, and the *one* entry lint(22) and lint(23) both certified as genuinely pending (`derdikman-2009`) had been ingested on 09-11 by a commit that says `INGEST` in its subject line. The row's diagnosis was right and its remedy was a hand-repair of a file, which is why it did not survive contact with the next wave |
| ~~L32~~ | ~~**The queue check `L30` specified is still not written, and this pass is the third recurrence — the first to take a whole wave**~~ | **Closed this pass — the ruling was taken and the check was written.** `8e0414a` added `S20` to `tools/wiki-stats.sh`, implementing exactly the test `L32` specified: for each `- [ ]` row, match `ingest(<author>-<year>)` against the git log, hard fail on a hit. The header comment states the asymmetry the row did not — *positive evidence only*, since a source ingested before the naming convention leaves no commit, so a missing match is not a claim that a row is genuinely pending. `S20` **OK** on entry at 0 pending. The standing question `L32` was blocking for three passes (*may `LINT` edit `tools/wiki-stats.sh`?*) is answered by the same commit and no longer gates `L33` or `L35` |
| ~~L33~~ | ~~**Row-to-row references inside the registries have no symmetry check — 7 of 7 audited references were one-way, for the second pass running**~~ | **Closed this pass — the check exists, and its first run says the hand audits were not pessimistic.** `8e0414a` added `S21`: for every `` `Gnnn` ``/`` `Tnnn` `` in a detail file's body, assert the named file mentions the citing row's id; it runs in both directions, as `L33` insisted, and grandfathers the pre-existing population in `_work/rowref-baseline.txt`. **First enforced run: 42 new one-way references, all from wave 26** (`G125`–`G129`, `T395`–`T404`, plus `G72`→`T395` and `T367`→`T401` written from the old row's side). All 42 were repaired this pass by writing the answering reference into 24 target files. The cumulative hand-audit figure `L33` closed on — 11 of 11 one-way — is now 53 of 53. What the row cannot close is the grandfathered block: see `L37` |
| L34 | **A wave entered `raw/` with no `ACQUIRE` behind it, and `S15` was the only thing that noticed** | *Carried, and this pass is the first evidence the defect is not structural.* Wave 26 ran the order the row asked for: `1b2fadf` wrote the want-list against three named requests **before** the clips existed, `74b37a8` filed all 16 with queue rows, and every row carries an acquire-side rationale rather than an abstract-derived one. So the pipeline can produce the distinction `L34` says nothing enforces — it just does not *require* it. The two candidate repairs are unchanged and neither is taken (`ACQUIRE` in reconcile mode over unqueued files; or `S15` distinguishing a queued row with a gap tag from one without), and the ruling `L32` was blocking no longer applies, so this row is now a choice rather than a block |
| ~~L35~~ | ~~**`S15` intermittently reports a false positive, and this pass acted on one**~~ | **Closed this pass — the suspect construct was removed rather than explained.** `8e0414a` replaced the ~480 per-file `echo \| grep -qxF` pipelines with two `comm` calls over sorted lists, exactly the repair `L35` proposed; the header comment records the diagnosis and the failed reproduction so neither is re-derived. Both halves of the check now descend from the same pair of lists, and `S15` reported **OK** on this pass's entry run with no spurious *in raw/ but not queued* entry. One clean run is not proof against an intermittent fault, but the construct that could produce it is gone |
| L36 | **Associative memory is a mechanism on twenty-two pages and a concept page on none — and the wave that was supposed to close it made it worse in the predicted direction** | **Carried, and now unblocked.** `L36` said *do not write the page before the wave-25 ingest*. The wave landed, and it produced **five more entity pages** carrying the machinery ([[wiki/entities/dense-associative-memory.md]], [[wiki/entities/two-body-dense-associative-memory.md]], [[wiki/entities/continuous-modern-hopfield-network.md]], [[wiki/entities/neuron-astrocyte-associative-memory.md]], [[wiki/entities/three-factor-key-value-memory.md]]) and no concept page stating the mechanism independently of a network. `associative memory` is now on **22** pages, up from 17, and the concept/entity split is **7/15** — the signature `L36` was opened on, deepened. **What did arrive changes the shape of the remaining work.** [[wiki/concepts/key-value-memory.md]] is a real concept page and it owns the *address* half end to end: the read, the two dials that generate every store here, the learned/fixed/drifting address question, and the equivalence that makes a trained linear layer a memory. What it does not own is the **storage rule and the capacity bound that relates it to retrieval** — that material is now spread across [[wiki/concepts/attractor-dynamics.md]] (the settling half), [[wiki/concepts/retrieval-capacity.md]] (a different question: how many distinct retrieval *sets*) and five entity pages (the numbers). So `L36`'s page is no longer "associative memory" whole — it is the **store half**, written against the address half that already exists, and the pair is the decomposition the wiki has been missing. The primary sources are now all on disk. **This is the highest-value structural row in the file and the only one whose precondition has just been satisfied** |
| L37 | **`S21` grandfathered 215 one-way row references, and nothing in either the check or this file works them down** | **New this pass, and it is `S19`'s shape exactly.** The check that closed `L33` protects the entry point and freezes what was already there: `_work/rowref-baseline.txt` holds **215** pairs, every one a registry row that names another row whose file never names it back. The baseline is exact — 0 stale entries after this pass's 42 repairs, so every pair in it is still one-way on disk. **Two grandfathered blocks now sit in the wiki for the same reason**: 108 `L3`/`L4` rows (`L27`, flat for eight passes) and 215 one-way references, both held by a check that can only see new violations. The repair is not a tool change: it is reading, and it is *bounded* — 215 pairs over 484 rows, and this pass's 42 took roughly the same effort as one page's `Connections` audit. **The cheapest schedule is a quota**: repair the 20 oldest baseline pairs per pass and prune them, which retires the block in eleven passes and costs each pass less than the wave it is already carrying. Nothing else in the file has a finite, counted end state |
| L38 | **A carried row can be false, and nothing re-verifies one — `L20` and `P26` both asserted a missing page that had existed for six days** | **New this pass, and it is `L32`'s defect moved from the queue into this file.** [[wiki/concepts/curriculum-learning.md]] was created at `482c93a` (2026-09-13), existed at the previous lint commit (2026-09-19), and *opens by naming `L20` as the row it discharges*. The previous pass nonetheless carried `L20` as *“no source landed”* and opened `P26` to acquire the source that was already on disk — so a row was not merely stale, it was **re-asserted and then used to justify new work**. `L32` proved the same thing about `_work/ingest-queue.md`: an unmaintained `- [ ]` certifies itself, because the arithmetic that checks it never asks whether the claim is true. The same asymmetry applies here and nothing checks it. **The mechanical half is cheap and has a precedent in `S20`**: for every row in this file whose text claims a page, a file or a source is *missing*, assert it is missing — a name in backticks or a `[[…]]` link that resolves on disk while the row says it does not is a hard fail. That covers `L19`/`L20`/`L36`-shaped rows, which are the ones that create acquisition work. The judgement half — a carried row whose *reasoning* has gone stale — is not mechanisable and is what a lint pass is for. **Ruling needed: should this be `S22`?** |
| L29 | **[[wiki/concepts/latent-graph-discovery.md]] is the largest page in the wiki and the one every decision routes through** | *Carried into a fifth pass, still growing, judgement half still untouched.* **146 KB**, up from 137, against population-geometry's 117 and default-mode-network's 112 — the gap to second place widened again. The argument is unchanged and the drift is the point: a page CLAUDE.md names as the tiebreaker for structural decisions has to be navigable *at the moment of a decision*, and no check in the tool can see that a well-formed page has stopped being readable. The cheapest probe is still unrun — count how many of its sections are reachable from its own opening framing, and whether the latent-bit table at [[wiki/overview.md]] has become the page's real index. **This pass adds the same datum wave 25 added**: sixteen wave-26 ingests decided that the wiki's self-generated motivational machinery has no update rule on either side of the subtraction, and none of it is reachable from the framing page that is supposed to route it |
| L6 | **Decide whether the joint-embedding entity pages should stay separate** | *Carried.* Seventeen of them, several deferring their central claim to [[wiki/concepts/representational-collapse.md]]. Not merge candidates; the shared comparison table is missing from most. `P6` supplies its one comparable column |
| L8 | **The `Connections` block is the wiki's only navigational structure and it is unweighted** | *Carried.* **9,454 edges over 411 pages**, all one type, none marked *primary*. Up 31% in edges on 30% more pages since the count was last taken, so the density per page is now flat where it had been climbing — which weakens the *densifying* half of the argument and leaves the *untyped* half untouched. Cheapest fix unchanged: mark one edge per page as primary. Bidirectionality is verified by `S1` in the script, and `S21` now does the same job one level down for registry rows |
| L16 | **The glossary's section discipline is enforced only by a duplicate check** | *Carried.* `S13` catches duplicate keys; **nothing catches a row filed under the wrong heading**, which is the failure that put 64 machine-learning rows under `## Benchmarks`. Section membership is a judgement, so the mechanical proxy is the append point: entries must be appended inside their section, not at end of file. Cheapest fix: have `INGEST` insert by section rather than by append. This pass added 8 rows by section and hit the duplicate check once (`ACME`, already present 378 rows earlier) — which is the check working, and evidence the append-point discipline is not enough on its own |
| L17 | **The `Closes when` fields are written and unaudited** | *Carried, and this pass discharges the wave's own five.* `S17` holds at 0 unset over 484 rows. The five new gap criteria (`G125`–`G129`) were read against the evidence the wave delivered and **none is met**, for the sixth pass running with the same reason: each closes on an experiment nobody has run, not on a reading nobody has done. Two are worth recording because they are unusually cheap and are now `P33`: `G125` closes on a scalar per option updated by a Pavlovian rule with three named read-outs, and `G127` on an equalised-versus-unequal redemption-delay reversal — **the first two criteria in the file whose experiments are two lines of code each**, against a register of criteria that mostly demand a new preparation. Remaining debt: the ~290 criteria still unread against their own evidence |

## Standing

| # | Rule | State at this pass |
|---|---|---|
| S1 | Keep `Connections` bidirectional | **Violated on entry — one asymmetric edge, and it is the smallest entry state the rule has ever had.** [[wiki/entities/option-critic.md]] → [[wiki/entities/hisd.md]] with no return trip, from the wave-26 option-discovery ingests; repaired by writing the reciprocal into `hisd`, which is the same supervision contrast read from the other side (offline corpus, no reward, a named nestable library against reward-driven interaction and no named skill). **One defect over sixteen ingests, against five over thirteen last pass**, on 9,454 edges. Worth recording *why*: the wave's four new entity pages were written as a mutually-comparing set (`option-critic`/`feudal-networks`/`diayn`/`go-explore` each tabulate the others), so the reciprocals were written as part of the argument rather than as bookkeeping |
| S2 | Lint after ~every 20 ingests | Twenty passes: 22, 20, 20, 20, 18, 17, 15, 20, 6, 0, 13, 17, 15, 0, 6, 14, 1, 13, **16**. Run at the natural boundary again — the wave finished and the queue emptied — which is the second consecutive pass at the interval the rule was written for |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | A stale index hides existing pages and causes duplicates |
| S4 | Check table rows by column count — **reset the expected width at every table boundary** | **Violated on entry (7 rows across 6 files), for the thirteenth pass running, and this is the first pass where `L22`'s escaping rule would have caught most of them: 6 of 7.** All six are a conditional, cardinality or absolute-value bar inside inline code inside a cell (`z\|s`, `z\|s_t`, `ẑ\|τ*`, `\|a\|`, `s_{t+c}\|s_t,g`, `a\|s`, `e^{\|β\|}` at [[wiki/concepts/information-sampling-vs-search.md]]:86); the seventh is the short-row class, a 3-column study table with a 2-cell row at [[wiki/concepts/token-reinforcement.md]]:96. See `L22` for the three-pass tally |
| S5 | Overview's source count is checked and exits non-zero | **Violated on entry, 480 against 496**, and this time the check was right and the number was simply stale — sixteen ingests landed after the paragraph was last written. Repaired to 496 alongside the four derived figures in the same sentence (226 concept, 185 entity, 128 gaps, 356 tensions), which `S10` requires be re-derived rather than remembered |
| S5b | **Overview's tension count likewise** | **Violated on entry, 346 vs 356** — 10 rows behind, repaired in the same edit as `S5`. Second consecutive pass where both source and tension counts drifted together, which is the signature of a wave landing between passes rather than of a maintenance failure |
| S6 | Write abbreviations the way the glossary lists them | **Three rows added and one collision *split*, which is the new finding.** Added `LPM` (Learning Progress Motivation), `IGM` (Information Gain Motivation) and `COGED` (Cognitive Effort Discounting, Westbrook & Braver 2015 — expanded on exactly one page and used on three). The collision: **`CPM` already meant Connectome-based Predictive Modelling in the glossary while two pages use it for Competence Progress Motivation** — unrelated senses, no duplicate key, so `S13` cannot see it. Rewritten as a two-sense row on the `PC` precedent. **`S13` catches a repeated key; nothing catches one key with two meanings**, and this is the second such case the wiki has found |
| S7 | A concept reaching ~50% of a host page is a page — and the host need not be a page | Scan re-run. No page has a section reaching ~50% of its host, and no page is thin: the smallest is 10.8 KB ([[wiki/entities/neuromatch.md]]). **The converse case is still live and still `L36`** |
| S8 | Every cited `G`/`T` identifier has a row; no row shares an identifier | Clean, and `S14` is a hard fail at **0 uncited gap rows** for the fourth pass running |
| S9 | A number quoted against a human is incomplete without its protocol | **Extended to models by `T291`**: a capability number is incomplete without its *frame*. A null under one framing does not measure the capability |
| S10 | Every published self-count names the command that produces it — **and something runs it** | **Held, and this pass is the clean case the rule wants.** Every figure in the overview's state paragraph drifted, every one was re-derived from the command the paragraph names, and the drift was ordinary staleness rather than a check agreeing with a broken source (lint 27's failure). The third clause added last pass — *a check that reads a maintained file inherits that file's maintenance* — was not exercised, because `S20` now guards the file `S5` descends from |
| S11 | Check source integrity through `https://status.claude.com/api/v2/incidents.json`, not the status page | Held for the thirteenth pass, through the API. **Clean window**: all sixteen wave-26 ingests are dated **2026-09-20**, and the two incidents in range (2026-09-11 14:19 and 2026-09-15 09:48, both *Claude Mythos 5.1 / Claude Fable 5.1*) name neither this model nor any window containing a commit — `git log` returns nothing inside either |
| S12 | Check both registries for cross-topic duplicates by reading, not by `grep` | Run on the wave's **15** new rows (`G125`–`G129`, `T395`–`T404`) against the 484-row registry. **Zero merges.** The five new gap rows are the closest call the check has had — all five say *a self-generated motivational quantity has no update rule* — and they survive as separate rows because each names a different quantity with a different `Closes when`: decay of the reward (`G125`), its antagonist (`G126`), its storage and redemption (`G127`), the cost term opposite it (`G128`), its supply (`G129`). A merge would have to discard four criteria to keep one |
| S13 | **No glossary key may appear twice** | Clean on entry and on exit. The three rows added this pass went in by section and hit no collision — but see `S6`: the check passed on a key whose two meanings are unrelated, which is the failure it is not built to see |
| S14 | **A registry row and its carrying pages must cite each other, both directions** | Held at zero for the eleventh pass on gaps, and at **0 of 356** uncited tension rows. The `≤1`-citer tail is 8 gap rows and 39 tension rows, and **not one of them is `L3`/`L4`** for the fourth pass |
| S15 | **The queue must reconcile exactly against `raw/`** | **OK on entry at `496 + 1 + 0 = 497`, and this time the arithmetic is backed**: `S20` independently confirms no pending row is false, which is precisely what was missing when this row reported OK and was wrong at lint 27. The `comm` rewrite (`L35`) also produced no spurious *in raw/ but not queued* entry |
| S18 | **A gap's `## From` names its carrying pages; each should cite the gap back** | **3 on entry, all repaired, 0 on exit.** `G33`→[[wiki/entities/diayn.md]] (wired as a Limits row: the decomposition is obtained with no reward, graph, demonstrator or task ensemble, and the count `N` is still a hard-set integer, so the row's *selection* half is untouched), `G119`→[[wiki/concepts/state-prediction-error.md]] (the row's counter-evidence: the same transient that is inadmissible to the goal-tracker's learning is *necessary* for predictive learning elsewhere), `G129`→[[wiki/concepts/learning-progress.md]] (the row's cleanest instance — a drive whose decay is principled and whose supply therefore cannot grow). **Three passes running at exactly 3 on entry** |
| S19 | **The level-admission rule** | Clean. 108 grandfathered, **0 stale baseline entries**, and 15 new rows all admitted at `L0`/`L1`/`L2`. Three consecutive waves with no exception request — and eight passes with the grandfathered block unmoved (`L27`) |
| S20 | **A pending queue row must be *true*** | New in the tool this pass (`8e0414a`, closing `L32`). **OK on entry** at 0 pending, so the check has not yet been exercised against a real defect — its first live test will be the next wave that lands between passes. Positive evidence only, by design: a missing `ingest(<slug>)` match is not a claim that a row is genuinely pending |
| S21 | **A registry row that names another row must be named back** | New in the tool this pass (`8e0414a`, closing `L33`). **Violated on entry — 42 new one-way references, every one from wave 26**, repaired by writing the answering reference into 24 target files; 0 on exit, 0 stale baseline entries. The grandfathered block is **215** pairs and is `L37` |
