# Priority Tasks

What to read or write next. Derived from [[wiki/architectural-gaps.md]], from open problems on concept pages, and from lint passes. Reordered whenever a gap opens or closes.

## Now

| # | Task | Why | Blocked on |
|---|---|---|---|
| P4 | **Ablate the interface** — learnable hybrid unit vs fixed converter, matched architecture | *Carried.* `T235` is `LIVE — B asserted, not measured`. [[wiki/concepts/transthalamic-context-routing.md]] says a real inter-module edge carries **two cargoes on two routes**, so the thing to ablate may be the second channel | Ready to attempt |
| P12 | **Re-price `G37` against the retrieval result** | **Promoted from `Next`, and wave 13 is why.** [[wiki/entities/cn-dpm.md]] gives `G37` its first number: forgetting **0.0**, gating **48.18%** at five components and **31.14%** at twenty. `G37` is also the fastest-growing cell in the file (15,386 → **17,027**, +11%) | Ready to attempt |
| P6 | **Score one joint-embedding model on held-out *transformations*** | *Carried.* [[wiki/concepts/manifold-untangling.md]]'s criterion is strictly stronger than the linear probes seventeen entity pages report; one protocol change re-prices all of them and supplies `L6`'s missing column | Ready to attempt |
| P5 | **Report units-to-competence for anything** | *Carried.* The only admissible evidence for the spiking substrate if its advantage is unit *count* (`T234`). Derivable from existing runs | Ready to attempt |
| P22 | **Run the effective-model ladder on one wiki architecture** | **New, from wave 13.** [[wiki/concepts/multi-token-embedding.md]] supplies the wiki's first instrument that *prices* an interpretation: replace each component with the simplest stand-in, re-run end to end, report accuracy at every rung (100 → 98 → 90 → 94 → 85%). Directly runnable against [[wiki/entities/othellogpt.md]], which is `P13`(a) | Ready to attempt |
| P7 | **Run `I8` and `I9` against the wiki's own entity pages** | *Carried.* The two instruments needing nothing but the model itself. The inventory's cost ordering is still untested *as a claim* | Ready to attempt |
| P23 | **Re-score one benchmark null under a second frame** (`T291`) | **New, from wave 14, and the cheapest re-analysis in the file.** The comparative literature gets opposite answers from the same subjects under cooperative vs competitive framings. Every null in the wiki was collected under exactly one frame, so every one of them is ambiguous between *absent capability* and *undeployed capability* (`G102`) | Ready to attempt |
| P8 | **Collect a per-item human error *profile* on one benchmark** | *Carried.* Job 4 at [[wiki/concepts/human-baseline.md]] and `F4`: aggregate parity is compatible with an orthogonal mechanism (.90 vs .91 accuracy at **0.06** profile correlation) | Human curation, or a re-analysis request |
| P9 | **Build the write-mask experiment (`G52`), and build `G64` with it** | *Carried.* Still the cheapest concrete architecture proposal here. `P4` is its prerequisite. **Wave 13 supplies its baseline**: uniform reservoir sampling, which beat every priority rule tested | — |
| P10 | **Run the `g`/`x` objective search, collapse filter first** | *Carried.* `G30` remains the deepest gap. A candidate must separate a real structural code from a constant `g` ([[wiki/concepts/representational-collapse.md]]) | Ready to attempt |

## Next

| # | Task | Why | Blocked on |
|---|---|---|---|
| P24 | **Give one architecture a growth schedule** (`G100`) | **New, from wave 14, and it stopped being a metaphor.** [[wiki/concepts/developmental-heterochrony.md]] measures the shape: a **vector of per-module phase offsets** over a trajectory whose form is conserved, sparse (~4% of components), gray-matter-specific. Not "train it slower". Nothing in the wiki has any growth schedule at all | Ready to attempt |
| P11 | **Score architectures against the six hardness sources** | *Carried, unmoved for nine passes.* 118 entity pages, instrument still empty. A **facet vector**, not a scalar (`G13`, now cited from [[wiki/concepts/latent-graph-discovery.md]]) | Ready to attempt |
| P13 | **Run wave 7's two specified-and-unrun experiments.** (a) Probe/ablate OthelloGPT's end-game features (`T157`/`T158`). (b) LeJEPA rescaled-loss checkpoint ranking against a patch-token probe | *Carried.* **`P22` supplies the missing method for (a)** and `T285` supplies the reason to run it: the wiki's record says every "not decomposable" verdict so far dissolved under a better frame | Ready to attempt |
| P14 | Merge the two de-aliasing mechanisms on paper: [[wiki/entities/cscg.md]]'s frozen clone pools against path-integrated position | *Carried, unmoved for nine passes.* The clone-pool construction is the rate-level answer to element distinctness, which one spiking neuron computes in a single unit (`G80`) | — |
| P15 | **Run the `γ_effective` readout** | *Carried.* `T141` is `LIVE` and load-bearing: if the kernel is hyperbolic, `δ` is an error on a quantity with no stationary value function | Ready to attempt |
| P16 | **Instrument one existing store with a read log** | *Carried, and wave 13 gives it a number to beat.* [[wiki/entities/cn-dpm.md]]'s gate scores 31.14% at twenty components. Log *which* structure was retrieved and score the schedule separately from the contents | Ready to attempt |
| P17 | Acquire a source on **conditional or gated inductive bias** (mixture-of-experts routing, typed representations, neuro-symbolic type systems) | *Carried, and wave 13 sharpened it into a live disagreement.* `T283`: must the router be Bayes' rule over generative models, or may it be a discriminative network? Two wiki entities take opposite sides and have never been run head-to-head. Also supplies `G12`'s and `G91`'s router | Human curation or a web-search pass |
| P19 | Acquire sources for the controlled-shortcut benchmarks (Shift-MNIST / biased CelebA / unfair dSprites) | *Carried, narrowed in wave 15.* **ObjectNet and ImageNet-C are closed** — [[wiki/entities/objectnet.md]] (Barbu et al. 2019) and [[wiki/entities/imagenet-c.md]] (Hendrycks & Dietterich 2019) are paged from primary sources and supply `I26`, `I27`, `I29` and `F15`. `I1` is still the first instrument in the inventory and still has no primary source for its image half. `P6` sharpens what to read them for | Human curation or a web-search pass |
| P20 | Acquire sources for artefacts named across pages with no file in `raw/`: deep Q-network / experience replay, elastic weight consolidation, progressive networks, episodic control, Monte Carlo tree search | *Three closed in wave 15* — [[wiki/entities/dqn.md]] (Mnih et al. 2015), [[wiki/entities/progressive-neural-networks.md]] (Rusu et al. 2016) and [[wiki/entities/elastic-weight-consolidation.md]] (Kirkpatrick et al. 2017) are now paged from primary sources. *Episodic control closed too* — [[wiki/entities/neural-episodic-control.md]] (Pritzel et al. 2017) is paged from the primary source, which also carries MFEC's mechanism and numbers as its baseline. *Monte Carlo tree search closed too* — [[wiki/entities/alphazero.md]] (Silver et al. 2017) is paged, and it is a **negative** result for the framing: the graph is handed over as a perfect simulator and the state/action discretisation is declared domain knowledge, so it isolates search, valuation and policy compilation and bears on discovery not at all (`G27`, `T14`). **Still second-hand:** Model-Free Episodic Control itself (Blundell 2016) | Human curation or a web-search pass |
| P25 | Acquire a source on **edge-density scaling** (`G101`) | **New, from wave 14.** 2.75× cortical volume on 1.25× the neurons — the difference is neuropil, i.e. connections. Every scaling argument in the wiki is about units, and no result in it says what a higher synapse-per-unit ratio buys | Human curation or a web-search pass |
| P26 | Acquire a primary source on **curriculum / training-order** (`L20`) | **New this pass.** The data-schedule half of `G100`, used on 36 pages and owned by none. Candidates: Bengio et al. 2009 (curriculum learning, the artefact the term traces to) paired with a developmental input-statistics source, so the machine lever and the biological schedule land together rather than the lever alone | Human curation or a web-search pass |
| P27 | **Ingest the five clippings that arrived during this pass** | **New.** `francioni-2026-vectorized-instructive-signals-dendrites` is the primary source and must precede its two secondary treatments; the two spiking posts (`firing-rate-fallacy`, `surrogate-gradients-spikes`) bear on `T234` and on [[wiki/concepts/spike-encoding-schemes.md]]. Filed under `Unfiled clippings` in the queue, outside wave 17's ordering | Ready to attempt |
| P28 | **Write the three missing overview sections — waves 15, 16 and 17** | **New this pass, and it is the largest single piece of debt in the wiki.** [[wiki/overview.md]]'s thematic body ends at wave 14; 58 sources across three waves have no synthesis section, and their contributions currently exist only as registry rows and per-page `Connections`. That is exactly the failure mode the overview exists to prevent — the wiki can state 107 gaps and not say what the last three waves *changed about the picture*. Wave 17 is the easiest of the three to write and the most owed, since it landed the primary sources under machinery running everywhere (InfoNCE, the tensor-product binder, the information bottleneck, ICM, the disentanglement theorem) | Ready to attempt |

## Structural work

| # | Task | Why |
|---|---|---|
| ~~L15~~ | ~~**27 tension rows are cited by no page**~~ | **Closed this pass.** All 27 (`T3 T14 T19 T54 T60 T66 T70 T71 T72 T73 T74 T87 T95 T101 T102 T103 T115 T163 T181 T194 T198 T200 T243 T244 T246 T255 T268`) are now wired into the page that carries them — three by extending an existing bullet that already stated the content without the row id, the rest as a new judged bullet in that page's `Open problems` or `Limitations`. `tools/registry-index.py` now reports **0 of 302** |
| ~~L18~~ | ~~**[[wiki/entities/default-mode-network.md]] has decayed into a per-source log**~~ | **Closed this pass.** All eight paper-named sections (`Menon 2023 — …`, `Raichle et al. 2001 — …`, `Paquola et al. 2025 — …`, …) are renamed to the claim each one makes and reordered **primary evidence before synthesis**: founding measurement → medial-prefrontal tiers → state-dependent membership → cytoarchitecture → anticorrelation → the shape reading → the twenty-year review → the developmental layer. Content is verbatim; each section keeps its provenance blockquote, so attribution moved from the heading to the quote where the schema puts it. Six positional cross-references (`Buckner et al. 2008 above`, `Satpute et al. 2026 above`, `everything above`) were repaired by naming the section rather than the direction — **that repair is the real cost of the format**, and the reason a per-source page is expensive to fix later rather than merely ugly |
| ~~L19~~ | ~~**The wiki has no page for confidence calibration and 12 pages depend on one**~~ | **Closed this pass, exactly as specified.** Wave 17 landed all three queued primary sources and they produced *two* pages rather than one, which is the more useful outcome: [[wiki/concepts/confidence-calibration.md]] (Guo et al. 2017 — ECE, its free bin count `M`, and the proof that temperature scaling cannot touch the ranking) and [[wiki/concepts/selective-prediction.md]] (Geifman & El-Yaniv 2017 — the risk–coverage curve and the wiki's first distribution-free competence guarantee), with [[wiki/concepts/metacognitive-efficiency.md]] (Fleming & Lau 2014) supplying the third quantity the wiki had been conflating with both. `T181`'s "score as precision rather than ranking" now has the vocabulary it was missing, and `T320` and `T325` are the new rows the three pages opened. **The lesson is `S7`'s converse holding:** the page written from three primary sources states things no synthesis of 36 scattered mentions could have — that `M` is a free parameter nobody reports, and that MCE can move *opposite* to ECE on identical predictions |
| L20 | **Training *order* is a design variable the wiki uses and does not own** | **New.** `curricul*` appears on 36 pages and ≥3 times on eight ([[wiki/concepts/skill-acquisition-efficiency.md]], [[wiki/entities/irene.md]], [[wiki/concepts/subgraph-matching.md]], [[wiki/concepts/intelligence-density.md]], [[wiki/entities/byol.md]], …), always as an assumed lever and never as a claim with evidence behind it. `G100` covers the **capacity** schedule (a model that grows) and nothing covers the **data** schedule (what it is shown, in what order). The two are the same design question from opposite sides and `P24` needs both. **Do not write it from the mentions** — the same argument as `L19`; the acquisition target is a primary source on curriculum learning or on developmental input statistics, and it belongs on the wave-18 want-list |
| L21 | **A gap's own provenance pages do not know the gap exists — 88 such edges** | **New this pass, and it is `S14`'s blind spot rather than a new kind of decay.** `S14` asks only that *some* page cite a gap, and it has been green for three passes. The stronger relation is provenance: a gap's `## From` names the pages that carry it, and 88 of those named pages never say the id — [[wiki/entities/lejepa.md]] stated `G108`'s entire content in its second paragraph while citing `T164`/`T168` instead, [[wiki/concepts/attention.md]] carries `G15`, `G48` and `G96` and names none. So the content is right and the *handle* is missing, which is why no reading check caught it: the page is not wrong, it is unsearchable by row. Now emitted as `S18` (tracked, not enforced — a `From` entry may name a page as context rather than as carrier). One fixed this pass (`G108` → lejepa) to establish the shape; the remaining 88 are one-line insertions at a sentence that already exists, and the wiki has no equivalent check on the tension side because tension detail files have **no `## From` section at all** by schema — their sources live inline in Positions A and B |
| L6 | **Decide whether the joint-embedding entity pages should stay separate** | *Carried.* Seventeen of them, several deferring their central claim to [[wiki/concepts/representational-collapse.md]]. Not merge candidates; the shared comparison table is missing from most. `P6` supplies its one comparable column |
| L8 | **The `Connections` block is the wiki's only navigational structure and it is unweighted** | *Carried.* **6,190 edges over 276 pages**, all one type, none marked *primary*. Cheapest fix unchanged: mark one edge per page as primary. Note it is now verified bidirectional by `S1` in the script rather than by hand |
| L16 | **The glossary's section discipline is enforced only by a duplicate check** | *Carried.* `S13` catches duplicate keys; **nothing catches a row filed under the wrong heading**, which is the failure that put 64 machine-learning rows under `## Benchmarks`. Section membership is a judgement, so the mechanical proxy is the append point: entries must be appended inside their section, not at end of file. Cheapest fix: have `INGEST` insert by section rather than by append. This pass added 8 rows by section and hit the duplicate check once (`ACME`, already present 378 rows earlier) — which is the check working, and evidence the append-point discipline is not enough on its own |
| L17 | **The 405 `Closes when` fields are written and unaudited** | *Carried, and further discharged.* This pass read the **19 tension criteria wave 16 was built to adjudicate** (`T30 T37 T67 T75 T85 T122 T141 T164 T174 T177 T269 T303 T304 T305 T306 T309 T312 T313 T314`) against the evidence those 19 ingests actually delivered. **None is retirable, and the reason is uniform:** every one of them closes on an *experiment nobody has run*, not on a reading nobody has done — the wave-16 artefacts supplied the positions in sharper form and in several cases supplied a cheaper discriminator (`T85`'s light-2 omission, `T122`'s level-dependent sign, `T305`'s DirectPred least-squares control), which moves the criterion closer without meeting it. Remaining debt: the ~256 tension criteria still unread against their own evidence |

## Standing

| # | Rule | State at this pass |
|---|---|---|
| S1 | Keep `Connections` bidirectional | **13 one-way or broken edges over 20 sources — 0.65/ingest, up from 0.47.** All fixed. The distribution is the finding: **9 of 13 came from a single page**, [[wiki/concepts/independent-causal-mechanisms.md]], whose 17-entry `Connections` block was written outward in one sitting and returned by only 8 targets. So the debt is not spread thinly across a wave — it is created in bursts, by the *widest-connecting* new page, and the width is what makes it likely (a page with 17 edges has 17 chances to forget). The 14th was a different class again and the worst kind: `BROKEN` rather than one-way — [[wiki/entities/rims.md]] linked `wiki/concepts/global-neuronal-workspace.md` when the page is under `entities/`, a path that resolves nowhere and that no reader would catch. **Sub-rule:** a new page with >10 outward edges should have its reciprocals written in the same sitting, not deferred |
| S2 | Lint after ~every 20 ingests | Honoured nine times running: 19, 22, 20, 20, 20, 18, 17, 15, **20** |
| S3 | Re-run `./tools/qmd-index.sh` after each ingest | A stale index hides existing pages and causes duplicates |
| S4 | Check table rows by column count — **reset the expected width at every table boundary** | **Fired six passes running.** 10 defects across 6 files, and for the first time they are *one* class rather than two: every one is a bare `\|` inside an inline code span in a table cell — `P(Ŷ = Y \| P̂ = p)`, `max_m \|acc(B_m) − conf(B_m)\|`, `p(x_{t+k}\|c_t)`, `max_j f(x\|j)`, `k = \|i − j\|`. Two are conditioning bars and three are magnitude bars, which is the point: the *character* is the defect, not the notation it belongs to, so the rule to state is mechanical rather than semantic — **inside a table cell, every `\|` in a code span must be escaped, whatever it means**. Note also that two of the ten were in [[wiki/glossary.md]] and [[wiki/index-entities.md]] — the defect travels when a page's formula is copied into an index row, so fixing the page is not enough |
| S5 | Overview's source count is checked and exits non-zero | Violated on entry for the **ninth** time (340 vs 360). Nine for nine. But this pass found the larger half of it: the check reads two numbers and the paragraph around them was **three waves stale in prose** — it claimed to cover "waves 0–16 in sections 1–17" when the body's last section is wave 14, so waves 15, 16 and 17 have no section at all. The counts were the visible symptom of a body that stopped being written. Fixed the claim to say so honestly and opened `P28` to write the three sections |
| S5b | **Overview's tension count likewise** | Violated on entry again (312 vs 325). Both counts drift together, as at every previous pass |
| S6 | Write abbreviations the way the glossary lists them | 5 added (`ACT`, `TPR`, `RIM / RIMs`, `PRM`, `ORM`), all from wave 17 and all appearing on 4–7 pages. The selection lesson is the breadth floor: `UT` (Universal Transformer) was rejected at **1 page**, which is the first time a candidate failed on breadth rather than on being already present — an abbreviation used once is a page-local shorthand and belongs expanded inline, not in a shared file |
| S7 | A concept reaching ~50% of a host page is a page — and the host need not be a page | Demonstrated seven times, and this pass states its **converse**, from `L19` and `L20`: a term appearing on 36–58 pages is *not* thereby a page. Breadth without depth means the wiki is borrowing a word, not carrying a claim, and the fix is an acquisition (a primary source that owns it), not a synthesis of the mentions |
| S8 | Every cited `G`/`T` identifier has a row; no row shares an identifier | Clean, and `S14` is now a hard fail at **0 uncited gap rows** for the second pass running |
| S9 | A number quoted against a human is incomplete without its protocol | **Extended to models by `T291`**: a capability number is incomplete without its *frame*. A null under one framing does not measure the capability |
| S10 | Every published self-count names the command that produces it — **and something runs it** | Violated on entry again (`S5`, `S5b`). The refinement this pass: **a self-count can be right while the sentence around it is wrong**. `wave 16 (4 of 19)` sat next to a correct-looking wave list and was fifteen ingests stale; nothing in the script reads prose. Cheapest partial fix: derive the per-wave `n of m` from the queue's own checkboxes, which are already the authority `S15` trusts |
| S11 | Check source integrity through `https://status.claude.com/api/v2/incidents.json`, not the status page | Held for the third pass |
| S12 | Check both registries for cross-wave duplicates by reading, not by `grep` | Not run this pass — wave 16 is one artefact per *named existing* tension, so it creates adjudications rather than rows, and the duplicate signature (≥3 shared sources *and* ≥3 shared carrying pages) cannot form. Due again at the wave-17 close |
| S13 | **No glossary key may appear twice** | Clean, and no blank-line breaks this pass. The 5 new rows were inserted **before the `## Neuroscience` heading** rather than appended at end of file, which is `L16`'s discipline executed rather than restated |
| S14 | **A registry row and its carrying pages must cite each other, both directions** | **Held at zero for the third pass — and this pass found what it does not see.** `S14` is satisfied by *one* citing page, so a gap cited by three pages passes while the three pages named in its own `## From` say nothing. 88 such edges (`L21`, now emitted as `S18`). Green here is a weaker claim than it reads |
| S15 | **The queue must reconcile exactly against `raw/`** | **Fired twice mid-pass, correctly, on five real files.** `340 + 1 + 22 = 363`, then `+3 = 366` when three more clippings landed while the lint was running. All five were freshly-clipped and unqueued, and all five arrived with **prose filenames containing spaces** (`The Firing Rate Fallacy.md`), which no other file in `raw/` has. Renamed to the `author-year-topic` convention and queued as `Unfiled clippings`. The check earns its keep: it is the only thing standing between a clipped file and silent non-ingestion |
| S18 | **A gap's `## From` names its carrying pages; each should cite the gap back** | **New this pass, tracked not enforced.** 89 on entry, 88 after wiring `G108` into [[wiki/entities/lejepa.md]]. Not promoted to a hard fail: a `From` entry legitimately names a page as *context* as well as *carrier*, and the two are not separable mechanically. The tension registry has no equivalent — its detail files carry no `## From` at all, their sources living inline in Positions A and B, which means **the provenance relation is checkable on 107 gaps and on none of the 325 tensions** |
