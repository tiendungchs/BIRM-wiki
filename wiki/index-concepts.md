# Index — Concepts

Every page in `wiki/concepts/`. One line each: what the concept *is*, and what it is load-bearing for. Updated whenever a concept page is created.

| Page | What it is | Load-bearing for |
|---|---|---|
| [[wiki/concepts/latent-graph-discovery.md]] | **CORE PROBLEM FRAMING.** Infer a hidden relational graph from observations, then navigate it | Everything — the problem every other page answers a piece of |
| [[wiki/concepts/neuroscience-ai-transfer.md]] | The methodological premise: import Marr levels 1–2 from the brain, treat plausibility as a search heuristic | Deciding which biological mechanisms are worth importing; the track record of what has actually transferred |
| [[wiki/concepts/complementary-learning-systems.md]] | Fast sparse hippocampal store + slow distributed cortex, coupled by replay | The biological derivation of the slow-**W** / fast-**M** split; de-aliasing via sparse conjunctive codes |
| [[wiki/concepts/meta-learning.md]] | Slow outer loop over a task distribution shapes a fast inner learner | The optimization statement of the meta-graph / instance-graph hierarchy; the rival (activity-based) fast level |
| [[wiki/concepts/continual-learning.md]] | Learn task sequences without catastrophic forgetting; importance-gated plasticity | The write mechanism for slow **W** — how a meta-graph accumulates across environment families |
| [[wiki/concepts/working-memory.md]] | Active maintenance with control separated from storage | Fast **M** as a controller-addressable store; the only architecture here doing explicit multi-hop traversal |
| [[wiki/concepts/memory-read-and-erase.md]] | The store's **access protocol** rather than its carrier: reads are scheduled events with an addressee and a lead time, and removal is a typed family (`replace` / `suppress` / `clear`) with a relevance-addressed clear | The controller/store interface — the output format a controller must emit (address + lead time + removal type), and the home of the G48/G49/G60 cluster |
| [[wiki/concepts/attention.md]] | Select a subset of input or memory per step; content-addressable retrieval | Read access to memory; multi-hop retrieval; attention weights as a soft one-step adjacency |
| [[wiki/concepts/priority-map.md]] | Selection for action as a map over locations valued by similarity to a held template, with the similarity (ventral prearcuate cortex) and the argmax (frontal eye field) computed in different areas | The two stages `softmax(qKᵀ)V` fuses; a query register that survives an action; a causal double dissociation of feature vs. spatial selection reaching down into V4; two separately ablatable write-ports onto one sensory population; query-volatility as a benchmark axis; the query as a *chosen* variable — a learned associate can occupy the template while the target is evicted |
| [[wiki/concepts/cognitive-control.md]] | A task model held as sustained activity, broadcast as excitatory bias into the systems it controls | Why the control variable is activity and not weights; abstract rules as reified objects (G8); where a capacity limit comes from |
| [[wiki/concepts/control-unity-and-diversity.md]] | The controller factors into one common component plus operation-specific ones (updating, shifting), with no inhibition-specific component | How many separately parameterized pieces a control module needs (G55); the stability–flexibility anticorrelation; task impurity as a benchmark-design flaw |
| [[wiki/concepts/policy-abstraction-hierarchy.md]] | A rule is one order more abstract when its output names another rule rather than an action; the orders are laid out rostro-caudally in frontal cortex and searched **in parallel** from the first trial, each level engaged only while it has a live decision | Where an abstraction layer sits and what engages it (G8, G12, G58); the wiki's only structure-present/structure-absent benchmark spec; per-edge attribution as a transfer test; the two disengagement triggers — no structure to find at this order, and no choice left to make |
| [[wiki/concepts/simulation-based-planning.md]] | Roll an internal model forward and evaluate imagined outcomes | The *use* half of LGD: path search over the estimated graph |
| [[wiki/concepts/abstract-structural-codes.md]] | Codes for position in a relational structure, invariant to content — the wiki's `g` | Far transfer, path consistency, state-space decomposition for hierarchical planning |
| [[wiki/concepts/biologically-plausible-credit-assignment.md]] | Local, weight-transport-free approximations to backpropagation | Whether the slow-**W** learner is trainable in a neural substrate at all |
| [[wiki/concepts/shortcut-learning.md]] | The default failure mode: a rule that fits the training and i.i.d. test data but not the intended structure | Why spurious edges survive (hardness 5); the four inductive-bias levers; why only out-of-distribution testing measures anything |
| [[wiki/concepts/skill-acquisition-efficiency.md]] | Intelligence as the rate at which priors and experience convert into skill at unseen tasks, weighted by generalization difficulty (AIT formalism) | The wiki's scoring function: what a benchmark must measure before any architecture's hardness-table row means anything (G17, G31) |
| [[wiki/concepts/synaptic-plasticity.md]] | Weight updates computed from pre/post activity plus at most one broadcast modulatory signal — Hebb, STDP, three-factor, node perturbation, eligibility traces | The write rule for fast **M**; the only family that learns *during deployment*; where delayed sparse credit becomes reachable |
| [[wiki/concepts/memory-allocation-excitability.md]] | A cell-wide, CREB-dependent excitability tag decaying over *hours* that selects **which** neurons a memory is written into, while synapses store **what** | The wiki's only allocation prior keyed on encoding *time* rather than input similarity; the allocate-to-link mechanism that writes relations between episodes never co-experienced; a two-signal write gate (dendritic plasticity × somatic firing) for G19 |
| [[wiki/concepts/engram.md]] | The memory trace as an addressable *set* of neurons — tagged at encoding by immediate-early genes, necessary and sufficient for recall, size-conserved, brain-wide, and linked to other traces by population **overlap** | The wiki's only edge substrate with no edge table: relations stored as intersections inside the node codes, written either by co-allocation in time or by repeated co-retrieval, and erasable without touching the memories they relate |
| [[wiki/concepts/meta-optimized-plasticity.md]] | The learning rule itself as the object of an outer optimization — differentiable, evolutionary, or self-referential | Slow **W** as plasticity coefficients rather than a parameter blob; the first candidate third tier (G9); the activations-as-weights bridge |
| [[wiki/concepts/core-knowledge.md]] | A small set of innate, domain-specific, entry-gated systems of abstract concepts — objects, agents, number, geometry, social partners (plus forms in the later inventory) — each identified by its signature limits | The architecture lever made concrete: what a pre-installed meta-graph contains, how to certify a prior by its failure profile, and why its fragments cannot be composed |
| [[wiki/concepts/predictive-coding-free-energy.md]] | One residual-minimising rule for perception, learning, action and thought, plus three typed prediction channels as the structural prior it needs on top | The fast-**M**/slow-**W** split derived from inference; the perception→simulation switch; concepts as attractors and composition as relaxation; the laminar cell-class assignment and the derived gamma-up / beta-down frequency asymmetry |
| [[wiki/concepts/expected-free-energy.md]] | The active-inference *planning* objective: score imagined futures by distance from a preference distribution plus remaining uncertainty — a linear latent-MDP reward plus one convex term, the negative state-marginal entropy | Making curiosity a derived gradient rather than a tuned bonus; the optimisation class of active-inference planning (convex MDP, `O(1/K)` mirror descent, no global value function); the performative structure of any agent that refits its model to its own data (G25); and the ρ-POMDP identity that fixes the exploration weight at `w = 1` without tuning |
| [[wiki/concepts/epistemic-value.md]] | The value of an action that changes only what the agent knows — four incompatible quantities (`E1`–`E4`) circulating under one name, defined over the state marginal, the belief, the parameters and the observation kernel respectively | The measurement half of expected free energy: four ablation literatures, the finding that the term which pays (parameter novelty) is the one both derivations exclude, and the audit that separates an information gain from a self-consistency penalty (T124, T125) |
| [[wiki/concepts/ignition.md]] | An all-or-none commit on a shared bus: a sub-threshold sensory wave either decays or, ~200–300 ms in, triggers a self-amplifying network-wide reverberation that seizes a long-range population, suppresses its competitors and stays live — with a slow, voltage-dependent (NMDA) feedback stage supplying the non-linearity | The wiki's only *discrete, dateable* "this is the system's current content" event, i.e. a step boundary for serial computation over parallel modules; the identity of a decision criterion with a network bifurcation point (and of bias with pre-stimulus gain); the deferred commit a 900 ms retro-cue proves exists; the admission gate with no learning rule (`G91`) |
| [[wiki/concepts/cortical-state-bistability.md]] | A content-free hold/release variable with its own dynamics: a 20–40 Hz beta regime that synchronises whichever ensemble currently holds the content, antagonised by a 1–9 Hz regime that dissolves that synchrony — the release ramps ~200 ms **before** an internally generated content switch and free-runs at the same rate with no input at all | Separating "what is held" from "may it be replaced", which every wiki architecture fuses; a step boundary predictable in advance from a content-free signal; the sub-threshold commit (piecemeal) as a namable failure state; handover by phase offset as a weight-free scheduler; the cheapest instance of an intrinsic mode (`G90`) |
| [[wiki/concepts/loopy-belief-propagation.md]] | Message passing run on a graph with cycles: wire several autoencoders together at their low-dimensional bottlenecks and iterate local messages, and the approximate joint posterior that results is a turbo-code — the near-Shannon-limit decoder rediscovered as a model of multi-modal integration, with synchrony as its scheduler and the MAP estimate as what gets broadcast | Fusion without a fusion module; a commit criterion that is convergence rather than a threshold; inference-time compute that improves the *estimate* on a fixed graph |
| [[wiki/concepts/precision-weighting.md]] | Precision (inverse variance) as a third represented quantity alongside states and parameters, carried by synaptic gain — and the taxonomy of probabilistic neuronal codes that decides how a posterior is represented at all | Why free energy is minimised at all (the ergodicity/existence argument); attention, salience, value and learning rate as one inferred number (T122); a normative set-point rule for gaps G50 and G56; goals as priors over sensory trajectories, deleting the value function |
| [[wiki/concepts/reward-prediction-error.md]] | One broadcast, unaddressed scalar `δ = r + γV(s′) − V(s)`, consumed at once as a plasticity term and as a gain | Where the wiki's four incompatible readings of the dopamine signal are stated side by side; the address problem (G19) and the two mechanisms that answer it |
| [[wiki/concepts/event-segmentation.md]] | Cutting a continuous stream into events, boundaries, schemata and episodes by watching which predictive encodings are active | Where discrete nodes and typed edges come from at all (gap G27); the coarse level for jumpy planning |
| [[wiki/concepts/linear-representation-hypothesis.md]] | Features as *directions* in activation space: state is a superposition `R = Σ a_i v_i`, reads are projections, writes are additions `R' ← R + α p_d` | Why probing is capacity-free and interventions cost one `axpy`; the residual stream's linear decomposability, which every mechanistic-interpretability instrument depends on; the basis problem — "linear" is a property of the pair (representation, feature vocabulary), not of the representation |
| [[wiki/concepts/counterfactual-probing.md]] | Extract structure from a trained generative model by intervening on its *input* at one point and clustering the distribution of responses — objecthood as a statistic over imagined interventions | Discovery without labels (G17); interventional data with no agent; the spatial half of G27; the **conditioning interface** as a fifth item in a world model's specification |
| [[wiki/concepts/affordance-grounded-symbols.md]] | A symbol as an equivalence class of situations sharing an *effect* under the agent's own actions; learned by a binary bottleneck inside an effect predictor, distilled to PDDL rules | The one discretisation criterion (G27) that is not computable by a passive observer; grounded primitives with built-in entry tests (G23); affordance models read as discrete world models |
| [[wiki/concepts/compositionality.md]] | New representations built by combining primitives — parts, sub-parts, relations — so a finite vocabulary generates an unbounded concept space | Productivity of the meta-graph; why binding can be one-shot; parsing as a cheap probe for real intermediate nodes |
| [[wiki/concepts/causal-model-building.md]] | Learning as construction of a model whose steps resemble the process that generated the data, rather than a rule that predicts it | What a recovered edge is supposed to *be*; the richness criterion as a shift-free evaluation protocol; the sample-efficiency numbers |
| [[wiki/concepts/amortized-inference.md]] | Compile slow structured inference into a fast feed-forward mapping — and plans into cached values | How a structured model can be queried in real time; the positive role of model-free machinery; arbitration as a second answer to *when to plan*; the uncertainty rule that makes that arbitration computable |
| [[wiki/concepts/universal-induction.md]] | The `2^-(program length)` mixture over all computable hypotheses — the maximally general inductive bias | The formal ceiling on what any bias can buy; the passive/active split that says structure discovery is well-posed for an observer and ill-posed for a participant |
| [[wiki/concepts/energy-based-models.md]] | A scalar compatibility function as the fundamental object; inference and reasoning are both minimisation over free variables | Representing one-to-many transitions without predicting them; contrastive vs. volume-minimising training; edge labels as capacity-bounded latents |
| [[wiki/concepts/program-induction.md]] | Rival one-problem reduction: reasoning as posterior search over compositions of primitives from a library, answered by executing the winning program | The dictionary between programs and paths and the two places it breaks; the four standing costs (authored library, intractable search, a length prior known to be wrong, one bit per rejected candidate); where G4 and G26 came from |
| [[wiki/concepts/test-time-training.md]] | Adaptation as a weight update at inference: fine-tune on the single instance's own demonstration pairs, producing a throwaway per-task model, then read the answer out of it (transduction) | The measurement that frozen deep learning cannot reach a developer-novel task (≤11% vs 53.5% on ARC-AGI-1); the memorization/recombination spectrum against program search, and the fact that the two solve disjoint task sets (G12); the authored prior relocated from a DSL to an augmentation group; instance structure that is never written back (G14) |
| [[wiki/concepts/refinement-loop.md]] | Per-task iterative improvement of a candidate (weights, program, prose, chain of thought) under a feedback signal computed from that task alone — the claim that TTT, evolutionary program synthesis and CoT self-correction are one mechanism in four substrates | The mechanism behind every 2025 ARC-AGI-2 score; the zero-pretraining results that bear on G35; SOAR as the wiki's first measured consolidation channel of this shape |
| [[wiki/concepts/external-verification.md]] | Pair a generator with an acceptance test independent of its training objective; the ladder of tests from final-answer matching to a proof kernel | The component every proposer page assumes and none supplies; inference-as-search and its price; why answer accuracy and proof completeness are different competencies; the three conditions a verifier must meet (T180, T181, G68) |
| [[wiki/concepts/language-of-thought.md]] | Rival one-problem reduction: thought as recursively combinable typed expressions, with a distribution over them in the probabilistic version — formally, the λ-calculus plus one random-choice operator, in which conditioning is itself definable | The non-embeddable symbolic slice; productivity and systematicity as diagnostics; the installed-vs-acquired dilemma (T8) and the equivocation problem a builder inherits from it; the universality thesis that makes expressivity free and puts every commitment in the prior; `mem`/`gensym` as variable creation on demand (G69); nested `query` as theory of mind (T183); counterfactual = condition the trace, intervene, re-execute |
| [[wiki/concepts/nameability.md]] | How easily a task's rule can be put into words, measured as description length and as within-task naming divergence — and found to predict how hard the task is to solve | The cheapest per-task difficulty index the wiki holds (G31), a prior over hypotheses whose units come from outside the solver's DSL (G26), and the evidence that human hypothesis generation is scaffolded by natural language rather than by a compact primitive set |
| [[wiki/concepts/equilibrium-propagation.md]] | The learning rule an energy implies: two relaxations, free and nudged, differenced in weight space — local, no weight transport, no backward pass | The wiki's most-evidenced biologically-motivated credit assignment (13.23% top-5 on full ImageNet), and the substrate on which a structural objective could be hung as a free external potential (G30) |
| [[wiki/concepts/three-component-framework.md]] | The design specification: objective function + learning rule + architecture, with the computation left to emerge from their interaction | Which slot any proposed mechanism occupies; the compactness (genome-bottleneck) argument; bias/variance scoring of candidate learning rules |
| [[wiki/concepts/prediction-compression-equivalence.md]] | Log-loss training *is* minimum code length; arithmetic coding converts any predictor into a lossless compressor and back | The computable shadow of `K(µ)`: a labels-free, task-free instrument for how much structure a model found — and the evidence that excellent code length coexists with no recovered graph |
| [[wiki/concepts/intelligence-density.md]] | `ℐ = log₂N/C` — independent outputs per bit of description; *knowing* is `ℐ(n) → ∞` as the domain grows, *memorizing* is `ℐ(n) → 0` | The dual of code length: scores the system alone, with no stream, task, reward or curriculum; makes recurrence a necessary condition for knowing (G36, T20) |
| [[wiki/concepts/divergence-objectives.md]] | Surprisal → entropy → cross-entropy → KL; the ladder that defines almost every objective in the wiki, and the asymmetry that decides what a fitted model does with ambiguity | The objective slot's default occupant and its hidden mode-covering preference; the formal statement of why the loss lever alone cannot separate a shortcut from the intended rule (G16) |
| [[wiki/concepts/subgraph-matching.md]] | Decide whether a query relational pattern occurs inside a larger stored graph, and where — NP-complete exactly, made a coordinate comparison by embedding graphs so that containment *is* the geometry | The wiki's third graph operation beside discovery and navigation: schema retrieval, entry conditions, event-schema preconditions and analogy all reduce to it (G37) |
| [[wiki/concepts/vector-symbolic-binding.md]] | Bind role to filler by circular convolution and superpose by addition, so a nested relational structure is one fixed-width vector whose dot product with another estimates *structural* similarity | The wiki's binding operator: the join for the `g`/`x` split, a fixed-width instance-graph format with an inverse, and the demonstration that a structural property can be made linearly readable by re-encoding rather than by a structural comparator (G37, G40, G69, T173) **Now also the continuous case** — fractional binding (Spatial Semantic Pointers) extends the same operator to real-valued features, where binding is addition, the origin is the identity, and similarity is a kernel with a tunable bandwidth. |
| [[wiki/concepts/analogical-mapping.md]] | Return the element-by-element correspondence between two relational structures, then transfer and validate the propositions one has and the other lacks — by `argmax` of a dot product over role-augmented sparse binary codes rather than by constraint-satisfaction search | The step every schema mechanism assumes and none performs: G37's second stage taken from `O(n⁴)`–`O(n!)` to `O(n²)`, plus the wiki's only memory-internal acceptance test for a candidate inference (T186, T187) |
| [[wiki/concepts/contextual-inference.md]] | One posterior over "which context am I in" controls memory creation, expression and updating at once; behaviour change splits into *proper* (a stored state moved) and *apparent* (only the mixing weights moved) learning | The retrieval-and-allocation policy the framing assumed (G37, G27): a graded posterior over a growing library plus a nonparametric rule for when to allocate a new memory — and the reason an adaptation curve cannot certify learning (G17) |
| [[wiki/concepts/perturbation-elicitability.md]] | Inject current at one site and ask whether reportable content changes; the rate falls from ~67% in unimodal cortex to 0% at the frontal pole, and the fall reads out coding scheme rather than hardware | The causal complement to probing (G17): perturbability as a proxy for abstraction, the robustness/steerability trade-off a distributed code buys, and the reason a substrate-basis edit fails where a code-basis edit succeeds; the evidence behind T269 |
| [[wiki/concepts/representation-probing.md]] | Fit a deliberately weak decoder from internal activations to a hypothesised structure, then ask separately whether the network uses what the decoder found | The wiki's only *internal* evaluation instrument (G17): it locates a graph estimate by layer and token, makes `g`/`x` and path-consistency measurable, splits an end-to-end score into decoded structure vs. behaviourally expressed structure, and carries the direction-of-inference discipline every selectivity claim needs (head-to-head posteriors, not activation-under-condition) |
| [[wiki/concepts/violation-of-expectation.md]] | Score a model by whether it finds the impossible continuation of a shared history more surprising than the possible one — no labels, no shift, no calibration | G17's cheapest instrument and the only one that can register an *inverted* rule; the protocol behind every IntPhys / BIB / AGENT number in the wiki, its four free parameters, and the untrained-network null that makes any of them interpretable |
| [[wiki/concepts/inhibitory-control-of-coding.md]] | Coding *features* — stability, selectivity, context generalization, information — set by four separable inhibitory channels acting on one excitatory population, with each channel's breadth of fan-in deciding which feature it owns | The actuator for G38's separation/completion bias, verified causally per feature; a fan-in-width rule for G40's factorise-vs-entangle decision; a within-traversal input router; and a unit-taxonomy instrument for interpretability |
| [[wiki/concepts/pattern-separation-completion.md]] | Separation and completion as two ends of one *transfer function* from input similarity to output similarity — dentate gyrus steep, CA3 shifted, CA1 linear — with anatomically separate write and read ports into the same recurrent store | The allocate-vs-reuse decision made by degree rather than by test: de-aliasing (G2) and retrieval (G37) as one tunable knob, plus what goes wrong when the knob is stuck (G38) |
| [[wiki/concepts/encoding-retrieval-alternation.md]] | Encoding and retrieval as opposite half-cycles of a free-running 4–8 Hz oscillation rather than a mode the controller selects — and the two half-cycles reused as the minus/plus phases of an error-driven learning rule | A controller for G38 that needs no error signal or neuromodulator, only a clock; the phase signal every two-phase local learning rule is missing; and the only mechanism in the wiki that makes a fast store write what it got *wrong* |
| [[wiki/concepts/cognitive-map.md]] | A relational map of an environment decomposed into three operations — spatial coding, *anchoring* to perceptual invariants, route planning — and the evidence that all three run in humans and extend to concept, social and discrete spaces | The framing's home domain and the source of its missing middle operation (G39): which map applies (context retrieval) and where on it the agent is (orientation), plus the neural read-outs of path search |
| [[wiki/concepts/nonspatial-maps.md]] | The map's evidence base outside physical space: fourteen domains with grid-like or distance codes (concept, social, discrete-graph, word, rank, gaze, accumulated evidence), four of them cellular, against one matched control that returns a null | Whether latent-graph discovery is one mechanism or a spatial mechanism plus analogies; the asymmetry that decides `G39` — the code and the search generalise off the plane and **anchoring has no non-spatial instance at all** |
| [[wiki/concepts/path-integration.md]] | Maintain position by accumulating actions, so identity comes from the moves that reached a state rather than from what is observed there | The mechanism (not brainstorm) for path-consistency of `g` (G3), the compression argument for structural codes, the precondition it needs (G41), and the determinant of the code's *scale* (the prediction horizon between memory writes) |
| [[wiki/concepts/distributed-reference-frames.md]] | Grid-like location coding as a *replicated* cortical primitive — one reference frame per area, anchored to objects rather than to the world — instead of one central map | Whether `g` is a single code or thousands needing consensus (G43); the widest evidence base for grids outside space, together with the detector caveat that undercuts it (T37) |
| [[wiki/concepts/successor-representation.md]] | State represented by discounted future occupancy, `S = ΣγⁿTⁿ`, `v = Sr`; rows resemble place fields, eigenvectors resemble grid cells | The cached middle between model-free and model-based; a second, spectral derivation of grid-like codes; one eigenbasis serving planning, exploration and replay; the temporal symmetry of the local write rule decides whether the stored map is the policy or the geometry |
| [[wiki/concepts/offline-replay.md]] | Compressed reinstatement of hippocampal sequences during rest — filtered, resampled and sometimes never observed, so replay *selects* rather than plays back | The content of the consolidation channel (G14); the only mechanism that edits the experience stream from inside the agent (G32); five jobs competing for one substrate |
| [[wiki/concepts/population-geometry.md]] | Population activity as a point on a low-dimensional curved surface; what a circuit represents is the *shape* of that surface and how task variables lie on it, not the tuning of any cell | The instrument for the level [[wiki/concepts/objective-identifiability.md]] licenses: intrinsic dimensionality, held-out reconstruction, cross-subject rotation alignment, cross-condition generalization / parallelism / shattering dimensionality with their defining source and its random-geometry null (Bernardi et al. 2020), and an input-geometry control — plus the two measurements of G40 (entangled at the cell, factorised at the population; abstraction and expressiveness rising together) and the wiki's only record of an abstract code *forming*, including by verbal instruction (G45) |
| [[wiki/concepts/manifold-untangling.md]] | All images of one object form a low-dimensional identity manifold; early stages leave those manifolds curved and interleaved so no hyperplane separates them, and the cascade's job is to flatten and *align* them until one weighted sum does — information reformatted, never created | The wiki's per-layer, fixed-reader success criterion (score identity decodability on held-out *transformation* conditions, and require it to rise with depth); the vision-side twin of CCGP; the three proposed mechanisms, of which temporal-contiguity learning is a rare objective-slot transfer with in-vivo causal evidence — and the gap it opens, that machine self-supervision has its nuisance group hand-declared by an augmentation list where biology infers it from the input's own time structure (G95) |
| [[wiki/concepts/manifold-constrained-learning.md]] | What a population can learn to do in hours is bounded by the co-modulation structure it already has: readouts inside the existing low-dimensional manifold are learnable, ones requiring new co-modulation patterns are not — and inside the manifold the activity repertoire is preserved too, so fast learning is re-association of intents with existing patterns | The causal upgrade of [[wiki/concepts/population-geometry.md]] — a fitted manifold predicts an unseen manipulation's outcome; the mechanistic content of the `priors` term in [[wiki/concepts/skill-acquisition-efficiency.md]] and a computable off-span proxy for generalization difficulty (G31); the behavioural cost of a wrongly imposed manifold topology (G47); the fast level as a rewritten lookup rather than a retrained representation, with a predictable performance ceiling (T79) |
| [[wiki/concepts/objective-identifiability.md]] | Many losses share a minimum and one loss has many minima, so neither the objective nor the solution is recoverable from the other; emergence claims need an inductive-bias audit | The wiki's licence-check on every "X emerged from task Y" result (G44); the population-vs-single-unit line for what a task-trained model may predict; the candidate contents of the empty objective slot |
| [[wiki/concepts/sparse-distributed-representations.md]] | Binary codes of dimension `n` with 0.5–3% active bits, whose payload is the arithmetic of overlap: a ~25-bit random subsample identifies a pattern out of 10⁶ at a false-positive rate below 1 in 10⁹, and a Boolean-OR union tests set membership in one thresholded dot product | The recognition (as opposed to capacity) account of why every fast store in the wiki is sparse; the closed-form price of de-aliasing (hardness source 3); the wiki's only `O(1)`-in-set-size membership primitive |
| [[wiki/concepts/dendritic-computation.md]] | The neuron as ~100 independent nonlinear coincidence detectors — each a dendritic segment firing an NMDA spike on 8–20 co-active clustered synapses — rather than one thresholded sum | What a "unit" is when counting capacity or comparing network sizes; a derived-and-confirmed parameter in the architecture slot; the branch-level target any local credit rule must reach |
| [[wiki/concepts/temporal-coding.md]] | Timing as the message: precision finer than any time constant in the circuit, obtained by threshold-crossing on a coherent volley over Hebbian-selected axonal delays | The quantitative case for T1 (20–25 µs output from 250 µs postsynaptic potentials); a teacher-free latent-variable estimator; the two parameters of an STDP window that actually matter (peak at `−t_r/2`; width bounds nothing) |
| [[wiki/concepts/spike-encoding-schemes.md]] | The interface before every spiking architecture: fourteen ways to turn a continuous signal into spikes, split by one question — is the exact time and order load-bearing? | The unowned design choice at the input of every SNN in the wiki; the capacity arithmetic (`log₂(N+1)` count rate vs `log₂(N!)` rank order) and the jitter cost that cancels it; the reference-origin gap G77 |
| [[wiki/concepts/spike-frequency-adaptation.md]] | One extra slow state variable per unit — recent firing raises the neuron's own threshold, so the inter-spike interval grows under sustained drive | A working-memory store that needs no recurrence, no persistent firing and no retrieval operation (1200 ms held by time constants of 30/300 ms); the same equation the wiki elsewhere uses to *destroy* persistence (T233); the substrate detail that *fixes* a gradient problem instead of causing one |
| [[wiki/concepts/learnable-synaptic-delays.md]] | The axonal delay `d_ji` as a trained parameter: exact event-based gradients for weights *and* delays on one adjoint backward pass, in feedforward and recurrent spiking networks | `G80` — the wiki's most expressive per-parameter variable finally has an optimiser; the first temporal parameter set by task error rather than by sweep; order detection ("A then B") as a wiring property rather than a sequence model |
| [[wiki/concepts/neuronal-parameter-heterogeneity.md]] | Every unit gets its own biophysical constants `[τ, γ, C, u_th, u_re]`, learned per neuron in a bi-level program separate from the weight loop | The first mechanism in the wiki that *sets* an adaptation time constant instead of sweeping it (G78); the reason spike-native networks now beat conversion past MNIST (T231); a learned parameter that lands on the measured biological distribution |
| [[wiki/concepts/analog-in-memory-computing.md]] | Weights as device conductances in a crossbar, the matrix–vector product done in place by Ohm's and Kirchhoff's laws — reads nearly free, writes expensive, stochastic and ~4-bit | The one cost model in the wiki where an inner loop is priced in *devices written* rather than gradient steps; the substrate argument for restricting adaptation to a delta rule on one layer (1120 of 342,720 devices); the finding that software-only meta-training transfers to the chip with no hardware model at all |
| [[wiki/concepts/circuit-size-separation.md]] | Scoring a unit type by *how many of it* a function costs: one spiking neuron computes coincidence detection and element distinctness that provably need `Ω(n)` sigmoidal hidden units (`≥1663` at 10,000 synapses), while the two classes stay mutually simulable | The only rigorous statement of what the spiking substrate buys — a density claim, not an expressibility one (T1, T234); pulse *shape* as a hard computational boundary; delays as strictly more expressive parameters than weights (`Θ(n log n)` vs `Θ(n)`) |
| [[wiki/concepts/canonical-cortical-microcircuit.md]] | The conserved laminar wiring diagram of neocortex — thalamus → L4 → L2/3 → L5 → L6 → L4, feedforward out of L2/3, feedback out of L5/6 into layer 1 — plus the two inhibitory geometries that act on it and the two rival functions proposed for the superficial/deep split — explore-then-commit, or prediction-error-then-expectation | The circuit the column-voting and cortical-uniformity arguments presuppose; the wiki's only named cortex-wide arbitration mechanism (iterated soft winner-take-all with a dendritic gating channel); typed edges (driver vs. modulator) and a measurable continuous hierarchy depth (SLN%); the demonstration that <10% of synapses can drive a recurrent circuit; the cell-class assignment of every term in the predictive-coding equations, and the two intrinsic connections it predicts but anatomy has not reported |
| [[wiki/concepts/transthalamic-context-routing.md]] | Every direct cortico-cortical edge is mirrored by a disynaptic one through higher-order thalamus, and the two carry different cargo: the direct edge the *content*, the transthalamic edge the sender's *state* — glutamatergic, area-addressed, target-dependent | The wiki's only per-edge context channel, and the counterexample to "a global quantity must be broadcast diffusely"; the driver/modulator test in importable form (time-locking, not rate, tracks causal influence); the demonstration that "behavioural state" is at least two independently routed variables (arousal ≠ locomotion) |
| [[wiki/concepts/predictive-adequacy.md]] | The standard for saying a description of a component is *right*: predict responses to arbitrary inputs, score on held-out data as a fraction of the noise-ceiling-corrected (**explainable**) variance, and freeze every parameter before changing the input distribution | The wiki's cheapest out-of-distribution instrument and the only one that certifies the *description* rather than the system (no ground truth needed); the measurement ladder that makes scores comparable across preparations; the finding that a fitted receptive field is a property of a (system, input distribution) pair — excitatory tuning stable across probe distributions, suppressive tuning not; the depth gradient of adequacy (retina ~81% → V1 ~40% → V4 ~10% of explainable variance) and the residual-type diagnostic that goes with it |
| [[wiki/concepts/random-feedback-addressing.md]] | Near-random reciprocal connectivity as a top-down addressing scheme: a mixed-selectivity controller delivers feature-precise feedback because off-target components are uncorrelated with the target population's tuning axis and cancel under balanced weights (`ΣW = 0`); a concentration parameter κ dials from precise-but-parochial (κ = 0) through global feature priming (κ ≈ 0.1) to illusory percepts (κ ≥ 0.2) | How a controller aims feedback with no address space and no decoder — the delivery mechanism [[wiki/concepts/attention.md]] and [[wiki/concepts/priority-map.md]] assume; the cost of mixed selectivity on the output side; the caution that within-task decoding accuracy says nothing about representational format |
| [[wiki/concepts/attractor-dynamics.md]] | Recurrent settling onto weight-defined fixed points, typed by *what defines them* (content / scaffold / orthogonalised / masked) and *how many* (discrete / continuous / sequential) | The one operation that serves retrieval, de-aliasing, maintenance and path integration at once; the node set the graph framing presupposes, and no edges |
| [[wiki/concepts/autonomous-pattern-generation.md]] | Emit a precise extended temporal signal when the input carries none of the target's structure — the reservoir answer being a random untrained recurrent net kept alive by a periodic driver, with only a linear readout trained | The *execution* half of latent-graph discovery: how a long structured output is produced from an internal store; the proof that a fading-memory substrate can generate forever but only phase-locked to its clock, hence the exact demand a run-time-conditioned sequence generator makes (G67) |
| [[wiki/concepts/recall-gated-consolidation.md]] | Gate the slow store's write on whether the fast store already recalls the proposed update — `Δw_LTM ∝ g(w_STM · w*)`, with `g`'s argument realised as prediction accuracy, decision confidence or familiarity | The valve on the consolidation channel (G14); a write-gate for slow **W** needing no task boundary, importance estimate or exemplars; the proof that population gating beats *any* synapse-local consolidation under bursty reinforcement |
| [[wiki/concepts/generalization-optimized-consolidation.md]] | Consolidate a relation only while transport lowers *generalization* error; the stopping point is set by how predictable that relation is for the slow learner | The sign condition on the consolidation channel (G14) — zero transport is optimal for unmodellable experience; the wiki's first built fast→slow transport loop (self-distillation from notebook reactivations); why the fast store must be permanent |
| [[wiki/concepts/schema-assimilation.md]] | Prior knowledge as an organized network defined by three tests (fit-boosted encoding, modification under challenge, inference between indirectly related elements); assimilation adds to it, accommodation rewrites it | Re-indexing consolidation by *overlap with stored structure* rather than by elapsed time; the causal separation of retrieval from selection (G37); a schema-membership criterion for the separate-vs-integrate knob (G38) |
| [[wiki/concepts/arbitrary-sensorimotor-mapping.md]] | An edge from a cue to an action whose endpoints share no spatial, perceptual or causal relation — so it carries no information but its own existence and can only be stored, never computed | The zero-transfer edge class (G11's symbolic slice); the wiki's one existence proof that an edge can be *represented* rather than wired (≈50% of prefrontal cells code the pairing); exemplar / higher-order rule / strategy shown separately lesionable; abstraction as a rate multiplier that cannot change the exponent; why a two-alternative binding benchmark measures strategy, not binding |
| [[wiki/concepts/controller-knowledge-vs-process.md]] | Whether the control layer stores knowledge of its own (goal-structured event sequences, partitioned by content category and by how much of the order is committed) or is a contentless procedure over posterior stores — with "process" reduced to a set of representations held active | Deciding whether a machine control module has parameters at all; the rubric that scores eight prefrontal theories against one another; the partial-order plan format nothing in the wiki implements |
| [[wiki/concepts/inter-areal-synchrony.md]] | What a measured correlation between two modules licenses: gamma-band coupling indexes direct synaptic drive on the link, theta-band coupling indexes a clock supplied by a third region, and the two survive different lesions | Typing inter-module channels by band rather than by endpoint pair; deciding when a correlation is evidence of a connection; a routing diagnostic for where a unit's tuning arrives from; a weight-free spectral probe of which population integrates the other's residual |
| [[wiki/concepts/mean-field-reduction.md]] | The coarse-graining ladder that whole-brain models run on: spiking population → state density (Fokker–Planck, linear even when the neurons are chaotic) → neural mass (first moment only) → neural field (first moment over cortical position, with finite conduction delay) | Naming which rung a population model occupies and what it discarded to get there; a CANN as the zero-delay limit of a neural field; conduction delay as a stability parameter separable from coupling strength; the derived answer to *how many timescales* (the spectrum of the density operator, G67); and the measurement that moments 2 and 4 compute on the stimulus timescale, which a mean-only model cannot express |
| [[wiki/concepts/dynamic-repertoire.md]] | Spontaneous activity as noise-driven exploration of the network configurations reachable around a fixed structural skeleton — anatomy sets the manifold, delays set its geometry, noise supplies the energy, criticality makes the excursions expressible | Why a "functional network" is a window-dependent artefact and not an object; delay as the generator (not merely the stabiliser) of macroscopic dynamics; anticorrelation without mutual inhibition; exploration as a substrate operating point rather than a schedule (G61); the caution against fitting anything to a long-window functional connectome |
| [[wiki/concepts/metastability.md]] | The regime where subsets of a delayed, noisy oscillator network transiently synchronise while the whole never does, measured by `σ_R` — the temporal variance of the Kuramoto order parameter — and reached by tuning one global coupling gain to the edge of a bifurcation | A set-point statistic computable from module activity alone, with no weights, labels or task; the model-independence argument for criticality (six incompatible node models, one working point); ghost attractors as a task repertoire latent in the coupling at zero maintenance cost; the 0.35 Hz low-pass bound on what fMRI can arbitrate; why a functional-graph statistic reports coupling gain rather than topology |
| [[wiki/concepts/integration-segregation-balance.md]] | Network topology as a fast control variable: a modular system moves along a single between-module-connectivity axis (participation coefficient `B`) in seconds, graded by task demand, with within-module connectivity fixed | A weight-free, label-free runtime state variable for a modular architecture; the demand→routing-dispersion mapping as monotone rather than binary; integration as a throughput change (drift rate ↑, non-decision time ↓, decision boundary unchanged) rather than a speed–accuracy trade; ascending neuromodulatory gain as the one knob that moves it |
| [[wiki/concepts/excitation-inhibition-balance.md]] | Splitting each long-range projection into an excitatory-targeting and an inhibitory-targeting share makes the *ratio* a per-edge knob that moves functional connectivity monotonically from full antisynchronisation to full synchronisation — and the same ratio sets decision integration time and working-memory volatility | A speed–accuracy trade produced by input statistics with no threshold moved; stability and flexibility as one dial rather than two; a local homeostatic rate rule as the precondition for a global parameter being learnable; a Hebbian learning rule whose teaching signal is a network-level statistic; prediction-identifiability without parameter-identifiability (143,000 degenerate parameters, `r = 0.9962` reproducible dynamics) |
| [[wiki/concepts/anatomical-harmonic-modes.md]] | Cortical activity as a weighted sum of Laplacian eigenmodes of an anatomy-derived graph, `F(x,t) = Σ_k a_k(t) ψ_k(x)`; the winning graph is an exponential distance rule (`λ = 0.162 mm⁻¹`) **plus** its rare >3 SD long-range violations, and ~20 of 200 modes carry the bulk of both resting connectivity and 47 task maps | The wiki's only *generated* (not measured) low-dimensional coordinate system; rare long edges as basis-shaping rather than throughput-shaping; a spectral objective on wiring computable without running the network; the honest counterexample — better reconstruction, identical behavioural prediction |
| [[wiki/concepts/cortical-traveling-waves.md]] | Direction of phase propagation across a network as a property of the wiring: waves run from low to high **instrength** (`s_i = Σ_j a_ij`), the human connectome hosts that gradient in every individual, and removing conduction delays destroys the direction while leaving the waves | A routing prior computable in one line from the weight matrix, with nothing learned or gated; delay as the ingredient that converts a degree gradient into a flow direction; a signed, task-free monitoring statistic (the wave flow potential) for a modular architecture |
| [[wiki/concepts/broadcast-hierarchy.md]] | A hierarchy that is deep without being a chain: the apex receives selectively from high-level areas and projects diffusely to many levels and laminar types at once, so a global model revision costs one act rather than `L−1` relays; plus the laminar type rule that makes the levels a heterarchy, and abstraction implemented as anatomical dimensionality reduction | The challenge to the chain coupling every predictive architecture assumes (T259); an update *scope* variable the precision gates cannot express; a routing prior set by endpoint structure rather than learned; the trigger that turns an event boundary into a licence for a global, prior-sourced rewrite |
| [[wiki/concepts/microarchitectural-topography.md]] | The spatial layout of microcircuit type across a cortical region, measured by two scalars on a cytoarchitectural field — **smoothness** (variance explained by spatial axes) and **waviness** (deviation from the local mean) — distinguishing a *gradient* (similar units adjacent; progressive convergence) from *interdigitation* (contrasting units alternating; linking of disparate sources), both present in one network | Turning locality into a design variable: because >90% of cortico-cortical connections are between neighbouring microcircuits, where a module type is placed determines what it can cheaply combine, so an architecture gets a serial abstraction pipeline or a local cross-type binder from the same wiring cost depending on the layout — a step every wiki architecture skips by addressing modules by index; also two label-free, task-free interpretability statistics computable on any embedding of units |
| [[wiki/concepts/activity-baseline.md]] | Every activation is a difference against a control state, so any decrease can be re-described as a hidden increase in the control; the escape is a quantity that is invariant across the system at rest while the activity underneath varies several-fold (the oxygen extraction fraction, uniform despite ~4× variation in flow and metabolism), used as a zero point | The reference-point defect that every activation-based measurement in the wiki inherits (T260); the dissociation that tonic rate carries no information about engagement state; the rule that a module's baseline is its ecologically ordinary input, not its null input; and the sentinel — a monitoring channel that must run uncalled |
| [[wiki/concepts/parallel-timescale-streams.md]] | The connectome as six concurrent state streams rather than one: the same authored alphabet of 126 network-combination masks fits co-activation at infraslow, δ, θ, α, β and γ, with lifetimes ~20–3000 ms, preserved *relative* transition/occupancy structure, and cross-timescale overlap at chance | The wiki's only measured answer to G67 (how many timescales, where the boundaries go) — many at once, sharing a codebook and a transition prior but no clock; a runnable transition rule for module coalitions (~70% one-bit edits, ~10% long jumps); the test that separates one-stream-resampled from genuinely parallel multi-timescale models |
| [[wiki/concepts/node-definition-problem.md]] | The choice of vertex set as a modelling decision with a conservation law: relational content moves between node-internal structure and edges as units are coarsened or refined, hard partitions cannot express units belonging to two modules, and per-instance node *placement* is read out as edge *weight* | G27 made concrete — what has to be decided before a graph can be discovered at all, and why refining granularity does not settle it |
| [[wiki/concepts/effective-connectivity.md]] | Edges as *parameters of an inverted generative model* rather than statistics of the data — which is what buys direction, sign and within-node recurrence — estimated by the three-part template neuronal model + observation model + optimiser | The identifiability price list (one global gain at group level vs >40,000 per-subject connections, decided by model class not by data); log model evidence as a structure-selection criterion that prunes edges instead of shrinking them; the claim that a mechanistic connectome has no atlas because its edges are indexed by brain state (`A + Σ_k B^k u_k`); a fitted whole-brain model used as a stimulation planner |
| [[wiki/concepts/connectome-state-transformation.md]] | The map *between* two state-specific connectivity graphs, estimated as one rank-10 linear operator from a population and applied to individuals it was never fitted on; decomposes into state-general, domain-general and task-specific subsystems | Storing context as an operator on a graph rather than as a graph per context; the sample cost of each level of the slow-**W**/fast-**M** split; the empirical form of `A + Σ_k B^k u_k` |
| [[wiki/concepts/structured-flows-on-manifolds.md]] | Time-scale separation at an instability makes a few slow *order parameters* enslave the rest, so a low-dimensional manifold and the flow on it are consequences of the operating point — and heterogeneous long-range connections are what break the symmetry that otherwise leaves that flow degenerate | Where dimensionality reduction comes from rather than being applied; why fluidity and *low* dimensionality co-occur at the same coupling band; degeneracy as the geometrical counterpart of non-identifiability, hence which parameters are worth inferring, decidable before data |
| [[wiki/concepts/connectome-hubs-and-cores.md]] | The measured anatomy of the cortical structural graph: sparse (3% density), edge mass mostly local (54% within-unit, 4% interhemispheric), degree exponential not scale-free, six independent measures (degree, strength, betweenness, efficiency, k-core/s-core, participation index) converging on one bilateral posterior medial/parietal core | Where a modular architecture should put its integrative bottleneck and what that costs (>70% of between-module edge mass on the core; centrality→resting blood flow `r² = 0.49`); the connector-hub vs provincial-hub distinction as a runnable diagnostic on trained networks; the structure→function ceiling `r² = 0.53–0.62`; the structural half of the default-mode-network dispute (T241), including the one component — medial prefrontal cortex — that is not in the core |
| [[wiki/concepts/small-world-topology.md]] | The definition the wiki uses everywhere and never states: high clustering `C` plus short mean path length `L` at a sparse edge budget (`σ = (C/C_rand)/(L/L_rand) > 1`), a graph *property* distinct from the Watts–Strogatz generator that is usually confused with it | Naming the local-specialisation vs global-integration trade under a wiring-cost constraint; the concave return on long-range shortcuts as an unrun sparse-architecture ablation; and the negative result — why `σ` is too weak an instrument to license any architectural claim, next to participation index, k-core and instrength gradient |
| [[wiki/concepts/function-to-structure-inference.md]] | The reverse direction is ill-posed: thresholding a correlation matrix to recover the wiring gives ≈6% precision empirically and ≈28% even when the structural matrix *is* the generating coupling matrix, because a 3%-dense graph multiplies every false-positive rate by ~30 — plus the three structured residuals (1/distance, two-step path sums `Σ_k w_ik w_kj`, non-stationarity) | The quantitative ceiling on edge-existence discovery from co-statistics; why structural connectivity is a prior and not a target; why AUC without a prevalence is not a result |
| [[wiki/concepts/dynamic-network-connectivity.md]] | A second, content-free strength register on existing connections — spine-local K⁺/HCN/TRPC shunts set in seconds by arousal, energy state and task demand, leaving the wiring diagram untouched | The wiki's only run-time gain parameter on a store; capacity as an imposed stability margin rather than a resource ceiling; the scalar that switches control between a deliberative and a reflexive system; the closed loop from the controller onto its own modulators (G50) |
| [[wiki/concepts/neuromodulatory-metaparameters.md]] | The hyperparameters of learning (`α`, `β`, `γ`) as *global* quantities carried by dedicated diffuse broadcast channels, with set-points computed from second-order statistics of the learner’s own error signal | Metalearning as feedback control rather than as an outer loop; the wiki’s only rules for setting a learning rate, an exploration temperature and a planning horizon online (G50, G56, G61); the rival to the one-role precision account of neuromodulation; and the audit that breaks the one-chemical-one-knob mapping (co-release, projection- and compartment-specific targeting, wiring versus volume transmission — T281) |
| [[wiki/concepts/temporal-abstraction-options.md]] | An option `⟨I, π_o, β⟩` is a whole policy selectable as one action, turning the decision process semi-Markov; it buys structured exploration and coarse credit assignment, and creates the option-discovery problem | Hierarchy over *time*: compiled paths as coarse graph edges, bottleneck subgoals as the arrow from a discovered graph to a skill library, and the measured cost when the library is wrong (gap G33) |
| [[wiki/concepts/evidence-accumulation.md]] | Integrate noisy per-alternative evidence and commit when the *normalised* log-posterior over alternatives — not the leader's raw score — crosses a threshold; the multihypothesis sequential probability ratio test and its claimed cortico-basal-ganglia implementation | The wiki's only closed-form answer to *when is the answer good enough* (G15); the softmax denominator as a computation with its own substrate rather than a free arithmetic step; an inverse-variance gain whose miscalibration cost is one-sided; Hick's law (`ln N`) as a consequence of the stopping rule rather than of a capacity limit |
| [[wiki/concepts/subjective-value.md]] | The scalar a chooser actually compares: the outcome put through the agent's own delay-discount kernel, explicitly represented on a common currency in ventral striatum, medial prefrontal and posterior cingulate cortex, with the neural rate quantitatively matching the rate fitted to that person's choices | Where the discount lives (in the value, not only in the learning rule); the outcome-value vs. action-value split; the horizon as a stable per-agent trait rather than a regulated statistic; the hyperbolic-vs-`γ^k` question (T141); the wiki's strongest internal-representation probe — covariation *and* zero mean offset |
| [[wiki/concepts/learned-world-models.md]] | The learned dynamics model as an object: `p(z'\|z,a)` plus a read-out, serving three distinct roles (neural simulator, dynamics model for planning, reward model), built on a deterministic/stochastic/hybrid transition, with or without a decoder | The wave's hub — what a world model is made of, which design choices are load-bearing, why prediction fidelity does not order models by planning utility, and what the objective actually identifies — including the one condition that lives in the behaviour policy rather than in the model |
| [[wiki/concepts/cross-embodiment-transfer.md]] | Transfer of a policy or vocabulary across bodies: five families of shared interface (normalised action schema, shared trunk + per-embodiment heads, universal action codebook, embodiment-agnostic intermediates, latent actions from video), each keeping a body-specific decoder below the shared level | The `g`/`x` factorisation with the actuator held out; abstraction-as-invariance with an enumerable membership test; the standing objection to agent-relative symbols (T172) |
| [[wiki/concepts/cross-modal-grounding.md]] | Attaching a linguistic symbol to the visual entity, attribute or relation it denotes; the four VLM training families, the rate–distortion frame that unifies them, and the measured result that nouns bind and relations do not | Why caption supervision buys nodes and not edges; three shortcuts built into the field's own benchmarks; the reconstruction-vs-joint-embedding question (T162) |
| [[wiki/concepts/retrieval-capacity.md]] | How many distinct *retrieval sets* a similarity-ranked store can be asked for, bounded by embedding dimension: `d ≥ log C(n,k)/log(1+1/γ)` by sphere packing | The capacity number every memory read in the wiki was missing (items stored vs. questions askable); why a factorised `⟨f(q), g(x)⟩` score is rank-limited and a joint scorer is not; the compositional-query ceiling on precomputed keys (G37, G18, T163) |
| [[wiki/concepts/representational-collapse.md]] | The cheapest solution to every self-supervised objective is to represent nothing; six structurally different places a provision against it can sit, and the partial-collapse case where a read-out degrades under a correctly-minimised loss | G34, and therefore the first filter on any candidate objective for `g` (G30), since a constant structural code is trivially path-consistent; the wiki's most quantitative evidence on shortcut selection; why "certified by construction" narrowed to per-read-out monitoring |
| [[wiki/concepts/problem-framing.md]] | Solving splits into constructing a problem representation and optimising within it; every benchmark and architecture here supplies the first and scores the second | Why a high ARC score is evidence about search and not about abstraction upstream of it; the domain restriction on refinement and verification (free retries); where G73 and G74 came from |
| [[wiki/concepts/rule-level-evaluation.md]] | Score the *rule the solver states* alongside the grid it emits, classified against the abstraction the task was authored to elicit (correct-intended / correct-unintended / incorrect), with rule–grid alignment as the faithfulness control | The tenth shortcut instrument and the only one needing no distribution shift, no second model and no controlled dataset; the measured decomposition of a score into framing and optimisation; the two-way distortion of accuracy (over-reports abstraction in text, under-reports it in vision) |
| [[wiki/concepts/certification-instruments.md]] | The inventory of nineteen protocols proposed for answering "did this system discover the structure or fit a correlation?", numbered `I1`–`I19` by what each one needs, plus the fourteen failure modes shared across them | G17's answer column; the instrument any "architecture X reasons" claim in the wiki has to name; why an honest evaluation returns a facet vector rather than a number |
| [[wiki/concepts/human-baseline.md]] | Not a number but a protocol — pool, attempt budget, aggregation order, exclusion rule, item-pool identity — the same raw data yielding baselines 25 points apart | The denominator of every at/above/below-human claim in the wiki; the four different jobs the baseline is asked to do, only one of which is a denominator; the scale of developer-aware generalization difficulty |
| [[wiki/concepts/benchmark-contamination.md]] | The question upstream of every score: was the item withheld at all? Taxonomy (input vs input–output leak × train vs test split), the reference-paraphrase differential `δ_train − δ_test` that detects it, and the degeneracy that makes a low reading uninformative | Whether any accuracy in the wiki measures ability or recall; the consolidated list of the wiki's contamination instruments and what each is blind to; `I19` and `F14`; the API-submission channel that makes evaluating a closed model a leakage event |
| [[wiki/concepts/cross-paradigm-interface.md]] | A trained module *between* two networks that speak different representational languages (`Y = Q·F·H·W(X)`), carrying either content (transmission) or parameters (modulation) | The wiki's only inter-module connection with its own parameters and its own objective (G52); a threshold gate addressed by task similarity that beats EWC / synaptic intelligence / context-dependent gating over 40 tasks; and a spiking symbolic reasoner on CLEVRER in which nothing was learned in spikes |
| [[wiki/concepts/spike-train-error-metrics.md]] | The loss function for a temporally coded output: eight proposed errors between a desired and an emitted spike train, one of them derived from a likelihood rather than posited, and the phase-portrait argument for which converge | `G76`; what a precisely timed spike costs to store (0.14 patterns per synapse at 1 ms); the derivation that fixes the presynaptic eligibility kernel as the postsynaptic potential |
| [[wiki/concepts/default-self-model.md]] | A model of one's own strengths and weaknesses maintained at baseline, claimed to be what medial prefrontal cortex's high resting metabolism buys; its content axis (self-descriptiveness, medial prefrontal + posterior cingulate) dissociates from its valence axis (ventral anterior cingulate), and its accuracy is supplied by a *separate*, lesionable region (orbitofrontal cortex) | Competence self-knowledge as an architectural component (`G89`); the finding that an uncorrected self-model is inflated rather than merely noisy, so a calibrator must sit outside the estimating loop with an external referent; and the two-level-contrast non-identifiability that generalises to the wiki's evaluation instruments |

**Coverage.** **246 sources** across twelve waves. *Per-source detail is not kept here* — it lives in the git log, in the `From` column of [[wiki/architectural-gaps.md]] and in the provenance notes at the foot of that file, and thematically in [[wiki/overview.md]]. What this note keeps is the one thing none of those hold: **what kind of claim each wave added**, since the wiki grew by layer rather than by topic and the layer is what tells you which pages can contradict which.

| Layer | What it added | Where it lives |
|---|---|---|
| **Skeleton** | Mechanism plus its role in the framing, with no quantitative result — the shape the wiki was built on | [[wiki/concepts/neuroscience-ai-transfer.md]] |
| **Evaluation constraint** | The first claim that is about *measurement* rather than mechanism: the intended solution is not identifiable from data alone, which qualifies every page after it | [[wiki/concepts/shortcut-learning.md]] |
| **Update rule** | What changes a weight, compared *between* biologically-motivated methods rather than against engineering | [[wiki/concepts/synaptic-plasticity.md]], [[wiki/concepts/meta-optimized-plasticity.md]] |
| **Prior** | What is in the meta-graph before any learning happens, and its identification criterion (signature limits, defeasibility) | [[wiki/concepts/core-knowledge.md]] |
| **Ceiling** | What an optimal learner can and cannot be guaranteed — the wiki's first proof-backed limit | [[wiki/concepts/universal-induction.md]] |
| **Substrate** | The free-energy rival to the graph framing, and the first account of where discrete nodes come from at all | [[wiki/concepts/predictive-coding-free-energy.md]] |
| **Model-building** | Building a causal model rather than fitting a function; the first evaluation protocols needing no distribution shift | [[wiki/concepts/causal-model-building.md]], [[wiki/concepts/program-induction.md]] |
| **Specification** | What one actually writes down — objective, learning rule, architecture — which retro-fits a slot label onto every mechanism already here | [[wiki/concepts/three-component-framework.md]] |
| **Scoring** | Intelligence as a conversion rate from priors and experience into skill on unseen tasks | [[wiki/concepts/skill-acquisition-efficiency.md]] |
| **Design** | A full architecture proposal rather than a survey, a formalism or a critique — every empty slot filled, no experiment for any of them | [[wiki/concepts/energy-based-models.md]] |
| **Measurement** | The prediction/compression identity: the uncomputable ceiling becomes a number obtainable for any model on any byte stream | [[wiki/concepts/prediction-compression-equivalence.md]] |
| **Dual of measurement** | The only instrument that scores the system alone rather than against something external, with the verdict in the asymptote | [[wiki/concepts/intelligence-density.md]] |
| **Floor** | Surprisal, log-loss, free energy and code length named as one quantity, with the mode-covering optimum of forward KL as its payload | [[wiki/concepts/divergence-objectives.md]] |
| **Operation** | Retrieval as a decidable question — *does this stored structure occur here?* — moved from search into the geometry of an embedding | [[wiki/concepts/subgraph-matching.md]] |
| **Arbitration** | A warning about measurement: behavioural change decomposes into a stored state moved and a weight over unchanged states moved | [[wiki/concepts/contextual-inference.md]] |
| **Instrument** | The half of measurement that opens the system rather than scoring it from outside — and its ceiling (probes need labels) | [[wiki/concepts/representation-probing.md]] |
| **Level** | What shape the activity has *before* anything is named — dimensionality and manifold geometry as data | [[wiki/concepts/population-geometry.md]] |
| **Model** | The cognitive map's update rule made a property of the architecture, and the first gap moved *down* rather than opened | [[wiki/concepts/path-integration.md]], [[wiki/concepts/cognitive-map.md]] |
| **Write rule** | Nodes written in one shot and edges accumulated over many, as two operating points of one speed–amplitude principle | [[wiki/concepts/synaptic-plasticity.md]], [[wiki/concepts/offline-replay.md]] |
| **Replication** | The map's machinery is not localised: the same cell types outside the hippocampal formation | [[wiki/concepts/distributed-reference-frames.md]] |
| **Licence** | The argument that a result the wiki already relied on does not mean what it says — two-way non-identifiability of objective and representation | [[wiki/concepts/objective-identifiability.md]] |
| **Implementation** | The substrate carrying computational content rather than detail — sparse codes as a recognition mechanism, not only a capacity argument | [[wiki/concepts/sparse-distributed-representations.md]] |
| **Wiring** | A connectome as the whole payload: the laminar graph every "cortical column" claim had been assuming | [[wiki/concepts/canonical-cortical-microcircuit.md]] |
| **Valve** | The consolidation channel given a specification a builder can implement | [[wiki/concepts/recall-gated-consolidation.md]] |
| **Sign** | The same channel shown to be harmful when run unregulated — the first argument that a wanted mechanism needs a *brake* | [[wiki/concepts/generalization-optimized-consolidation.md]] |
| **Inventory** | Whether a controller has *contents* rather than only a process | [[wiki/concepts/controller-knowledge-vs-process.md]] |
| **Normative** (wave 6) | An objective that says what to *do* rather than what to represent, with the epistemic term as the contested part | [[wiki/concepts/expected-free-energy.md]], [[wiki/concepts/temporal-abstraction-options.md]] |
| **World model** (wave 7) | Self-supervised structure learning and its characteristic failure — the collapse a representation objective admits by construction | [[wiki/concepts/learned-world-models.md]], [[wiki/concepts/representational-collapse.md]] |
| **Sequence substrate** (wave 8) | How time is held at all: fixed random recurrence, continuous-time state, explicit state-space | [[wiki/concepts/attractor-dynamics.md]], [[wiki/concepts/test-time-training.md]] |
| **Transfer operation** (wave 9) | Analogy split into retrieval, mapping and transfer, with the cost measured to be in *retrieval* | [[wiki/concepts/analogical-mapping.md]], [[wiki/concepts/program-induction.md]] |
| **Certification** (wave 10) | The protocols themselves inventoried and priced, plus the two things every score presupposes — a human denominator and a withheld item | [[wiki/concepts/certification-instruments.md]], [[wiki/concepts/human-baseline.md]], [[wiki/concepts/benchmark-contamination.md]] |
| **Description adequacy** (wave 12) | The third evaluation question, arriving from sensory neurophysiology: not whether the system found the structure or contains it, but whether *our model of the system* predicts it off-distribution | [[wiki/concepts/predictive-adequacy.md]] |
| **Event substrate** (wave 11) | The spiking layer specified end to end — input code, output loss, unit timescale, and the interface to a rate network | [[wiki/concepts/spike-encoding-schemes.md]], [[wiki/concepts/spike-train-error-metrics.md]], [[wiki/concepts/neuronal-parameter-heterogeneity.md]], [[wiki/concepts/learnable-synaptic-delays.md]], [[wiki/concepts/cross-paradigm-interface.md]], [[wiki/concepts/circuit-size-separation.md]], [[wiki/concepts/analog-in-memory-computing.md]] |
| **Scale ladder** (wave 12) | How a population of neurons becomes a node in a whole-brain graph, what each coarse-graining step costs, what the assembled network does when nothing is asked of it, and where the wiring puts its bottleneck | [[wiki/concepts/mean-field-reduction.md]], [[wiki/concepts/dynamic-repertoire.md]], [[wiki/concepts/metastability.md]], [[wiki/concepts/cortical-traveling-waves.md]], [[wiki/concepts/node-definition-problem.md]], [[wiki/concepts/effective-connectivity.md]], [[wiki/concepts/structured-flows-on-manifolds.md]], [[wiki/concepts/connectome-hubs-and-cores.md]], [[wiki/concepts/small-world-topology.md]], [[wiki/concepts/function-to-structure-inference.md]], [[wiki/concepts/integration-segregation-balance.md]], [[wiki/concepts/excitation-inhibition-balance.md]], [[wiki/concepts/anatomical-harmonic-modes.md]], [[wiki/concepts/parallel-timescale-streams.md]], [[wiki/concepts/broadcast-hierarchy.md]], [[wiki/concepts/activity-baseline.md]], [[wiki/concepts/transthalamic-context-routing.md]] |

**How to read the table.** Layers added *before* the evaluation-constraint row are stated without an instrument behind them; layers from **Certification** onwards are the wiki's attempt to supply one retrospectively. The recurring pattern is that a mechanism layer and its instrument arrive several waves apart, and the gap between them is where most of [[wiki/empirical-tensions.md]] lives.
